import { t as __commonJSMin } from "./chunk-C-Qwzh9l.js";
import { t as require_buffer } from "./buffer-BFHFsWYa.js";
//#region browser-external:fs
var require_browser_external_fs = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = Object.create(new Proxy({}, { get(_, key) {
		if (key !== "__esModule" && key !== "__proto__" && key !== "constructor" && key !== "splice") console.warn(`Module "fs" has been externalized for browser compatibility. Cannot access "fs.${key}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
	} }));
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/file-header.js
var require_file_header = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = class FileHeader {
		/**
		* @type {number}
		*/
		ocadMark;
		/**
		* @type {number}
		*/
		fileType;
		/**
		* @type {number}
		*/
		version;
		/**
		* @type {number}
		*/
		subVersion;
		/**
		* @type {number}
		*/
		subSubVersion;
		/**
		* @type {number}
		*/
		symbolIndexBlock;
		/**
		* @type {number}
		*/
		objectIndexBlock;
		/**
		* @type {number}
		*/
		offlineSyncSerial;
		/**
		* @type {number}
		*/
		currentFileVersion;
		/**
		* @type {number}
		*/
		stringIndexBlock;
		/**
		* @type {number}
		*/
		fileNamePos;
		/**
		* @type {number}
		*/
		fileNameSize;
		/**
		* @type {number}
		*/
		mrStartBlockPosition;
		/**
		* @param {import('./buffer-reader')} reader
		*/
		constructor(reader) {
			if (reader.buffer.length - reader.offset < 60) throw new Error("Not an OCAD file (not large enough to hold header)");
			this.ocadMark = reader.readSmallInt();
			this.fileType = reader.readByte();
			reader.readByte();
			this.version = reader.readSmallInt();
			this.subVersion = reader.readByte();
			this.subSubVersion = reader.readByte();
			this.symbolIndexBlock = reader.readCardinal();
			this.objectIndexBlock = reader.readCardinal();
			this.offlineSyncSerial = reader.readInteger();
			this.currentFileVersion = reader.readInteger();
			reader.readCardinal();
			reader.readCardinal();
			this.stringIndexBlock = reader.readCardinal();
			this.fileNamePos = reader.readCardinal();
			this.fileNameSize = reader.readCardinal();
			reader.readCardinal();
			reader.readCardinal();
			reader.readCardinal();
			this.mrStartBlockPosition = reader.readCardinal();
		}
		/**
		* Tells if this is a valid OCAD file (magic number is correct).
		* @returns {boolean}
		*/
		isValid() {
			return this.ocadMark === 3245;
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/td-poly.js
var require_td_poly = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Represents a TDPoly, which is a coordinate pair with optional flags.
	* The class is an array of X and Y coordinates, with the flags stored in
	* the `xFlags` and `yFlags` properties.
	*
	* OCAD coordinates use 1/100 mm units, unmanipulated coordinates from an OCAD
	* file are 24 bit signed integers.
	*
	* @extends {Array<number>}
	*/
	var TdPoly = class TdPoly extends Array {
		/**
		* @type {number}
		*/
		xFlags;
		/**
		* @type {number}
		*/
		yFlags;
		/**
		* @param {number} ocadX
		* @param {number} ocadY
		* @param {number} [xFlags]
		* @param {number} [yFlags]
		*/
		constructor(ocadX, ocadY, xFlags, yFlags) {
			super(xFlags === void 0 ? ocadX >> 8 : ocadX, yFlags === void 0 ? ocadY >> 8 : ocadY);
			this.xFlags = xFlags === void 0 ? ocadX & 255 : xFlags;
			this.yFlags = yFlags === void 0 ? ocadY & 255 : yFlags;
		}
		isFirstBezier() {
			return !!(this.xFlags & 1);
		}
		isSecondBezier() {
			return !!(this.xFlags & 2);
		}
		hasNoLeftLine() {
			return this.xFlags & 4;
		}
		isBorderOrVirtualLine() {
			return !!(this.xFlags & 8);
		}
		isCornerPoint() {
			return !!(this.yFlags & 1);
		}
		isFirstHolePoint() {
			return !!(this.yFlags & 2);
		}
		hasNoRightLine() {
			return this.yFlags & 4;
		}
		isDashPoint() {
			return !!(this.yFlags & 8);
		}
		vLength() {
			return Math.sqrt(this[0] * this[0] + this[1] * this[1]);
		}
		add(c1) {
			return new TdPoly(this[0] + c1[0], this[1] + c1[1], this.xFlags, this.yFlags);
		}
		sub(c1) {
			return new TdPoly(this[0] - c1[0], this[1] - c1[1], this.xFlags, this.yFlags);
		}
		mul(f) {
			return new TdPoly(this[0] * f, this[1] * f, this.xFlags, this.yFlags);
		}
		unit() {
			const l = this.vLength();
			return this.mul(1 / l);
		}
		rotate(theta) {
			return new TdPoly(this[0] * Math.cos(theta) - this[1] * Math.sin(theta), this[0] * Math.sin(theta) + this[1] * Math.cos(theta), this.xFlags, this.yFlags);
		}
		/**
		* Compare the coordinates of this `TdPoly` to another `TdPoly`.
		* @param {TdPoly} other
		* @returns `true` if the X and Y of both coordinates are equal
		*/
		equalCoords(other) {
			return this[0] === other[0] && this[1] === other[1];
		}
	};
	/**
	* Instantiate a TdPoly from a pair of coordinates.
	* @param {number} x
	* @param {number} y
	* @returns {TdPoly}
	*/
	TdPoly.fromCoords = (x, y) => new TdPoly(x << 8, y << 8);
	module.exports = TdPoly;
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/invalid-symbol-element-exception.js
var require_invalid_symbol_element_exception = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = class InvalidSymbolElementException extends Error {
		constructor(msg, symbolElement) {
			super(msg);
			this.symbolElement = symbolElement;
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/symbol-element.js
var require_symbol_element = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TdPoly = require_td_poly();
	var InvalidSymbolElementException = require_invalid_symbol_element_exception();
	/**
	* @class SymbolElement
	* @property {number} type
	* @property {number} flags
	* @property {number} color
	* @property {number} lineWidth
	* @property {number} diameter
	* @property {number} numberCoords
	* @property {TdPoly[]} coords
	*/
	module.exports = class SymbolElement {
		/**
		* @type {number}
		*/
		type;
		/**
		* @type {number}
		*/
		flags;
		/**
		* @type {number}
		*/
		color;
		/**
		* @type {number}
		*/
		lineWidth;
		/**
		* @type {number}
		*/
		diameter;
		/**
		* @type {number}
		*/
		numberCoords;
		/**
		* @type {TdPoly[]}
		*/
		coords;
		/**
		* @param {import('./buffer-reader')} reader
		*/
		constructor(reader) {
			this.type = reader.readSmallInt();
			this.flags = reader.readWord();
			this.color = reader.readSmallInt();
			this.lineWidth = reader.readSmallInt();
			this.diameter = reader.readSmallInt();
			this.numberCoords = reader.readSmallInt();
			reader.readCardinal();
			if (this.type < 1 || this.type > 4) throw new InvalidSymbolElementException(`Symbol element with invalid type (${this.type}).`);
			if (this.numberCoords >= 0) {
				this.coords = new Array(this.numberCoords);
				for (let j = 0; j < this.numberCoords; j++) this.coords[j] = new TdPoly(reader.readInteger(), reader.readInteger());
			} else throw new InvalidSymbolElementException(`Symbol element with invalid (${this.numberCoords}) number of coordinates.`, this);
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/symbol.js
var require_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var SymbolElement = require_symbol_element();
	var InvalidSymbolElementException = require_invalid_symbol_element_exception();
	/**
	* @typedef {import('./buffer-reader')} BufferReader
	*/
	/**
	* @typedef {import('./symbol-types').SymbolType} SymbolType
	*/
	/**
	* @typedef {Object} BaseSymbolProps
	* @property  {Error[]} warnings
	* @property  {number} size
	* @property  {number} symNum
	* @property  {string} number
	* @property  {number} otp
	* @property  {number} flags
	* @property  {boolean} selected
	* @property  {number} status
	* @property  {number} preferredDrawingTool
	* @property  {number} csMode
	* @property  {number} csObjType
	* @property  {number} csCdFlags
	* @property  {number} extent
	* @property  {number} filePos
	* @property  {number} group
	* @property  {number} nColors
	* @property  {number[]} colors
	* @property  {string} description
	* @property  {number[]} iconBits
	* @property {() => boolean} isHidden
	*/
	/**
	* @typedef {import('./area-symbol').AreaSymbolDef} AreaSymbol
	*/
	/**
	* @typedef {import('./line-symbol').LineSymbol} LineSymbol
	*/
	/**
	* @typedef {import('./point-symbol').PointSymbolDef} PointSymbol
	*/
	/**
	* @typedef {import('./text-symbol').TextSymbolDef} TextSymbol
	*/
	/**
	* @typedef {AreaSymbol|LineSymbol|PointSymbol|TextSymbol} BaseSymbolDef
	*/
	var BaseSymbol = class {
		/**
		* @type {Error[]}
		*/
		warnings;
		/**
		* @type {number}
		*/
		size;
		/**
		* @type {number}
		*/
		symNum;
		/**
		* @type {string}
		*/
		number;
		/**
		* @type {number}
		*/
		otp;
		/**
		* @type {number}
		*/
		flags;
		/**
		* @type {boolean}
		*/
		selected;
		/**
		* @type {number}
		*/
		status;
		/**
		* @type {number}
		*/
		preferredDrawingTool;
		/**
		* @type {number}
		*/
		csMode;
		/**
		* @type {number}
		*/
		csObjType;
		/**
		* @type {number}
		*/
		csCdFlags;
		/**
		* @type {number}
		*/
		extent;
		/**
		* @type {number}
		*/
		filePos;
		/**
		* @type {number}
		*/
		group;
		/**
		* @type {number}
		*/
		nColors;
		/**
		* @type {number[]}
		*/
		colors;
		/**
		* @type {string}
		*/
		description;
		/**
		* @type {number[]}
		*/
		iconBits;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			this.warnings = [];
			this.size = reader.readInteger();
			this.symNum = reader.readInteger();
			this.number = `${Math.floor(this.symNum / 1e3)}.${this.symNum % 1e3}`;
			this.otp = reader.readByte();
			this.flags = reader.readByte();
			this.selected = !!reader.readByte();
			this.status = reader.readByte();
			this.preferredDrawingTool = reader.readByte();
			this.csMode = reader.readByte();
			this.csObjType = reader.readByte();
			this.csCdFlags = reader.readByte();
			this.extent = reader.readInteger();
			this.filePos = reader.readCardinal();
		}
		/**
		* @param {BufferReader} reader
		* @param {number} dataSize
		* @returns {SymbolElement[]}
		*/
		readElements(reader, dataSize) {
			const elements = [];
			for (let i = 0; i < dataSize; i += 2) try {
				reader.push(reader.offset);
				const element = new SymbolElement(reader);
				elements.push(element);
				i += element.numberCoords;
			} catch (e) {
				if (e instanceof InvalidSymbolElementException) this.warnings.push(e);
				else throw e;
			} finally {
				const size = reader.getSize();
				reader.pop();
				reader.skip(size);
			}
			return elements;
		}
		isHidden() {
			return (this.status & 2) === 2;
		}
	};
	var Symbol10 = class extends BaseSymbol {
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			this.group = reader.readSmallInt();
			this.nColors = reader.readSmallInt();
			this.colors = new Array(14);
			for (let i = 0; i < this.colors.length; i++) this.colors[i] = reader.readSmallInt();
			this.description = "";
			reader.readByte();
			for (let i = 1; i < 32; i++) {
				const c = reader.readByte();
				if (c) this.description += String.fromCharCode(c);
			}
			this.iconBits = new Array(484);
			for (let i = 0; i < this.iconBits.length; i++) this.iconBits[i] = reader.readByte();
		}
	};
	var Symbol11 = class extends BaseSymbol {
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			reader.readByte();
			reader.readByte();
			this.nColors = reader.readSmallInt();
			this.colors = new Array(14);
			for (let i = 0; i < this.colors.length; i++) this.colors[i] = reader.readSmallInt();
			this.description = "";
			for (let i = 0; i < 64 / 2; i++) {
				const c = reader.readWord();
				if (c) this.description += String.fromCharCode(c);
			}
			this.iconBits = new Array(484);
			for (let i = 0; i < this.iconBits.length; i++) this.iconBits[i] = reader.readByte();
			this.symbolTreeGroup = new Array(64);
			for (let i = 0; i < this.symbolTreeGroup.length; i++) this.symbolTreeGroup[i] = reader.readWord();
		}
	};
	module.exports = {
		Symbol10,
		Symbol11
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/point-symbol.js
var require_point_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { Symbol10, Symbol11 } = require_symbol();
	/**
	* @typedef {import('./buffer-reader')} BufferReader
	*/
	/**
	* @typedef {object} PointSymbolProps
	* @property {1} type
	* @property {number} dataSize
	*/
	/** @typedef {import('./symbol').BaseSymbolProps & PointSymbolProps} PointSymbolDef */
	/** @implements {PointSymbolDef} */
	var PointSymbol10 = class extends Symbol10 {
		/**
		* @type {1}
		*/
		type;
		/**
		* @type {number}
		*/
		dataSize;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			this.type = 1;
			this.dataSize = reader.readWord();
			reader.readSmallInt();
			this.elements = this.readElements(reader, this.dataSize);
		}
	};
	/** @implements {PointSymbolDef} */
	var PointSymbol11 = class extends Symbol11 {
		/**
		* @type {1}
		*/
		type;
		/**
		* @type {number}
		*/
		dataSize;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			reader.skip(64);
			this.type = 1;
			this.dataSize = reader.readWord();
			reader.readSmallInt();
			this.elements = this.readElements(reader, this.dataSize);
		}
	};
	module.exports = {
		10: PointSymbol10,
		11: PointSymbol11,
		12: PointSymbol11,
		2018: PointSymbol11
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/line-symbol.js
var require_line_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { Symbol10, Symbol11 } = require_symbol();
	/**
	* @typedef {import('./symbol-element')} SymbolElement
	*/
	/**
	* @typedef {import('./symbol').BaseSymbolProps} BaseSymbolProps
	*/
	/**
	* @typedef {Object} DecreaseDef10
	* @property {number} decMode
	* @property {number} decLast
	* @property {number} res
	*/
	/**
	* @typedef {Object} DecreaseDef11
	* @property {number} decMode
	* @property {number} decSymbolSize
	* @property {boolean} decSymbolDistance
	* @property {boolean} decSymbolWidth
	* @property {number} decSymbolSize
	*/
	/**
	* @typedef {DecreaseDef10|Decrease11} DecreaseDef
	*/
	/**
	* @typedef {object} LineSymbolType
	* @property {2} type
	* @property {number} lineColor
	* @property {number} lineWidth
	* @property {number} lineStyle
	* @property {number} distFromStart
	* @property {number} distToEnd
	* @property {number} mainLength
	* @property {number} endLength
	* @property {number} mainGap
	* @property {number} secGap
	* @property {number} endGap
	* @property {number} minSym
	* @property {number} nPrimSym
	* @property {number} primSymDist
	
	* @property {BaseDoubleLine} doubleLine
	* @property {DecreaseDef} decrease
	
	* @property {number} frColor
	* @property {number} frWidth
	* @property {number} frStyle
	* @property {number} primDSize
	* @property {number} secDSize
	* @property {number} cornerDSize
	* @property {number} startDSize
	* @property {number} endDSize
	* @property {number} useSymbolFlags
	* @property {number} reserved
	
	* @property {SymbolElement[]} primSymElements
	* @property {SymbolElement[]} secSymElements
	* @property {SymbolElement[]} cornerSymElements
	* @property {SymbolElement[]} startSymElements
	* @property {SymbolElement[]} endSymElements
	* 
	* @property {(reader: BufferReader, dataSize: number) => SymbolElement[]} readElements
	* 
	* @typedef {BaseSymbolProps & LineSymbolType} LineSymbol
	* @property {import('./symbol-types').LineSymbolType} type
	*/
	/** @typedef {import("./buffer-reader")} BufferReader */
	/** @implements {LineSymbolType} */
	var LineSymbol10 = class extends Symbol10 {
		/** @type {2} */
		type;
		/** @type {number} */
		lineColor;
		/** @type {number} */
		lineWidth;
		/** @type {number} */
		lineStyle;
		/** @type {number} */
		distFromStart;
		/** @type {number} */
		distToEnd;
		/** @type {number} */
		mainLength;
		/** @type {number} */
		endLength;
		/** @type {number} */
		mainGap;
		/** @type {number} */
		secGap;
		/** @type {number} */
		endGap;
		/** @type {number} */
		minSym;
		/** @type {number} */
		nPrimSym;
		/** @type {number} */
		primSymDist;
		/** @type {BaseDoubleLine} */
		doubleLine;
		/** @type {DecreaseDef} */
		decrease;
		/** @type {number} */
		frColor;
		/** @type {number} */
		frWidth;
		/** @type {number} */
		frStyle;
		/** @type {number} */
		primDSize;
		/** @type {number} */
		secDSize;
		/** @type {number} */
		cornerDSize;
		/** @type {number} */
		startDSize;
		/** @type {number} */
		endDSize;
		/** @type {number} */
		useSymbolFlags;
		/** @type {number} */
		reserved;
		/** @type {SymbolElement[]} */
		primSymElements;
		/** @type {SymbolElement[]} */
		secSymElements;
		/** @type {SymbolElement[]} */
		cornerSymElements;
		/** @type {SymbolElement[]} */
		startSymElements;
		/** @type {SymbolElement[]} */
		endSymElements;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			readLineSymbol(this, reader, DoubleLine10, Decrease10);
		}
	};
	/** @implements {LineSymbolType} */
	var LineSymbol11 = class extends Symbol11 {
		/** @type {2} */
		type;
		/** @type {number} */
		lineColor;
		/** @type {number} */
		lineWidth;
		/** @type {number} */
		lineStyle;
		/** @type {number} */
		distFromStart;
		/** @type {number} */
		distToEnd;
		/** @type {number} */
		mainLength;
		/** @type {number} */
		endLength;
		/** @type {number} */
		mainGap;
		/** @type {number} */
		secGap;
		/** @type {number} */
		endGap;
		/** @type {number} */
		minSym;
		/** @type {number} */
		nPrimSym;
		/** @type {number} */
		primSymDist;
		/** @type {BaseDoubleLine} */
		doubleLine;
		/** @type {DecreaseDef} */
		decrease;
		/** @type {number} */
		frColor;
		/** @type {number} */
		frWidth;
		/** @type {number} */
		frStyle;
		/** @type {number} */
		primDSize;
		/** @type {number} */
		secDSize;
		/** @type {number} */
		cornerDSize;
		/** @type {number} */
		startDSize;
		/** @type {number} */
		endDSize;
		/** @type {number} */
		useSymbolFlags;
		/** @type {number} */
		reserved;
		/** @type {SymbolElement[]} */
		primSymElements;
		/** @type {SymbolElement[]} */
		secSymElements;
		/** @type {SymbolElement[]} */
		cornerSymElements;
		/** @type {SymbolElement[]} */
		startSymElements;
		/** @type {SymbolElement[]} */
		endSymElements;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			reader.skip(64);
			readLineSymbol(this, reader, DoubleLine11, Decrease11);
		}
	};
	var BaseDoubleLine = class {
		/** @type {number} */
		dblMode;
		/** @type {number} */
		dblFlags;
		/** @type {number} */
		dblFillColor;
		/** @type {number} */
		dblLeftColor;
		/** @type {number} */
		dblRightColor;
		/** @type {number} */
		dblWidth;
		/** @type {number} */
		dblLeftWidth;
		/** @type {number} */
		dblRightWidth;
		/** @type {number} */
		dblLength;
		/** @type {number} */
		dblGap;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			this.dblMode = reader.readWord();
			this.dblFlags = reader.readWord();
			this.dblFillColor = reader.readSmallInt();
			this.dblLeftColor = reader.readSmallInt();
			this.dblRightColor = reader.readSmallInt();
			this.dblWidth = reader.readSmallInt();
			this.dblLeftWidth = reader.readSmallInt();
			this.dblRightWidth = reader.readSmallInt();
			this.dblLength = reader.readSmallInt();
			this.dblGap = reader.readSmallInt();
		}
	};
	var DoubleLine10 = class extends BaseDoubleLine {
		/** @type {number[]} */
		dblRes;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			this.dblRes = new Array(3);
			for (let i = 0; i < this.dblRes.length; i++) this.dblRes[i] = reader.readSmallInt();
		}
	};
	var DoubleLine11 = class extends BaseDoubleLine {
		/** @type {number[]} */
		dblRes;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			this.dblBackgroundColor = reader.readSmallInt();
			this.dblRes = new Array(2);
			for (let i = 0; i < this.dblRes.length; i++) this.dblRes[i] = reader.readSmallInt();
		}
	};
	var Decrease10 = class {
		/** @type {number} */
		decMode;
		/** @type {number} */
		decLast;
		/** @type {number} */
		res;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			this.decMode = reader.readWord();
			this.decLast = reader.readWord();
			this.res = reader.readWord();
		}
	};
	var Decrease11 = class {
		/** @type {number} */
		decMode;
		/** @type {number} */
		decSymbolSize;
		/** @type {boolean} */
		decSymbolDistance;
		/** @type {boolean} */
		decSymbolWidth;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			this.decMode = reader.readWord();
			this.decSymbolSize = reader.readSmallInt();
			this.decSymbolDistance = !!reader.readByte();
			this.decSymbolWidth = !!reader.readByte();
		}
	};
	/**
	* @param {LineSymbol} symbol
	* @param {BufferReader} reader
	* @param {typeof DoubleLine10 | typeof DoubleLine11} DoubleLine
	* @param {typeof Decrease10 | typeof Decrease11} Decrease
	*/
	var readLineSymbol = (symbol, reader, DoubleLine, Decrease) => {
		symbol.type = 2;
		symbol.lineColor = reader.readSmallInt();
		symbol.lineWidth = reader.readSmallInt();
		symbol.lineStyle = reader.readSmallInt();
		symbol.distFromStart = reader.readSmallInt();
		symbol.distToEnd = reader.readSmallInt();
		symbol.mainLength = reader.readSmallInt();
		symbol.endLength = reader.readSmallInt();
		symbol.mainGap = reader.readSmallInt();
		symbol.secGap = reader.readSmallInt();
		symbol.endGap = reader.readSmallInt();
		symbol.minSym = reader.readSmallInt();
		symbol.nPrimSym = reader.readSmallInt();
		symbol.primSymDist = reader.readSmallInt();
		symbol.doubleLine = new DoubleLine(reader);
		symbol.decrease = new Decrease(reader);
		symbol.frColor = reader.readSmallInt();
		symbol.frWidth = reader.readSmallInt();
		symbol.frStyle = reader.readSmallInt();
		symbol.primDSize = reader.readWord();
		symbol.secDSize = reader.readWord();
		symbol.cornerDSize = reader.readWord();
		symbol.startDSize = reader.readWord();
		symbol.endDSize = reader.readWord();
		symbol.useSymbolFlags = reader.readByte();
		symbol.reserved = reader.readByte();
		symbol.primSymElements = symbol.readElements(reader, symbol.primDSize);
		symbol.secSymElements = symbol.readElements(reader, symbol.secDSize);
		symbol.cornerSymElements = symbol.readElements(reader, symbol.cornerDSize);
		symbol.startSymElements = symbol.readElements(reader, symbol.startDSize);
		symbol.endSymElements = symbol.readElements(reader, symbol.endDSize);
	};
	module.exports = {
		10: LineSymbol10,
		11: LineSymbol11,
		12: LineSymbol11,
		2018: LineSymbol11
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/area-symbol.js
var require_area_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { Symbol10, Symbol11 } = require_symbol();
	/**
	* @typedef {import('./buffer-reader')} BufferReader
	*/
	/**
	* @typedef {import('./symbol-element')} SymbolElement
	*/
	/**
	* @typedef {object} AreaSymbolProps
	*
	* @property {3} type
	* @property {number} borderSym
	* @property {number} fillColor
	* @property {number} hatchMode
	* @property {number} hatchColor
	* @property {number} hatchLineWidth
	* @property {number} hatchDist
	* @property {number} hatchAngle1
	* @property {number} hatchAngle2
	* @property {boolean} fillOn
	* @property {boolean} borderOn
	* @property {number} structMode
	* @property {number} structWidth
	* @property {number} structHeight
	* @property {number} structAngle
	* @property {number} structRes
	* @property {number} dataSize
	* @property {SymbolElement[]} elements
	*/
	/** @typedef {import('./symbol').BaseSymbolProps & AreaSymbolProps} AreaSymbolDef */
	/** @implements {AreaSymbolDef} */
	var AreaSymbol10 = class extends Symbol10 {
		/**
		* @type {3}
		*/
		type;
		/**
		* @type {number}
		*/
		borderSym;
		/**
		* @type {number}
		*/
		fillColor;
		/**
		* @type {number}
		*/
		hatchMode;
		/**
		* @type {number}
		*/
		hatchColor;
		/**
		* @type {number}
		*/
		hatchLineWidth;
		/**
		* @type {number}
		*/
		hatchDist;
		/**
		* @type {number}
		*/
		hatchAngle1;
		/**
		* @type {number}
		*/
		hatchAngle2;
		/**
		* @type {boolean}
		*/
		fillOn;
		/**
		* @type {boolean}
		*/
		borderOn;
		/**
		* @type {number}
		*/
		structMode;
		/**
		* @type {number}
		*/
		structWidth;
		/**
		* @type {number}
		*/
		structHeight;
		/**
		* @type {number}
		*/
		structAngle;
		/**
		* @type {number}
		*/
		structRes;
		/**
		* @type {number}
		*/
		dataSize;
		/**
		* @type {SymbolElement[]}
		*/
		elements;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			this.type = 3;
			this.borderSym = reader.readInteger();
			this.fillColor = reader.readSmallInt();
			this.hatchMode = reader.readSmallInt();
			this.hatchColor = reader.readSmallInt();
			this.hatchLineWidth = reader.readSmallInt();
			this.hatchDist = reader.readSmallInt();
			this.hatchAngle1 = reader.readSmallInt();
			this.hatchAngle2 = reader.readSmallInt();
			this.fillOn = !!reader.readByte();
			this.borderOn = !!reader.readByte();
			this.structMode = reader.readSmallInt();
			this.structWidth = reader.readSmallInt();
			this.structHeight = reader.readSmallInt();
			this.structAngle = reader.readSmallInt();
			this.structRes = reader.readSmallInt();
			this.dataSize = reader.readWord();
			this.elements = this.readElements(reader, this.dataSize);
		}
	};
	/** @implements {AreaSymbolDef} */
	var AreaSymbol11 = class extends Symbol11 {
		/**
		* @type {3}
		*/
		type;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			reader.skip(64);
			this.type = 3;
			this.borderSym = reader.readInteger();
			this.fillColor = reader.readSmallInt();
			this.hatchMode = reader.readSmallInt();
			this.hatchColor = reader.readSmallInt();
			this.hatchLineWidth = reader.readSmallInt();
			this.hatchDist = reader.readSmallInt();
			this.hatchAngle1 = reader.readSmallInt();
			this.hatchAngle2 = reader.readSmallInt();
			this.fillOn = !!reader.readByte();
			this.borderOn = !!reader.readByte();
			this.structMode = reader.readSmallInt();
			this.structWidth = reader.readSmallInt();
			this.structHeight = reader.readSmallInt();
			this.structAngle = reader.readSmallInt();
			this.structRes = reader.readSmallInt();
			this.dataSize = reader.readWord();
			this.elements = this.readElements(reader, this.dataSize);
		}
	};
	/** @implements {AreaSymbolDef} */
	var AreaSymbol12 = class extends Symbol11 {
		/**
		* @type {3}
		*/
		type;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			reader.skip(64);
			this.type = 3;
			this.borderSym = reader.readInteger();
			this.fillColor = reader.readSmallInt();
			this.hatchMode = reader.readSmallInt();
			this.hatchColor = reader.readSmallInt();
			this.hatchLineWidth = reader.readSmallInt();
			this.hatchDist = reader.readSmallInt();
			this.hatchAngle1 = reader.readSmallInt();
			this.hatchAngle2 = reader.readSmallInt();
			this.fillOn = !!reader.readByte();
			this.borderOn = !!reader.readByte();
			this.structMode = reader.readByte();
			this.structDraw = reader.readByte();
			this.structWidth = reader.readSmallInt();
			this.structHeight = reader.readSmallInt();
			this.structAngle = reader.readSmallInt();
			this.structIrregularVarX = reader.readByte();
			this.structIrregularVarY = reader.readByte();
			this.structIrregularMinDist = reader.readSmallInt();
			this.structRes = reader.readSmallInt();
			this.dataSize = reader.readWord();
			this.elements = this.readElements(reader, this.dataSize);
		}
	};
	module.exports = {
		10: AreaSymbol10,
		11: AreaSymbol11,
		12: AreaSymbol12,
		2018: AreaSymbol12
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/text-symbol.js
var require_text_symbol = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { Symbol10, Symbol11 } = require_symbol();
	/** @typedef {import('./buffer-reader')} BufferReader */
	/**
	* @typedef {object} TextSymbolProps
	* @property {4} type
	* @property {string} fontName
	* @property {number} fontColor
	* @property {number} fontSize
	* @property {number} weight
	* @property {boolean} italic
	* @property {number} res1
	* @property {number} charSpace
	* @property {number} wordSpace
	* @property {number} alignment
	* @property {number} lineSpace
	* @property {number} paraSpace
	* @property {number} indentFirst
	* @property {number} indentOther
	* @property {number} nTabs
	* @property {number[]} tabs
	* @property {boolean} lbOn
	* @property {number} lbColor
	* @property {number} lbWidth
	* @property {number} lbDist
	* @property {number} res2
	* @property {number} frMode
	* @property {number} frStyle
	* @property {boolean} pointSymOn
	* @property {number} pointSymNumber
	* @property {() => number} getHorizontalAlignment
	* @property {() => number} getVerticalAlignment
	*/
	/** @typedef {TextSymbolProps & import('./symbol').BaseSymbolProps} TextSymbolDef */
	/** @implements {TextSymbolDef} */
	var TextSymbol10 = class extends Symbol10 {
		/**
		* @type {4}
		*/
		type;
		/**
		* @type {string}
		*/
		fontName;
		/**
		* @type {number}
		*/
		fontColor;
		/**
		* @type {number}
		*/
		fontSize;
		/**
		* @type {number}
		*/
		weight;
		/**
		* @type {boolean}
		*/
		italic;
		/**
		* @type {number}
		*/
		res1;
		/**
		* @type {number}
		*/
		charSpace;
		/**
		* @type {number}
		*/
		wordSpace;
		/**
		* @type {number}
		*/
		alignment;
		/**
		* @type {number}
		*/
		lineSpace;
		/**
		* @type {number}
		*/
		paraSpace;
		/**
		* @type {number}
		*/
		indentFirst;
		/**
		* @type {number}
		*/
		indentOther;
		/**
		* @type {number}
		*/
		nTabs;
		/**
		* @type {number[]}
		*/
		tabs;
		/**
		* @type {boolean}
		*/
		lbOn;
		/**
		* @type {number}
		*/
		lbColor;
		/**
		* @type {number}
		*/
		lbWidth;
		/**
		* @type {number}
		*/
		lbDist;
		/**
		* @type {number}
		*/
		res2;
		/**
		* @type {number}
		*/
		frMode;
		/**
		* @type {number}
		*/
		frStyle;
		/**
		* @type {boolean}
		*/
		pointSymOn;
		/**
		* @type {number}
		*/
		pointSymNumber;
		/**
		* @param {BufferReader} reader
		*/
		constructor(reader) {
			super(reader);
			readTextSymbol(this, reader);
		}
		getVerticalAlignment() {
			return verticalAlignment(this.alignment);
		}
		getHorizontalAlignment() {
			return horizontalAlignment(this.alignment);
		}
	};
	/** @implements {TextSymbolDef} */
	var TextSymbol11 = class extends Symbol11 {
		/**
		* @type {4}
		*/
		type;
		/**
		* @type {string}
		*/
		fontName;
		/**
		* @type {number}
		*/
		fontColor;
		/**
		* @type {number}
		*/
		fontSize;
		/**
		* @type {number}
		*/
		weight;
		/**
		* @type {boolean}
		*/
		italic;
		/**
		* @type {number}
		*/
		res1;
		/**
		* @type {number}
		*/
		charSpace;
		/**
		* @type {number}
		*/
		wordSpace;
		/**
		* @type {number}
		*/
		alignment;
		/**
		* @type {number}
		*/
		lineSpace;
		/**
		* @type {number}
		*/
		paraSpace;
		/**
		* @type {number}
		*/
		indentFirst;
		/**
		* @type {number}
		*/
		indentOther;
		/**
		* @type {number}
		*/
		nTabs;
		/**
		* @type {number[]}
		*/
		tabs;
		/**
		* @type {boolean}
		*/
		lbOn;
		/**
		* @type {number}
		*/
		lbColor;
		/**
		* @type {number}
		*/
		lbWidth;
		/**
		* @type {number}
		*/
		lbDist;
		/**
		* @type {number}
		*/
		res2;
		/**
		* @type {number}
		*/
		frMode;
		/**
		* @type {number}
		*/
		frStyle;
		/**
		* @type {boolean}
		*/
		pointSymOn;
		/**
		* @type {number}
		*/
		pointSymNumber;
		constructor(reader) {
			super(reader);
			reader.skip(64);
			readTextSymbol(this, reader);
		}
		getVerticalAlignment() {
			return verticalAlignment(this.alignment);
		}
		getHorizontalAlignment() {
			return horizontalAlignment(this.alignment);
		}
	};
	/**
	*
	* @param {TextSymbolDef} symbol
	* @param {BufferReader} reader
	*/
	var readTextSymbol = (symbol, reader) => {
		symbol.type = 4;
		symbol.fontName = "";
		const fontLength = reader.readByte();
		for (let i = 0; i < fontLength; i++) {
			const c = reader.readByte();
			if (c) symbol.fontName += String.fromCharCode(c);
		}
		for (let i = 1; i < 32 - fontLength; i++) reader.readByte();
		symbol.fontColor = reader.readSmallInt();
		symbol.fontSize = reader.readSmallInt();
		symbol.weight = reader.readSmallInt();
		symbol.italic = !!reader.readByte();
		symbol.res1 = reader.readByte();
		symbol.charSpace = reader.readSmallInt();
		symbol.wordSpace = reader.readSmallInt();
		symbol.alignment = reader.readSmallInt();
		symbol.lineSpace = reader.readSmallInt();
		symbol.paraSpace = reader.readSmallInt();
		symbol.indentFirst = reader.readSmallInt();
		symbol.indentOther = reader.readSmallInt();
		symbol.nTabs = reader.readSmallInt();
		symbol.tabs = [];
		for (let i = 0; i < 32; i++) symbol.tabs.push(reader.readCardinal());
		symbol.lbOn = reader.readWordBool();
		symbol.lbColor = reader.readSmallInt();
		symbol.lbWidth = reader.readSmallInt();
		symbol.lbDist = reader.readSmallInt();
		symbol.res2 = reader.readSmallInt();
		symbol.frMode = reader.readByte();
		symbol.frStyle = reader.readByte();
		symbol.pointSymOn = !!reader.readByte();
		symbol.pointSymNumber = reader.readByte();
	};
	var verticalAlignment = (a) => a & 252;
	var horizontalAlignment = (a) => a & 3;
	module.exports = {
		10: TextSymbol10,
		11: TextSymbol11,
		12: TextSymbol11,
		2018: TextSymbol11,
		VerticalAlignBottom: 0,
		VerticalAlignMiddle: 4,
		VerticalAlignTop: 8,
		HorizontalAlignLeft: 0,
		HorizontalAlignCenter: 1,
		HorizontalAlignRight: 2,
		HorizontalAlignAllLine: 3
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/symbol-types.js
var require_symbol_types = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* @typedef {1} PointSymbolType
	*/
	/**
	* @typedef {2} LineSymbolType
	*/
	/**
	* @typedef {3} AreaSymbolType
	*/
	/**
	* @typedef {4} TextSymbolType
	*/
	/**
	* @typedef {7} RectangleSymbolType
	*/
	/**
	* @typedef {PointSymbolType|LineSymbolType|AreaSymbolType|TextSymbolType|RectangleSymbolType} SymbolType
	*/
	module.exports = {
		PointSymbolType: 1,
		LineSymbolType: 2,
		AreaSymbolType: 3,
		TextSymbolType: 4,
		LineTextSymbolType: 6,
		RectangleSymbolType: 7,
		DblFillColorOn: 1
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/symbol-index.js
var require_symbol_index = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var PointSymbol = require_point_symbol();
	var LineSymbol = require_line_symbol();
	var AreaSymbol = require_area_symbol();
	var TextSymbol = require_text_symbol();
	var { PointSymbolType, LineSymbolType, AreaSymbolType, TextSymbolType, RectangleSymbolType, LineTextSymbolType } = require_symbol_types();
	/** @typedef {import('./buffer-reader')} BufferReader */
	/** @typedef {import('./symbol').BaseSymbolDef} Symbol */
	module.exports = class SymbolIndexBlock {
		/**
		* @type {number}
		*/
		nextSymbolIndexBlock;
		/**
		* @type {number[]}
		*/
		symbolPosition;
		/**
		* @type {string[]}
		*/
		warnings;
		/**
		* @param {BufferReader} reader
		* @param {number} version
		* @param {import('./').ReadOcadOptions} options
		*/
		constructor(reader, version, options = {}) {
			this.version = version;
			this.nextSymbolIndexBlock = reader.readInteger();
			this.symbolPosition = new Array(256);
			this.warnings = [];
			this.options = options;
			for (let i = 0; i < this.symbolPosition.length; i++) this.symbolPosition[i] = reader.readInteger();
		}
		/**
		* @param {BufferReader} reader
		* @returns {Symbol[]}
		*/
		parseSymbols(reader) {
			return this.symbolPosition.filter((sp) => sp > 0).map((sp) => this.parseSymbol(reader, sp)).filter((s) => s);
		}
		/**
		* @param {BufferReader} reader
		* @param {number} offset
		* @returns {Symbol}
		*/
		parseSymbol(reader, offset) {
			if (!offset) return;
			reader.push(offset);
			const type = reader.buffer.readInt8(offset + 8);
			let symbol;
			try {
				let Cls;
				switch (type) {
					case PointSymbolType:
						Cls = PointSymbol[this.version];
						break;
					case LineSymbolType:
						Cls = LineSymbol[this.version];
						break;
					case AreaSymbolType:
						Cls = AreaSymbol[this.version];
						break;
					case TextSymbolType:
						Cls = TextSymbol[this.version];
						break;
					case LineTextSymbolType:
						this.warnings.push(`Ignoring line text symbol ${reader.buffer.readInt32LE(offset + 4)}.`);
						return null;
					case RectangleSymbolType:
						this.warnings.push(`Ignoring rectangle symbol ${reader.buffer.readInt32LE(offset + 4)}.`);
						return null;
					default: throw new Error(`Unknown symbol type ${type}`);
				}
				reader.push(offset);
				symbol = new Cls(reader);
				reader.pop();
				this.warnings = this.warnings.concat(symbol.warnings);
			} catch (e) {
				if (!this.options.failOnWarning) this.warnings.push(e);
				else throw e;
			}
			reader.pop();
			return symbol;
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/invalid-object-index-block-exception.js
var require_invalid_object_index_block_exception = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = class InvalidObjectIndexBlockException extends Error {};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/lrect.js
var require_lrect = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TdPoly = require_td_poly();
	module.exports = class LRect {
		/**
		* @type {TdPoly}
		*/
		min;
		/**
		* @type {TdPoly}
		*/
		max;
		/**
		* @param {import('./buffer-reader')} reader
		*/
		constructor(reader) {
			this.min = new TdPoly(reader.readInteger(), reader.readInteger());
			this.max = new TdPoly(reader.readInteger(), reader.readInteger());
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/tobject.js
var require_tobject = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TdPoly = require_td_poly();
	/** @typedef {import('./buffer-reader')} BufferReader */
	/** @typedef {import('./object-index').ObjectIndex} ObjectIndex */
	var BaseTObject = class {
		/** @type {ObjectIndex} */
		objIndex;
		/** @type {number} */
		objType;
		/** @type {number} */
		sym;
		/** @type {number} */
		otp;
		/** @type {boolean} */
		unicode;
		/** @type {number} */
		ang;
		/** @type {number} */
		col;
		/** @type {number} */
		lineWidth;
		/** @type {number} */
		diamFlags;
		/** @type {number} */
		serverObjectId;
		/** @type {number} */
		height;
		/** @type {number} */
		creationDate;
		/** @type {number} */
		multirepresentationId;
		/** @type {number} */
		modificationDate;
		/** @type {number} */
		nItem;
		/** @type {number} */
		nText;
		/** @type {number} */
		nObjectString;
		/** @type {number} */
		nDatabaseString;
		/** @type {number} */
		objectStringType;
		/** @type {number} */
		res1;
		/** @type {string} */
		text;
		/** @type {string|undefined} */
		objectString;
		/** @type {string|undefined} */
		databaseString;
		/** @type {TdPoly[]} */
		coordinates;
		/**
		* @param {BufferReader} reader
		* @param {ObjectIndex} objIndex
		*/
		constructor(reader, objIndex) {
			this.objIndex = objIndex;
			this.objType = objIndex.objType;
		}
	};
	/**
	* OCAD version 10 TObject structure.
	*/
	var TObject10 = class extends BaseTObject {
		/**
		* @param {BufferReader} reader
		* @param {ObjectIndex} objIndex
		*/
		constructor(reader, objIndex) {
			super(reader, objIndex);
			this.sym = reader.readInteger();
			this.otp = reader.readByte();
			this.unicode = !!reader.readByte();
			this.ang = reader.readSmallInt();
			this.nItem = reader.readCardinal();
			this.nText = reader.readWord();
			reader.readSmallInt();
			this.col = reader.readInteger();
			this.lineWidth = reader.readSmallInt();
			this.diamFlags = reader.readSmallInt();
			reader.readInteger();
			reader.readByte();
			reader.readByte();
			reader.readSmallInt();
			this.height = reader.readInteger();
			this.coordinates = new Array(this.nItem);
			reader.skip(4);
			for (let i = 0; i < this.nItem; i++) this.coordinates[i] = new TdPoly(reader.readInteger(), reader.readInteger());
			this.text = reader.readWideString(this.unicode, this.nText);
		}
	};
	/**
	* OCAD version 11 TObject structure.
	*/
	var TObject11 = class extends BaseTObject {
		/**
		* @param {BufferReader} reader
		* @param {ObjectIndex} objIndex
		*/
		constructor(reader, objIndex) {
			super(reader, objIndex);
			this.sym = reader.readInteger();
			this.otp = reader.readByte();
			this.unicode = !!reader.readByte();
			this.ang = reader.readSmallInt();
			this.nItem = reader.readCardinal();
			this.nText = reader.readWord();
			this.mark = reader.readByte();
			this.snappingMark = reader.readByte();
			this.col = reader.readInteger();
			this.lineWidth = reader.readSmallInt();
			this.diamFlags = reader.readSmallInt();
			this.serverObjectId = reader.readInteger();
			this.height = reader.readInteger();
			this._date = reader.readDouble();
			this.coordinates = new Array(this.nItem);
			for (let i = 0; i < this.nItem; i++) this.coordinates[i] = new TdPoly(reader.readInteger(), reader.readInteger());
			this.text = reader.readWideString(this.unicode, this.nText);
		}
	};
	/**
	* OCAD version 12 and 2018 TObject structure.
	*/
	var TObject12 = class extends BaseTObject {
		/**
		* @param {BufferReader} reader
		* @param {ObjectIndex} objIndex
		*/
		constructor(reader, objIndex) {
			super(reader, objIndex);
			this.sym = reader.readInteger();
			this.otp = reader.readByte();
			this.unicode = !!reader.readByte();
			this.ang = reader.readSmallInt();
			this.col = reader.readInteger();
			this.lineWidth = reader.readSmallInt();
			this.diamFlags = reader.readSmallInt();
			this.serverObjectId = reader.readInteger();
			this.height = reader.readInteger();
			this.creationDate = reader.readDouble();
			this.multirepresentationId = reader.readCardinal();
			this.modificationDate = reader.readDouble();
			this.nItem = reader.readCardinal();
			this.nText = reader.readWord();
			this.nObjectString = reader.readWord();
			this.nDatabaseString = reader.readWord();
			this.objectStringType = reader.readByte();
			this.res1 = reader.readByte();
			this.coordinates = new Array(this.nItem);
			for (let i = 0; i < this.nItem; i++) this.coordinates[i] = new TdPoly(reader.readInteger(), reader.readInteger());
			this.text = reader.readWideString(this.unicode, this.nText);
			this.objectString = reader.readWideString(this.unicode, this.nObjectString);
			this.databaseString = reader.readWideString(this.unicode, this.nDatabaseString);
		}
	};
	module.exports = {
		TObject: BaseTObject,
		10: TObject10,
		11: TObject11,
		12: TObject12,
		2018: TObject12
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/object-index.js
var require_object_index = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var InvalidObjectIndexBlockException = require_invalid_object_index_block_exception();
	var LRect = require_lrect();
	var TObject = require_tobject();
	/**
	* @typedef {import('./tobject')} TObject
	*/
	/**
	* @typedef {import('./buffer-reader')} BufferReader
	*/
	/**
	* @typedef {Object} ObjectIndex
	* @property {LRect} rc
	* @property {number} pos
	* @property {number} len
	* @property {number} sym
	* @property {number} objType
	* @property {number} encryptedMode
	* @property {number} status
	* @property {number} viewType
	* @property {number} color
	* @property {number} group
	* @property {number} impLayer
	* @property {number} dbDatasetHash
	* @property {number} dbKeyHash
	* @property {number} _index
	*/
	/**
	* @typedef {Object} ObjectIndexBlock
	*/
	module.exports = class ObjectIndexBlock {
		/** @type {number} */
		version;
		/** @type {number} */
		nextObjectIndexBlock;
		/** @type {ObjectIndex[]} */
		table;
		/**
		* @param {BufferReader} reader
		* @param {number} startIndex
		* @param {number} version
		*/
		constructor(reader, startIndex, version) {
			this.version = version;
			this.nextObjectIndexBlock = reader.readInteger();
			if (this.nextObjectIndexBlock > reader.buffer.length - 10244) throw new InvalidObjectIndexBlockException(`Invalid object index block pointer ${this.nextObjectIndexBlock} > ${reader.buffer.length - 10244}.`);
			this.table = new Array(256);
			for (let i = 0; i < 256; i++) {
				const rc = new LRect(reader);
				this.table[i] = {
					rc,
					pos: reader.readInteger(),
					len: reader.readInteger(),
					sym: reader.readInteger(),
					objType: reader.readByte(),
					encryptedMode: reader.readByte(),
					status: reader.readByte(),
					viewType: reader.readByte(),
					color: reader.readSmallInt(),
					group: reader.readSmallInt(),
					impLayer: reader.readSmallInt(),
					dbDatasetHash: reader.readByte(),
					dbKeyHash: reader.readByte(),
					_index: startIndex + i
				};
			}
		}
		/**
		* Reads the objects contained in this object index.
		* @param {BufferReader} reader
		* @returns {TObject[]}
		*/
		readObjects(reader) {
			return this.table.filter((o) => o.status > 0 && o.status < 3).map((o) => this.readObject(reader, o)).filter((o) => o);
		}
		readObject(reader, objIndex) {
			if (!objIndex.pos) return;
			reader.push(objIndex.pos);
			const tObject = new TObject[this.version](reader, objIndex);
			reader.pop();
			return tObject;
		}
	};
}));
//#endregion
//#region node_modules/safe-buffer/index.js
var require_safe_buffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var buffer = require_buffer();
	var Buffer = buffer.Buffer;
	function copyProps(src, dst) {
		for (var key in src) dst[key] = src[key];
	}
	if (Buffer.from && Buffer.alloc && Buffer.allocUnsafe && Buffer.allocUnsafeSlow) module.exports = buffer;
	else {
		copyProps(buffer, exports);
		exports.Buffer = SafeBuffer;
	}
	function SafeBuffer(arg, encodingOrOffset, length) {
		return Buffer(arg, encodingOrOffset, length);
	}
	copyProps(Buffer, SafeBuffer);
	SafeBuffer.from = function(arg, encodingOrOffset, length) {
		if (typeof arg === "number") throw new TypeError("Argument must not be a number");
		return Buffer(arg, encodingOrOffset, length);
	};
	SafeBuffer.alloc = function(size, fill, encoding) {
		if (typeof size !== "number") throw new TypeError("Argument must be a number");
		var buf = Buffer(size);
		if (fill !== void 0) if (typeof encoding === "string") buf.fill(fill, encoding);
		else buf.fill(fill);
		else buf.fill(0);
		return buf;
	};
	SafeBuffer.allocUnsafe = function(size) {
		if (typeof size !== "number") throw new TypeError("Argument must be a number");
		return Buffer(size);
	};
	SafeBuffer.allocUnsafeSlow = function(size) {
		if (typeof size !== "number") throw new TypeError("Argument must be a number");
		return buffer.SlowBuffer(size);
	};
}));
//#endregion
//#region node_modules/string_decoder/lib/string_decoder.js
var require_string_decoder = /* @__PURE__ */ __commonJSMin(((exports) => {
	var Buffer = require_safe_buffer().Buffer;
	var isEncoding = Buffer.isEncoding || function(encoding) {
		encoding = "" + encoding;
		switch (encoding && encoding.toLowerCase()) {
			case "hex":
			case "utf8":
			case "utf-8":
			case "ascii":
			case "binary":
			case "base64":
			case "ucs2":
			case "ucs-2":
			case "utf16le":
			case "utf-16le":
			case "raw": return true;
			default: return false;
		}
	};
	function _normalizeEncoding(enc) {
		if (!enc) return "utf8";
		var retried;
		while (true) switch (enc) {
			case "utf8":
			case "utf-8": return "utf8";
			case "ucs2":
			case "ucs-2":
			case "utf16le":
			case "utf-16le": return "utf16le";
			case "latin1":
			case "binary": return "latin1";
			case "base64":
			case "ascii":
			case "hex": return enc;
			default:
				if (retried) return;
				enc = ("" + enc).toLowerCase();
				retried = true;
		}
	}
	function normalizeEncoding(enc) {
		var nenc = _normalizeEncoding(enc);
		if (typeof nenc !== "string" && (Buffer.isEncoding === isEncoding || !isEncoding(enc))) throw new Error("Unknown encoding: " + enc);
		return nenc || enc;
	}
	exports.StringDecoder = StringDecoder;
	function StringDecoder(encoding) {
		this.encoding = normalizeEncoding(encoding);
		var nb;
		switch (this.encoding) {
			case "utf16le":
				this.text = utf16Text;
				this.end = utf16End;
				nb = 4;
				break;
			case "utf8":
				this.fillLast = utf8FillLast;
				nb = 4;
				break;
			case "base64":
				this.text = base64Text;
				this.end = base64End;
				nb = 3;
				break;
			default:
				this.write = simpleWrite;
				this.end = simpleEnd;
				return;
		}
		this.lastNeed = 0;
		this.lastTotal = 0;
		this.lastChar = Buffer.allocUnsafe(nb);
	}
	StringDecoder.prototype.write = function(buf) {
		if (buf.length === 0) return "";
		var r;
		var i;
		if (this.lastNeed) {
			r = this.fillLast(buf);
			if (r === void 0) return "";
			i = this.lastNeed;
			this.lastNeed = 0;
		} else i = 0;
		if (i < buf.length) return r ? r + this.text(buf, i) : this.text(buf, i);
		return r || "";
	};
	StringDecoder.prototype.end = utf8End;
	StringDecoder.prototype.text = utf8Text;
	StringDecoder.prototype.fillLast = function(buf) {
		if (this.lastNeed <= buf.length) {
			buf.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, this.lastNeed);
			return this.lastChar.toString(this.encoding, 0, this.lastTotal);
		}
		buf.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, buf.length);
		this.lastNeed -= buf.length;
	};
	function utf8CheckByte(byte) {
		if (byte <= 127) return 0;
		else if (byte >> 5 === 6) return 2;
		else if (byte >> 4 === 14) return 3;
		else if (byte >> 3 === 30) return 4;
		return byte >> 6 === 2 ? -1 : -2;
	}
	function utf8CheckIncomplete(self, buf, i) {
		var j = buf.length - 1;
		if (j < i) return 0;
		var nb = utf8CheckByte(buf[j]);
		if (nb >= 0) {
			if (nb > 0) self.lastNeed = nb - 1;
			return nb;
		}
		if (--j < i || nb === -2) return 0;
		nb = utf8CheckByte(buf[j]);
		if (nb >= 0) {
			if (nb > 0) self.lastNeed = nb - 2;
			return nb;
		}
		if (--j < i || nb === -2) return 0;
		nb = utf8CheckByte(buf[j]);
		if (nb >= 0) {
			if (nb > 0) if (nb === 2) nb = 0;
			else self.lastNeed = nb - 3;
			return nb;
		}
		return 0;
	}
	function utf8CheckExtraBytes(self, buf, p) {
		if ((buf[0] & 192) !== 128) {
			self.lastNeed = 0;
			return "�";
		}
		if (self.lastNeed > 1 && buf.length > 1) {
			if ((buf[1] & 192) !== 128) {
				self.lastNeed = 1;
				return "�";
			}
			if (self.lastNeed > 2 && buf.length > 2) {
				if ((buf[2] & 192) !== 128) {
					self.lastNeed = 2;
					return "�";
				}
			}
		}
	}
	function utf8FillLast(buf) {
		var p = this.lastTotal - this.lastNeed;
		var r = utf8CheckExtraBytes(this, buf, p);
		if (r !== void 0) return r;
		if (this.lastNeed <= buf.length) {
			buf.copy(this.lastChar, p, 0, this.lastNeed);
			return this.lastChar.toString(this.encoding, 0, this.lastTotal);
		}
		buf.copy(this.lastChar, p, 0, buf.length);
		this.lastNeed -= buf.length;
	}
	function utf8Text(buf, i) {
		var total = utf8CheckIncomplete(this, buf, i);
		if (!this.lastNeed) return buf.toString("utf8", i);
		this.lastTotal = total;
		var end = buf.length - (total - this.lastNeed);
		buf.copy(this.lastChar, 0, end);
		return buf.toString("utf8", i, end);
	}
	function utf8End(buf) {
		var r = buf && buf.length ? this.write(buf) : "";
		if (this.lastNeed) return r + "�";
		return r;
	}
	function utf16Text(buf, i) {
		if ((buf.length - i) % 2 === 0) {
			var r = buf.toString("utf16le", i);
			if (r) {
				var c = r.charCodeAt(r.length - 1);
				if (c >= 55296 && c <= 56319) {
					this.lastNeed = 2;
					this.lastTotal = 4;
					this.lastChar[0] = buf[buf.length - 2];
					this.lastChar[1] = buf[buf.length - 1];
					return r.slice(0, -1);
				}
			}
			return r;
		}
		this.lastNeed = 1;
		this.lastTotal = 2;
		this.lastChar[0] = buf[buf.length - 1];
		return buf.toString("utf16le", i, buf.length - 1);
	}
	function utf16End(buf) {
		var r = buf && buf.length ? this.write(buf) : "";
		if (this.lastNeed) {
			var end = this.lastTotal - this.lastNeed;
			return r + this.lastChar.toString("utf16le", 0, end);
		}
		return r;
	}
	function base64Text(buf, i) {
		var n = (buf.length - i) % 3;
		if (n === 0) return buf.toString("base64", i);
		this.lastNeed = 3 - n;
		this.lastTotal = 3;
		if (n === 1) this.lastChar[0] = buf[buf.length - 1];
		else {
			this.lastChar[0] = buf[buf.length - 2];
			this.lastChar[1] = buf[buf.length - 1];
		}
		return buf.toString("base64", i, buf.length - n);
	}
	function base64End(buf) {
		var r = buf && buf.length ? this.write(buf) : "";
		if (this.lastNeed) return r + this.lastChar.toString("base64", 0, 3 - this.lastNeed);
		return r;
	}
	function simpleWrite(buf) {
		return buf.toString(this.encoding);
	}
	function simpleEnd(buf) {
		return buf && buf.length ? this.write(buf) : "";
	}
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/parameter-string.js
var require_parameter_string = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { StringDecoder } = require_string_decoder();
	var decoder = new StringDecoder("utf8");
	/**
	* @typedef {string|string[]} StringIndexValue
	*/
	/**
	* @typedef {{ _first: string, _pairs: { code: string, value: StringIndexValue }[]}} SourceValues
	*/
	/** @typedef {import("./buffer-reader")} BufferReader */
	/** @typedef {import("./string-index").TStringIndex} TStringIndex */
	/**
	* @typedef {{[key: string]: string|string[]} & SourceValues } ParameterStringValues
	*/
	/**
	* Represents an OCAD parameter string. The string has the following format:
	* ```
	* <first value>\t<code1><value1>\t<code2><value2>\t...
	* ```
	*
	* The values can be accessed through the `values` property. The first value is
	* stored in the `_first` property. The code-value pairs are stored in the
	* `_pairs` property.
	*/
	module.exports = class ParameterString {
		/**
		* @type {number}
		*/
		recType;
		/**
		* @type {ParameterStringValues}
		*/
		values;
		/**
		* @param {BufferReader} reader
		* @param {TStringIndex} indexRecord
		*/
		constructor(reader, indexRecord) {
			this.recType = indexRecord.recType;
			const offset = reader.offset;
			let strLen = 0;
			while (reader.readByte()) strLen++;
			const vals = decoder.end(reader.buffer.subarray(offset, offset + strLen)).split("	");
			this.values = {
				_first: vals[0],
				_pairs: []
			};
			for (let i = 1; i < vals.length; i++) {
				const code = vals[i][0];
				const value = vals[i].substring(1);
				let codeValues = this.values[code];
				if (!codeValues) this.values[code] = value;
				else {
					if (!Array.isArray(codeValues)) codeValues = this.values[code] = [codeValues];
					codeValues.push(value);
				}
				this.values._pairs.push({
					code,
					value
				});
			}
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/string-index.js
var require_string_index = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var ParameterString = require_parameter_string();
	/** @typedef {import("./buffer-reader")} BufferReader */
	/**
	* @typedef {Object} TStringIndex
	* @property {number} pos
	* @property {number} len
	* @property {number} recType
	* @property {number} objIndex
	*/
	module.exports = class StringIndexBlock {
		/**
		* @type {number}
		*/
		nextStringIndexBlock;
		/**
		* @type {TStringIndex[]}
		*/
		table;
		/**
		* @type {Object.<number, { pos: number, len: number, recType: number, objIndex: number }>}
		*/
		constructor(reader) {
			this.nextStringIndexBlock = reader.readInteger();
			this.table = new Array(256);
			for (let i = 0; i < 256; i++) this.table[i] = {
				pos: reader.readInteger(),
				len: reader.readInteger(),
				recType: reader.readInteger(),
				objIndex: reader.readInteger()
			};
		}
		/**
		* @param {BufferReader} reader
		* @returns {Object.<number, ParameterString[]>}
		*/
		getStrings(reader) {
			return this.table.filter((si) => si.recType > 0).map((si) => {
				reader.push(si.pos);
				const s = new ParameterString(reader, si);
				reader.pop();
				return s;
			}).reduce((pss, ps) => {
				let typeStrings = pss[ps.recType];
				if (!typeStrings) pss[ps.recType] = typeStrings = [];
				typeStrings.push(ps);
				return pss;
			}, {});
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/buffer-reader.js
var require_buffer_reader = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Encapsulates reading binary values for a buffer using the type names
	* used in the OCAD file format specification.
	*
	* The reader also supports pushing and popping the current offset like a stack,
	* which is useful when reading nested structures.
	*
	* @property {number} offset
	* @property {number[]} stack
	*/
	module.exports = class BufferReader {
		/**
		* @type {import('buffer').Buffer}
		*/
		buffer;
		/**
		* @type {number}
		*/
		offset;
		/**
		* @type {number[]}
		*/
		stack;
		/**
		* Constructs a new reader from the buffer, starting a the given offset.
		* @param {import('buffer').Buffer} buffer
		* @param {number} offset
		*/
		constructor(buffer, offset = 0) {
			this.buffer = buffer;
			this.offset = offset;
			this.stack = [];
		}
		readInteger() {
			const val = this.buffer.readInt32LE(this.offset);
			this.offset += 4;
			return val;
		}
		readCardinal() {
			const val = this.buffer.readUInt32LE(this.offset);
			this.offset += 4;
			return val;
		}
		readSmallInt() {
			const val = this.buffer.readInt16LE(this.offset);
			this.offset += 2;
			return val;
		}
		readByte() {
			const val = this.buffer.readInt8(this.offset);
			this.offset++;
			return val;
		}
		readWord() {
			const val = this.buffer.readUInt16LE(this.offset);
			this.offset += 2;
			return val;
		}
		readWordBool() {
			return !!this.readWord();
		}
		readDouble() {
			const val = this.buffer.readDoubleLE(this.offset);
			this.offset += 8;
			return val;
		}
		/**
		* Reads a OCAD "wide string" from the buffer. For some OCAD versions,
		* a wide string is a string of 16-bit characters, for others it is a
		* string of 32-bit characters; setting the unicode parameter to true
		* will read 16-bit characters.
		*
		* If the length parameter is not given, the length of the string is read
		* as the first byte from the buffer.
		*
		* @param {boolean} unicode Whether to read 16-bit or 32-bit characters
		* @param {number=} len The length of the string
		* @returns {string}
		*/
		readWideString(unicode, len) {
			if (len == null) len = this.readByte();
			const textChars = [];
			for (let i = 0; i < len * (unicode ? 2 : 4); i++) {
				const c = unicode ? this.readByte() : this.readWord();
				if (!c) break;
				textChars.push(String.fromCharCode(c));
			}
			return textChars.filter((c) => c !== "\r").join("").trim();
		}
		/**
		* Returns the number of bytes read since the last push() call.
		* @returns {number}
		*/
		getSize() {
			return this.offset - this.stack[this.stack.length - 1];
		}
		/**
		* Skips the given number of bytes.
		* @param {number} bytes
		*/
		skip(bytes) {
			this.offset += bytes;
		}
		/**
		* Pushes the current offset onto the stack and sets the offset to the given
		* value.
		* @param {number} offset
		*/
		push(offset) {
			this.stack.push(this.offset);
			this.offset = offset;
		}
		/**
		* Pops the current offset from the stack.
		*/
		pop() {
			const nextOffset = this.stack.pop();
			if (nextOffset == null) throw new Error("Stack underflow");
			this.offset = nextOffset;
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/cmyk-to-rgb.js
var require_cmyk_to_rgb = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	*
	* @param {[number,number,number,number]} src CMYK values (0-100)
	* @returns {Uint8ClampedArray} RGB values (0-255)
	*/
	module.exports = function convertToRgb(src) {
		const toFraction = 1 / 100;
		const rgb = new Uint8ClampedArray(3);
		const c = src[0] * toFraction;
		const m = src[1] * toFraction;
		const y = src[2] * toFraction;
		const k = src[3] * toFraction;
		rgb[0] = 255 + c * (-4.387332384609988 * c + 54.48615194189176 * m + 18.82290502165302 * y + 212.25662451639585 * k + -285.2331026137004) + m * (1.7149763477362134 * m - 5.6096736904047315 * y + -17.873870861415444 * k - 5.497006427196366) + y * (-2.5217340131683033 * y - 21.248923337353073 * k + 17.5119270841813) + k * (-21.86122147463605 * k - 189.48180835922747);
		rgb[1] = 255 + c * (8.841041422036149 * c + 60.118027045597366 * m + 6.871425592049007 * y + 31.159100130055922 * k + -79.2970844816548) + m * (-15.310361306967817 * m + 17.575251261109482 * y + 131.35250912493976 * k - 190.9453302588951) + y * (4.444339102852739 * y + 9.8632861493405 * k - 24.86741582555878) + k * (-20.737325471181034 * k - 187.80453709719578);
		rgb[2] = 255 + c * (.8842522430003296 * c + 8.078677503112928 * m + 30.89978309703729 * y - .23883238689178934 * k + -14.183576799673286) + m * (10.49593273432072 * m + 63.02378494754052 * y + 50.606957656360734 * k - 112.23884253719248) + y * (.03296041114873217 * y + 115.60384449646641 * k + -193.58209356861505) + k * (-22.33816807309886 * k - 180.12613974708367);
		return rgb;
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/crs-grids.js
var require_crs_grids = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/** @typedef {[number, number, string, string]} GridDef */
	/**
	* @type {GridDef[]}
	*/
	module.exports = [
		[
			-2060,
			32760,
			"EPSG",
			"WGS 84 / UTM zone 60S"
		],
		[
			-2059,
			32759,
			"EPSG",
			"WGS 84 / UTM zone 59S"
		],
		[
			-2058,
			32758,
			"EPSG",
			"WGS 84 / UTM zone 58S"
		],
		[
			-2057,
			32757,
			"EPSG",
			"WGS 84 / UTM zone 57S"
		],
		[
			-2056,
			32756,
			"EPSG",
			"WGS 84 / UTM zone 56S"
		],
		[
			-2055,
			32755,
			"EPSG",
			"WGS 84 / UTM zone 55S"
		],
		[
			-2054,
			32754,
			"EPSG",
			"WGS 84 / UTM zone 54S"
		],
		[
			-2053,
			32753,
			"EPSG",
			"WGS 84 / UTM zone 53S"
		],
		[
			-2052,
			32752,
			"EPSG",
			"WGS 84 / UTM zone 52S"
		],
		[
			-2051,
			32751,
			"EPSG",
			"WGS 84 / UTM zone 51S"
		],
		[
			-2050,
			32750,
			"EPSG",
			"WGS 84 / UTM zone 50S"
		],
		[
			-2049,
			32749,
			"EPSG",
			"WGS 84 / UTM zone 49S"
		],
		[
			-2048,
			32748,
			"EPSG",
			"WGS 84 / UTM zone 48S"
		],
		[
			-2047,
			32747,
			"EPSG",
			"WGS 84 / UTM zone 47S"
		],
		[
			-2046,
			32746,
			"EPSG",
			"WGS 84 / UTM zone 46S"
		],
		[
			-2045,
			32745,
			"EPSG",
			"WGS 84 / UTM zone 45S"
		],
		[
			-2044,
			32744,
			"EPSG",
			"WGS 84 / UTM zone 44S"
		],
		[
			-2043,
			32743,
			"EPSG",
			"WGS 84 / UTM zone 43S"
		],
		[
			-2042,
			32742,
			"EPSG",
			"WGS 84 / UTM zone 42S"
		],
		[
			-2041,
			32741,
			"EPSG",
			"WGS 84 / UTM zone 41S"
		],
		[
			-2040,
			32740,
			"EPSG",
			"WGS 84 / UTM zone 40S"
		],
		[
			-2039,
			32739,
			"EPSG",
			"WGS 84 / UTM zone 39S"
		],
		[
			-2038,
			32738,
			"EPSG",
			"WGS 84 / UTM zone 38S"
		],
		[
			-2037,
			32737,
			"EPSG",
			"WGS 84 / UTM zone 37S"
		],
		[
			-2036,
			32736,
			"EPSG",
			"WGS 84 / UTM zone 36S"
		],
		[
			-2035,
			32735,
			"EPSG",
			"WGS 84 / UTM zone 35S"
		],
		[
			-2034,
			32734,
			"EPSG",
			"WGS 84 / UTM zone 34S"
		],
		[
			-2033,
			32733,
			"EPSG",
			"WGS 84 / UTM zone 33S"
		],
		[
			-2032,
			32732,
			"EPSG",
			"WGS 84 / UTM zone 32S"
		],
		[
			-2031,
			32731,
			"EPSG",
			"WGS 84 / UTM zone 31S"
		],
		[
			-2030,
			32730,
			"EPSG",
			"WGS 84 / UTM zone 30S"
		],
		[
			-2029,
			32729,
			"EPSG",
			"WGS 84 / UTM zone 29S"
		],
		[
			-2028,
			32728,
			"EPSG",
			"WGS 84 / UTM zone 28S"
		],
		[
			-2027,
			32727,
			"EPSG",
			"WGS 84 / UTM zone 27S"
		],
		[
			-2026,
			32726,
			"EPSG",
			"WGS 84 / UTM zone 26S"
		],
		[
			-2025,
			32725,
			"EPSG",
			"WGS 84 / UTM zone 25S"
		],
		[
			-2024,
			32724,
			"EPSG",
			"WGS 84 / UTM zone 24S"
		],
		[
			-2023,
			32723,
			"EPSG",
			"WGS 84 / UTM zone 23S"
		],
		[
			-2022,
			32722,
			"EPSG",
			"WGS 84 / UTM zone 22S"
		],
		[
			-2021,
			32721,
			"EPSG",
			"WGS 84 / UTM zone 21S"
		],
		[
			-2020,
			32720,
			"EPSG",
			"WGS 84 / UTM zone 20S"
		],
		[
			-2019,
			32719,
			"EPSG",
			"WGS 84 / UTM zone 19S"
		],
		[
			-2018,
			32718,
			"EPSG",
			"WGS 84 / UTM zone 18S"
		],
		[
			-2017,
			32717,
			"EPSG",
			"WGS 84 / UTM zone 17S"
		],
		[
			-2016,
			32716,
			"EPSG",
			"WGS 84 / UTM zone 16S"
		],
		[
			-2015,
			32715,
			"EPSG",
			"WGS 84 / UTM zone 15S"
		],
		[
			-2014,
			32714,
			"EPSG",
			"WGS 84 / UTM zone 14S"
		],
		[
			-2013,
			32713,
			"EPSG",
			"WGS 84 / UTM zone 13S"
		],
		[
			-2012,
			32712,
			"EPSG",
			"WGS 84 / UTM zone 12S"
		],
		[
			-2011,
			32711,
			"EPSG",
			"WGS 84 / UTM zone 11S"
		],
		[
			-2010,
			32710,
			"EPSG",
			"WGS 84 / UTM zone 10S"
		],
		[
			-2009,
			32709,
			"EPSG",
			"WGS 84 / UTM zone 9S"
		],
		[
			-2008,
			32708,
			"EPSG",
			"WGS 84 / UTM zone 8S"
		],
		[
			-2007,
			32707,
			"EPSG",
			"WGS 84 / UTM zone 7S"
		],
		[
			-2006,
			32706,
			"EPSG",
			"WGS 84 / UTM zone 6S"
		],
		[
			-2005,
			32705,
			"EPSG",
			"WGS 84 / UTM zone 5S"
		],
		[
			-2004,
			32704,
			"EPSG",
			"WGS 84 / UTM zone 4S"
		],
		[
			-2003,
			32703,
			"EPSG",
			"WGS 84 / UTM zone 3S"
		],
		[
			-2002,
			32702,
			"EPSG",
			"WGS 84 / UTM zone 2S"
		],
		[
			-2001,
			32701,
			"EPSG",
			"WGS 84 / UTM zone 1S"
		],
		[
			2001,
			32601,
			"EPSG",
			"WGS 84 / UTM zone 1N"
		],
		[
			2002,
			32602,
			"EPSG",
			"WGS 84 / UTM zone 2N"
		],
		[
			2003,
			32603,
			"EPSG",
			"WGS 84 / UTM zone 3N"
		],
		[
			2004,
			32604,
			"EPSG",
			"WGS 84 / UTM zone 4N"
		],
		[
			2005,
			32605,
			"EPSG",
			"WGS 84 / UTM zone 5N"
		],
		[
			2006,
			32606,
			"EPSG",
			"WGS 84 / UTM zone 6N"
		],
		[
			2007,
			32607,
			"EPSG",
			"WGS 84 / UTM zone 7N"
		],
		[
			2008,
			32608,
			"EPSG",
			"WGS 84 / UTM zone 8N"
		],
		[
			2009,
			32609,
			"EPSG",
			"WGS 84 / UTM zone 9N"
		],
		[
			2010,
			32610,
			"EPSG",
			"WGS 84 / UTM zone 10N"
		],
		[
			2011,
			32611,
			"EPSG",
			"WGS 84 / UTM zone 11N"
		],
		[
			2012,
			32612,
			"EPSG",
			"WGS 84 / UTM zone 12N"
		],
		[
			2013,
			32613,
			"EPSG",
			"WGS 84 / UTM zone 13N"
		],
		[
			2014,
			32614,
			"EPSG",
			"WGS 84 / UTM zone 14N"
		],
		[
			2015,
			32615,
			"EPSG",
			"WGS 84 / UTM zone 15N"
		],
		[
			2016,
			32616,
			"EPSG",
			"WGS 84 / UTM zone 16N"
		],
		[
			2017,
			32617,
			"EPSG",
			"WGS 84 / UTM zone 17N"
		],
		[
			2018,
			32618,
			"EPSG",
			"WGS 84 / UTM zone 18N"
		],
		[
			2019,
			32619,
			"EPSG",
			"WGS 84 / UTM zone 19N"
		],
		[
			2020,
			32620,
			"EPSG",
			"WGS 84 / UTM zone 20N"
		],
		[
			2021,
			32621,
			"EPSG",
			"WGS 84 / UTM zone 21N"
		],
		[
			2022,
			32622,
			"EPSG",
			"WGS 84 / UTM zone 22N"
		],
		[
			2023,
			32623,
			"EPSG",
			"WGS 84 / UTM zone 23N"
		],
		[
			2024,
			32624,
			"EPSG",
			"WGS 84 / UTM zone 24N"
		],
		[
			2025,
			32625,
			"EPSG",
			"WGS 84 / UTM zone 25N"
		],
		[
			2026,
			32626,
			"EPSG",
			"WGS 84 / UTM zone 26N"
		],
		[
			2027,
			32627,
			"EPSG",
			"WGS 84 / UTM zone 27N"
		],
		[
			2028,
			32628,
			"EPSG",
			"WGS 84 / UTM zone 28N"
		],
		[
			2029,
			32629,
			"EPSG",
			"WGS 84 / UTM zone 29N"
		],
		[
			2030,
			32630,
			"EPSG",
			"WGS 84 / UTM zone 30N"
		],
		[
			2031,
			32631,
			"EPSG",
			"WGS 84 / UTM zone 31N"
		],
		[
			2032,
			32632,
			"EPSG",
			"WGS 84 / UTM zone 32N"
		],
		[
			2033,
			32633,
			"EPSG",
			"WGS 84 / UTM zone 33N"
		],
		[
			2034,
			32634,
			"EPSG",
			"WGS 84 / UTM zone 34N"
		],
		[
			2035,
			32635,
			"EPSG",
			"WGS 84 / UTM zone 35N"
		],
		[
			2036,
			32636,
			"EPSG",
			"WGS 84 / UTM zone 36N"
		],
		[
			2037,
			32637,
			"EPSG",
			"WGS 84 / UTM zone 37N"
		],
		[
			2038,
			32638,
			"EPSG",
			"WGS 84 / UTM zone 38N"
		],
		[
			2039,
			32639,
			"EPSG",
			"WGS 84 / UTM zone 39N"
		],
		[
			2040,
			32640,
			"EPSG",
			"WGS 84 / UTM zone 40N"
		],
		[
			2041,
			32641,
			"EPSG",
			"WGS 84 / UTM zone 41N"
		],
		[
			2042,
			32642,
			"EPSG",
			"WGS 84 / UTM zone 42N"
		],
		[
			2043,
			32643,
			"EPSG",
			"WGS 84 / UTM zone 43N"
		],
		[
			2044,
			32644,
			"EPSG",
			"WGS 84 / UTM zone 44N"
		],
		[
			2045,
			32645,
			"EPSG",
			"WGS 84 / UTM zone 45N"
		],
		[
			2046,
			32646,
			"EPSG",
			"WGS 84 / UTM zone 46N"
		],
		[
			2047,
			32647,
			"EPSG",
			"WGS 84 / UTM zone 47N"
		],
		[
			2048,
			32648,
			"EPSG",
			"WGS 84 / UTM zone 48N"
		],
		[
			2049,
			32649,
			"EPSG",
			"WGS 84 / UTM zone 49N"
		],
		[
			2050,
			32650,
			"EPSG",
			"WGS 84 / UTM zone 50N"
		],
		[
			2051,
			32651,
			"EPSG",
			"WGS 84 / UTM zone 51N"
		],
		[
			2052,
			32652,
			"EPSG",
			"WGS 84 / UTM zone 52N"
		],
		[
			2053,
			32653,
			"EPSG",
			"WGS 84 / UTM zone 53N"
		],
		[
			2054,
			32654,
			"EPSG",
			"WGS 84 / UTM zone 54N"
		],
		[
			2055,
			32655,
			"EPSG",
			"WGS 84 / UTM zone 55N"
		],
		[
			2056,
			32656,
			"EPSG",
			"WGS 84 / UTM zone 56N"
		],
		[
			2057,
			32657,
			"EPSG",
			"WGS 84 / UTM zone 57N"
		],
		[
			2058,
			32658,
			"EPSG",
			"WGS 84 / UTM zone 58N"
		],
		[
			2059,
			32659,
			"EPSG",
			"WGS 84 / UTM zone 59N"
		],
		[
			2060,
			32660,
			"EPSG",
			"WGS 84 / UTM zone 60N"
		],
		[
			3028,
			31257,
			"EPSG",
			"MGI / Austria GK M28"
		],
		[
			3031,
			31258,
			"EPSG",
			"MGI / Austria GK M31"
		],
		[
			3034,
			31259,
			"EPSG",
			"MGI / Austria GK M34"
		],
		[
			3035,
			31254,
			"EPSG",
			"MGI / Austria GK West"
		],
		[
			3036,
			31255,
			"EPSG",
			"MGI / Austria GK Central"
		],
		[
			3038,
			31256,
			"EPSG",
			"MGI / Austria GK East"
		],
		[
			4001,
			31300,
			"EPSG",
			"Belge 1972 / Belge Lambert 72"
		],
		[
			4002,
			3447,
			"EPSG",
			"ETRS89 / Belgian Lambert 2005"
		],
		[
			4003,
			3812,
			"EPSG",
			"ETRS89 / Belgian Lambert 2008"
		],
		[
			5e3,
			27700,
			"EPSG",
			"OSGB 1936 / British National Grid"
		],
		[
			6001,
			2391,
			"EPSG",
			"KKJ / Finland zone 1"
		],
		[
			6002,
			2392,
			"EPSG",
			"KKJ / Finland zone 2"
		],
		[
			6003,
			2393,
			"EPSG",
			"KKJ / Finland Uniform Coordinate System"
		],
		[
			6004,
			2394,
			"EPSG",
			"KKJ / Finland zone 4"
		],
		[
			6005,
			3067,
			"EPSG",
			"ETRS89 / TM35FIN(E N)"
		],
		[
			6006,
			3873,
			"EPSG",
			"ETRS89 / GK19FIN"
		],
		[
			6007,
			3874,
			"EPSG",
			"ETRS89 / GK20FIN"
		],
		[
			6008,
			3875,
			"EPSG",
			"ETRS89 / GK21FIN"
		],
		[
			6009,
			3876,
			"EPSG",
			"ETRS89 / GK22FIN"
		],
		[
			6010,
			3877,
			"EPSG",
			"ETRS89 / GK23FIN"
		],
		[
			6011,
			3878,
			"EPSG",
			"ETRS89 / GK24FIN"
		],
		[
			6012,
			3879,
			"EPSG",
			"ETRS89 / GK25FIN"
		],
		[
			6013,
			3880,
			"EPSG",
			"ETRS89 / GK26FIN"
		],
		[
			6014,
			3881,
			"EPSG",
			"ETRS89 / GK27FIN"
		],
		[
			6015,
			3882,
			"EPSG",
			"ETRS89 / GK28FIN"
		],
		[
			6016,
			3883,
			"EPSG",
			"ETRS89 / GK29FIN"
		],
		[
			6017,
			3884,
			"EPSG",
			"ETRS89 / GK30FIN"
		],
		[
			6018,
			3885,
			"EPSG",
			"ETRS89 / GK31FIN"
		],
		[
			6019,
			3126,
			"EPSG",
			"ETRS89 / ETRS-GK19FIN"
		],
		[
			6020,
			3127,
			"EPSG",
			"ETRS89 / ETRS-GK20FIN"
		],
		[
			6021,
			3128,
			"EPSG",
			"ETRS89 / ETRS-GK21FIN"
		],
		[
			6022,
			3129,
			"EPSG",
			"ETRS89 / ETRS-GK22FIN"
		],
		[
			6023,
			3130,
			"EPSG",
			"ETRS89 / ETRS-GK23FIN"
		],
		[
			6024,
			3131,
			"EPSG",
			"ETRS89 / ETRS-GK24FIN"
		],
		[
			6025,
			3132,
			"EPSG",
			"ETRS89 / ETRS-GK25FIN"
		],
		[
			6026,
			3133,
			"EPSG",
			"ETRS89 / ETRS-GK26FIN"
		],
		[
			6027,
			3134,
			"EPSG",
			"ETRS89 / ETRS-GK27FIN"
		],
		[
			6028,
			3135,
			"EPSG",
			"ETRS89 / ETRS-GK28FIN"
		],
		[
			6029,
			3136,
			"EPSG",
			"ETRS89 / ETRS-GK29FIN"
		],
		[
			6030,
			3137,
			"EPSG",
			"ETRS89 / ETRS-GK30FIN"
		],
		[
			6031,
			3138,
			"EPSG",
			"ETRS89 / ETRS-GK31FIN"
		],
		[
			6032,
			3387,
			"EPSG",
			"KKJ / Finland zone 5"
		],
		[
			7001,
			27591,
			"EPSG",
			"NTF (Paris) / Nord France;#Deprecated: Changed projCRS name. Use EPSG:27561 instead"
		],
		[
			7002,
			27581,
			"EPSG",
			"NTF (Paris) / France I;#Deprecated: Changed projCRS name. Use EPSG:27571 instead"
		],
		[
			7003,
			27592,
			"EPSG",
			"NTF (Paris) / Centre France;#Deprecated: Changed projCRS name. Use EPSG:27562 instead"
		],
		[
			7004,
			27572,
			"EPSG",
			"NTF (Paris) / Lambert zone II"
		],
		[
			7005,
			27593,
			"EPSG",
			"NTF (Paris) / Sud France;#Deprecated: Changed projCRS name. Use EPSG:27563 instead"
		],
		[
			7006,
			27583,
			"EPSG",
			"NTF (Paris) / France III;#Deprecated: Changed projCRS name. Use EPSG:27573 instead"
		],
		[
			7007,
			27594,
			"EPSG",
			"NTF (Paris) / Corse;#Deprecated: Changed projCRS name. Use EPSG:27564 instead"
		],
		[
			7008,
			27584,
			"EPSG",
			"NTF (Paris) / France IV;#Deprecated: Changed projCRS name. Use EPSG:27574 instead"
		],
		[
			7009,
			2154,
			"EPSG",
			"RGF93 / Lambert-93"
		],
		[
			7010,
			3942,
			"EPSG",
			"RGF93 / CC42"
		],
		[
			7011,
			3943,
			"EPSG",
			"RGF93 / CC43"
		],
		[
			7012,
			3944,
			"EPSG",
			"RGF93 / CC44"
		],
		[
			7013,
			3945,
			"EPSG",
			"RGF93 / CC45"
		],
		[
			7014,
			3946,
			"EPSG",
			"RGF93 / CC46"
		],
		[
			7015,
			3947,
			"EPSG",
			"RGF93 / CC47"
		],
		[
			7016,
			3948,
			"EPSG",
			"RGF93 / CC48"
		],
		[
			7017,
			3949,
			"EPSG",
			"RGF93 / CC49"
		],
		[
			7018,
			3950,
			"EPSG",
			"RGF93 / CC50"
		],
		[
			8002,
			31466,
			"EPSG",
			"DHDN / 3-degree Gauss-Kruger zone 2"
		],
		[
			8003,
			31467,
			"EPSG",
			"DHDN / 3-degree Gauss-Kruger zone 3"
		],
		[
			8004,
			31468,
			"EPSG",
			"DHDN / 3-degree Gauss-Kruger zone 4"
		],
		[
			8005,
			31469,
			"EPSG",
			"DHDN / 3-degree Gauss-Kruger zone 5"
		],
		[
			8006,
			25831,
			"EPSG",
			"ETRS89 / UTM zone 31N"
		],
		[
			8007,
			25832,
			"EPSG",
			"ETRS89 / UTM zone 32N"
		],
		[
			8008,
			25833,
			"EPSG",
			"ETRS89 / UTM zone 33N"
		],
		[
			8009,
			0,
			";",
			"TRS89 32N 7Stellen;#CRS-Code missing in OCA"
		],
		[
			8010,
			0,
			";",
			"TRS89 33N 7Stellen;#CRS-Code missing in OCA"
		],
		[
			8011,
			0,
			";",
			"TRS89 32N 8Stellen;#CRS-Code missing in OCA"
		],
		[
			8012,
			0,
			";",
			"TRS89 33N 8Stellen;#CRS-Code missing in OCA"
		],
		[
			8013,
			3068,
			"EPSG",
			"DHDN / Soldner Berlin"
		],
		[
			8014,
			3068,
			"EPSG",
			"DHDN / Soldner Berlin"
		],
		[
			9001,
			29902,
			"EPSG",
			"TM65 / Irish Grid"
		],
		[
			9002,
			29903,
			"EPSG",
			"TM75 / Irish Grid"
		],
		[
			9003,
			2157,
			"EPSG",
			"IRENET95 / Irish Transverse Mercator"
		],
		[
			11001,
			30161,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS I"
		],
		[
			11002,
			30162,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS II"
		],
		[
			11003,
			30163,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS III"
		],
		[
			11004,
			30164,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS IV"
		],
		[
			11005,
			30165,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS V"
		],
		[
			11006,
			30166,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS VI"
		],
		[
			11007,
			30167,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS VII"
		],
		[
			11008,
			30168,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS VIII"
		],
		[
			11009,
			30169,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS IX"
		],
		[
			11010,
			30170,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS X"
		],
		[
			11011,
			30171,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XI"
		],
		[
			11012,
			30172,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XII"
		],
		[
			11013,
			30173,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XIII"
		],
		[
			11014,
			30174,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XIV"
		],
		[
			11015,
			30175,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XV"
		],
		[
			11016,
			30176,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XVI"
		],
		[
			11017,
			30177,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XVII"
		],
		[
			11018,
			30178,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XVIII"
		],
		[
			11019,
			30179,
			"EPSG",
			"Tokyo / Japan Plane Rectangular CS XIX"
		],
		[
			11020,
			2443,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS I"
		],
		[
			11021,
			2444,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS II"
		],
		[
			11022,
			2445,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS III"
		],
		[
			11023,
			2446,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS IV"
		],
		[
			11024,
			2447,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS V"
		],
		[
			11025,
			2448,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS VI"
		],
		[
			11026,
			2449,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS VII"
		],
		[
			11027,
			2450,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS VIII"
		],
		[
			11028,
			2451,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS IX"
		],
		[
			11029,
			2452,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS X"
		],
		[
			11030,
			2453,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XI"
		],
		[
			11031,
			2454,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XII"
		],
		[
			11032,
			2455,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XIII"
		],
		[
			11033,
			2456,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XIV"
		],
		[
			11034,
			2457,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XV"
		],
		[
			11035,
			2458,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XVI"
		],
		[
			11036,
			2459,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XVII"
		],
		[
			11037,
			2460,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XVIII"
		],
		[
			11039,
			2461,
			"EPSG",
			"JGD2000 / Japan Plane Rectangular CS XIX"
		],
		[
			12001,
			27391,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone I"
		],
		[
			12002,
			27392,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone II"
		],
		[
			12003,
			27393,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone III"
		],
		[
			12004,
			27394,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone IV"
		],
		[
			12005,
			27395,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone V"
		],
		[
			12006,
			27396,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone VI"
		],
		[
			12007,
			27397,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone VII"
		],
		[
			12008,
			27398,
			"EPSG",
			"NGO 1948 (Oslo) / NGO zone VIII"
		],
		[
			12009,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			12010,
			23032,
			"EPSG",
			"ED50 / UTM zone 32N"
		],
		[
			12011,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			12012,
			23034,
			"EPSG",
			"ED50 / UTM zone 34N"
		],
		[
			12013,
			23035,
			"EPSG",
			"ED50 / UTM zone 35N"
		],
		[
			12014,
			23036,
			"EPSG",
			"ED50 / UTM zone 36N"
		],
		[
			13001,
			6124,
			"EPSG",
			"WGS 84 / EPSG Arctic zone 4-12"
		],
		[
			13002,
			3006,
			"EPSG",
			"SWEREF99 TM"
		],
		[
			13003,
			3007,
			"EPSG",
			"SWEREF99 12 00"
		],
		[
			13004,
			3008,
			"EPSG",
			"SWEREF99 13 30"
		],
		[
			13005,
			3009,
			"EPSG",
			"SWEREF99 15 00"
		],
		[
			13006,
			3010,
			"EPSG",
			"SWEREF99 16 30"
		],
		[
			13007,
			3011,
			"EPSG",
			"SWEREF99 18 00"
		],
		[
			13008,
			3012,
			"EPSG",
			"SWEREF99 14 15"
		],
		[
			13009,
			3013,
			"EPSG",
			"SWEREF99 15 45"
		],
		[
			13010,
			3014,
			"EPSG",
			"SWEREF99 17 15"
		],
		[
			13011,
			3015,
			"EPSG",
			"SWEREF99 18 45"
		],
		[
			13012,
			3016,
			"EPSG",
			"SWEREF99 20 15"
		],
		[
			13013,
			3017,
			"EPSG",
			"SWEREF99 21 45"
		],
		[
			13014,
			3018,
			"EPSG",
			"SWEREF99 23 15"
		],
		[
			14001,
			21781,
			"EPSG",
			"CH1903 / LV03"
		],
		[
			14002,
			2056,
			"EPSG",
			"CH1903+ / LV95"
		],
		[
			15001,
			3912,
			"EPSG",
			"MGI 1901 / Slovene National Grid"
		],
		[
			15002,
			3794,
			"EPSG",
			"Slovenia 1996 / Slovene National Grid"
		],
		[
			16001,
			4685,
			"SR",
			"ORG;Gauss Boaga Fuso Ovest"
		],
		[
			16002,
			4686,
			"EPSG",
			"MAGNA-SIRGAS"
		],
		[
			16003,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			16004,
			23032,
			"EPSG",
			"ED50 / UTM zone 32N"
		],
		[
			16005,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			16006,
			23034,
			"EPSG",
			"ED50 / UTM zone 34N"
		],
		[
			16007,
			25832,
			"EPSG",
			"ETRS89 / UTM zone 32N"
		],
		[
			16008,
			25833,
			"EPSG",
			"ETRS89 / UTM zone 33N"
		],
		[
			16009,
			6707,
			"EPSG",
			"RDN2008 / UTM zone 32N (N-E)"
		],
		[
			16010,
			6708,
			"EPSG",
			"RDN2008 / UTM zone 33N (N-E)"
		],
		[
			16011,
			6875,
			"EPSG",
			"RDN2008 / Italy zone (N-E)"
		],
		[
			16012,
			6876,
			"EPSG",
			"RDN2008 / Zone 12 (N-E)"
		],
		[
			17001,
			2206,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 9"
		],
		[
			17002,
			2207,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 10"
		],
		[
			17003,
			2208,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 11"
		],
		[
			17004,
			2209,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 12"
		],
		[
			17005,
			2210,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 13"
		],
		[
			17006,
			2211,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 14"
		],
		[
			17007,
			2212,
			"EPSG",
			"ED50 / 3-degree Gauss-Kruger zone 15"
		],
		[
			17008,
			7006,
			"EPSG",
			"Nahrwan 1934 / UTM zone 38N"
		],
		[
			18001,
			2046,
			"EPSG",
			"Hartebeesthoek94 / Lo15"
		],
		[
			18002,
			2047,
			"EPSG",
			"Hartebeesthoek94 / Lo17"
		],
		[
			18003,
			2048,
			"EPSG",
			"Hartebeesthoek94 / Lo19"
		],
		[
			18004,
			2049,
			"EPSG",
			"Hartebeesthoek94 / Lo21"
		],
		[
			18005,
			2050,
			"EPSG",
			"Hartebeesthoek94 / Lo23"
		],
		[
			18006,
			2051,
			"EPSG",
			"Hartebeesthoek94 / Lo25"
		],
		[
			18007,
			2052,
			"EPSG",
			"Hartebeesthoek94 / Lo27"
		],
		[
			18008,
			2053,
			"EPSG",
			"Hartebeesthoek94 / Lo29"
		],
		[
			18009,
			2054,
			"EPSG",
			"Hartebeesthoek94 / Lo31"
		],
		[
			18010,
			2055,
			"EPSG",
			"Hartebeesthoek94 / Lo33"
		],
		[
			19e3,
			2193,
			"EPSG",
			"NZGD2000 / New Zealand Transverse Mercator 2000"
		],
		[
			20001,
			28348,
			"EPSG",
			"GDA94 / MGA zone 48"
		],
		[
			20002,
			28349,
			"EPSG",
			"GDA94 / MGA zone 49"
		],
		[
			20003,
			28350,
			"EPSG",
			"GDA94 / MGA zone 50"
		],
		[
			20004,
			28351,
			"EPSG",
			"GDA94 / MGA zone 51"
		],
		[
			20005,
			28352,
			"EPSG",
			"GDA94 / MGA zone 52"
		],
		[
			20006,
			28353,
			"EPSG",
			"GDA94 / MGA zone 53"
		],
		[
			20007,
			28354,
			"EPSG",
			"GDA94 / MGA zone 54"
		],
		[
			20008,
			28355,
			"EPSG",
			"GDA94 / MGA zone 55"
		],
		[
			20009,
			28356,
			"EPSG",
			"GDA94 / MGA zone 56"
		],
		[
			20010,
			28357,
			"EPSG",
			"GDA94 / MGA zone 57"
		],
		[
			20011,
			28358,
			"EPSG",
			"GDA94 / MGA zone 58"
		],
		[
			20012,
			3112,
			"EPSG",
			"GDA94 / Geoscience Australia Lambert"
		],
		[
			21001,
			23032,
			"EPSG",
			"ED50 / UTM zone 32N"
		],
		[
			21002,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			21003,
			25832,
			"EPSG",
			"ETRS89 / UTM zone 32N"
		],
		[
			21004,
			25833,
			"EPSG",
			"ETRS89 / UTM zone 33N"
		],
		[
			23001,
			0,
			";",
			"outh Africa rotated WG15;#CRS-Code missing in OCA"
		],
		[
			23002,
			0,
			";",
			"outh Africa rotated WG17;#CRS-Code missing in OCA"
		],
		[
			23003,
			0,
			";",
			"outh Africa rotated WG19;#CRS-Code missing in OCA"
		],
		[
			23004,
			0,
			";",
			"outh Africa rotated WG21;#CRS-Code missing in OCA"
		],
		[
			23005,
			0,
			";",
			"outh Africa rotated WG23;#CRS-Code missing in OCA"
		],
		[
			23006,
			0,
			";",
			"outh Africa rotated WG25;#CRS-Code missing in OCA"
		],
		[
			23007,
			0,
			";",
			"outh Africa rotated WG27;#CRS-Code missing in OCA"
		],
		[
			23008,
			0,
			";",
			"outh Africa rotated WG29;#CRS-Code missing in OCA"
		],
		[
			23009,
			0,
			";",
			"outh Africa rotated WG31;#CRS-Code missing in OCA"
		],
		[
			23010,
			0,
			";",
			"outh Africa rotated WG33;#CRS-Code missing in OCA"
		],
		[
			24e3,
			4272,
			"EPSG",
			"NZGD49"
		],
		[
			26e3,
			3346,
			"EPSG",
			"LKS94 / Lithuania TM"
		],
		[
			27e3,
			3301,
			"EPSG",
			"Estonian Coordinate System of 1997"
		],
		[
			28e3,
			3059,
			"EPSG",
			"LKS92 / Latvia TM"
		],
		[
			29e3,
			2100,
			"EPSG",
			"GGRS87 / Greek Grid"
		],
		[
			30001,
			23028,
			"EPSG",
			"ED50 / UTM zone 28N"
		],
		[
			30002,
			23029,
			"EPSG",
			"ED50 / UTM zone 29N"
		],
		[
			30003,
			23030,
			"EPSG",
			"ED50 / UTM zone 30N"
		],
		[
			30004,
			23030,
			"EPSG",
			"ED50 / UTM zone 30N"
		],
		[
			30005,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			30006,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			31005,
			7009,
			"SR",
			"ORG;GK 5 Croatia"
		],
		[
			31006,
			7010,
			"SR",
			"ORG;GK 6 Croatia"
		],
		[
			31007,
			3765,
			"EPSG",
			"HTRS96 / Croatia TM"
		],
		[
			32e3,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			33001,
			2169,
			"EPSG",
			"Luxembourg 1930 / Gauss"
		],
		[
			33002,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			34e3,
			29902,
			"EPSG",
			"TM65 / Irish Grid"
		],
		[
			35e3,
			2462,
			"EPSG",
			"Albanian 1987 / Gauss-Kruger zone 4"
		],
		[
			36001,
			25833,
			"EPSG",
			"ETRS89 / UTM zone 33N"
		],
		[
			36002,
			25834,
			"EPSG",
			"ETRS89 / UTM zone 34N"
		],
		[
			37001,
			25834,
			"EPSG",
			"ETRS89 / UTM zone 34N"
		],
		[
			37002,
			25835,
			"EPSG",
			"ETRS89 / UTM zone 35N"
		],
		[
			38001,
			0,
			";",
			"celand UTM HJ1955 26N;#CRS-Code missing in OCA"
		],
		[
			38002,
			0,
			";",
			"celand UTM HJ1955 27N;#CRS-Code missing in OCA"
		],
		[
			38003,
			0,
			";",
			"celand UTM HJ1955 28N;#CRS-Code missing in OCA"
		],
		[
			38004,
			3057,
			"EPSG",
			"ISN93 / Lambert 1993"
		],
		[
			39e3,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			4e4,
			23032,
			"EPSG",
			"ED50 / UTM zone 32N"
		],
		[
			41001,
			28991,
			"EPSG",
			"Amersfoort / RD Old"
		],
		[
			41002,
			23031,
			"EPSG",
			"ED50 / UTM zone 31N"
		],
		[
			41003,
			23032,
			"EPSG",
			"ED50 / UTM zone 32N"
		],
		[
			41004,
			28992,
			"EPSG",
			"Amersfoort / RD New"
		],
		[
			42001,
			27493,
			"EPSG",
			"Datum 73 / Modified Portuguese Grid"
		],
		[
			42002,
			23028,
			"EPSG",
			"ED50 / UTM zone 28N"
		],
		[
			43001,
			0,
			";",
			"omania S42 34N;#CRS-Code missing in OCA"
		],
		[
			43002,
			0,
			";",
			"omania S42 35N;#CRS-Code missing in OCA"
		],
		[
			44e3,
			23033,
			"EPSG",
			"ED50 / UTM zone 33N"
		],
		[
			46001,
			5514,
			"EPSG",
			"S-JTSK / Krovak East North"
		],
		[
			46002,
			28403,
			"EPSG",
			"Pulkovo 1942 / Gauss-Kruger zone 3;#Deprecated: Change of base CRS. Use EPSG:3333 instead"
		],
		[
			47001,
			5514,
			"EPSG",
			"S-JTSK / Krovak East North"
		],
		[
			47002,
			28403,
			"EPSG",
			"Pulkovo 1942 / Gauss-Kruger zone 3;#Deprecated: Change of base CRS. Use EPSG:3333 instead"
		],
		[
			47003,
			28404,
			"EPSG",
			"Pulkovo 1942 / Gauss-Kruger zone 4"
		],
		[
			48001,
			2180,
			"EPSG",
			"ETRS89 / Poland CS92"
		],
		[
			48002,
			2176,
			"EPSG",
			"ETRS89 / Poland CS2000 zone 5"
		],
		[
			48003,
			2177,
			"EPSG",
			"ETRS89 / Poland CS2000 zone 6"
		],
		[
			48004,
			2178,
			"EPSG",
			"ETRS89 / Poland CS2000 zone 7"
		],
		[
			48005,
			2179,
			"EPSG",
			"ETRS89 / Poland CS2000 zone 8"
		],
		[
			49e3,
			23700,
			"EPSG",
			"HD72 / EOV"
		],
		[
			50001,
			26929,
			"EPSG",
			"NAD83 / Alabama East"
		],
		[
			50002,
			26930,
			"EPSG",
			"NAD83 / Alabama West"
		],
		[
			50004,
			26932,
			"EPSG",
			"NAD83 / Alaska zone 2"
		],
		[
			50005,
			26933,
			"EPSG",
			"NAD83 / Alaska zone 3"
		],
		[
			50006,
			26934,
			"EPSG",
			"NAD83 / Alaska zone 4"
		],
		[
			50007,
			26935,
			"EPSG",
			"NAD83 / Alaska zone 5"
		],
		[
			50008,
			26936,
			"EPSG",
			"NAD83 / Alaska zone 6"
		],
		[
			50009,
			26937,
			"EPSG",
			"NAD83 / Alaska zone 7"
		],
		[
			50010,
			26938,
			"EPSG",
			"NAD83 / Alaska zone 8"
		],
		[
			50011,
			26939,
			"EPSG",
			"NAD83 / Alaska zone 9"
		],
		[
			50012,
			26940,
			"EPSG",
			"NAD83 / Alaska zone 10"
		],
		[
			50013,
			26948,
			"EPSG",
			"NAD83 / Arizona East"
		],
		[
			50014,
			26949,
			"EPSG",
			"NAD83 / Arizona Central"
		],
		[
			50015,
			26950,
			"EPSG",
			"NAD83 / Arizona West"
		],
		[
			50016,
			26951,
			"EPSG",
			"NAD83 / Arkansas North"
		],
		[
			50017,
			26952,
			"EPSG",
			"NAD83 / Arkansas South"
		],
		[
			50018,
			26941,
			"EPSG",
			"NAD83 / California zone 1"
		],
		[
			50019,
			26942,
			"EPSG",
			"NAD83 / California zone 2"
		],
		[
			50020,
			26943,
			"EPSG",
			"NAD83 / California zone 3"
		],
		[
			50021,
			26944,
			"EPSG",
			"NAD83 / California zone 4"
		],
		[
			50022,
			26945,
			"EPSG",
			"NAD83 / California zone 5"
		],
		[
			50023,
			26946,
			"EPSG",
			"NAD83 / California zone 6"
		],
		[
			50024,
			26953,
			"EPSG",
			"NAD83 / Colorado North"
		],
		[
			50025,
			26954,
			"EPSG",
			"NAD83 / Colorado Central"
		],
		[
			50026,
			26955,
			"EPSG",
			"NAD83 / Colorado South"
		],
		[
			50027,
			26956,
			"EPSG",
			"NAD83 / Connecticut"
		],
		[
			50028,
			26957,
			"EPSG",
			"NAD83 / Delaware"
		],
		[
			50029,
			26958,
			"EPSG",
			"NAD83 / Florida East"
		],
		[
			50030,
			26959,
			"EPSG",
			"NAD83 / Florida West"
		],
		[
			50031,
			26960,
			"EPSG",
			"NAD83 / Florida North"
		],
		[
			50032,
			26966,
			"EPSG",
			"NAD83 / Georgia East"
		],
		[
			50033,
			26967,
			"EPSG",
			"NAD83 / Georgia West"
		],
		[
			50034,
			26961,
			"EPSG",
			"NAD83 / Hawaii zone 1"
		],
		[
			50035,
			26962,
			"EPSG",
			"NAD83 / Hawaii zone 2"
		],
		[
			50036,
			26963,
			"EPSG",
			"NAD83 / Hawaii zone 3"
		],
		[
			50037,
			26964,
			"EPSG",
			"NAD83 / Hawaii zone 4"
		],
		[
			50038,
			26965,
			"EPSG",
			"NAD83 / Hawaii zone 5"
		],
		[
			50039,
			26968,
			"EPSG",
			"NAD83 / Idaho East"
		],
		[
			50040,
			26969,
			"EPSG",
			"NAD83 / Idaho Central"
		],
		[
			50041,
			26970,
			"EPSG",
			"NAD83 / Idaho West"
		],
		[
			50042,
			26971,
			"EPSG",
			"NAD83 / Illinois East"
		],
		[
			50043,
			26972,
			"EPSG",
			"NAD83 / Illinois West"
		],
		[
			50044,
			26973,
			"EPSG",
			"NAD83 / Indiana East"
		],
		[
			50045,
			26974,
			"EPSG",
			"NAD83 / Indiana West"
		],
		[
			50046,
			26975,
			"EPSG",
			"NAD83 / Iowa North"
		],
		[
			50047,
			26976,
			"EPSG",
			"NAD83 / Iowa South"
		],
		[
			50048,
			26977,
			"EPSG",
			"NAD83 / Kansas North"
		],
		[
			50049,
			26978,
			"EPSG",
			"NAD83 / Kansas South"
		],
		[
			50050,
			26979,
			"EPSG",
			"NAD83 / Kentucky North;#Deprecated: Deprecation of constituent map projection 14100 which was in error. Use EPSG:2205 instead"
		],
		[
			50051,
			26980,
			"EPSG",
			"NAD83 / Kentucky South"
		],
		[
			50052,
			26981,
			"EPSG",
			"NAD83 / Louisiana North"
		],
		[
			50053,
			26982,
			"EPSG",
			"NAD83 / Louisiana South"
		],
		[
			50054,
			32199,
			"EPSG",
			"NAD83 / Louisiana Offshore"
		],
		[
			50055,
			26983,
			"EPSG",
			"NAD83 / Maine East"
		],
		[
			50056,
			26984,
			"EPSG",
			"NAD83 / Maine West"
		],
		[
			50057,
			26985,
			"EPSG",
			"NAD83 / Maryland"
		],
		[
			50058,
			26986,
			"EPSG",
			"NAD83 / Massachusetts Mainland"
		],
		[
			50059,
			26987,
			"EPSG",
			"NAD83 / Massachusetts Island"
		],
		[
			50060,
			26988,
			"EPSG",
			"NAD83 / Michigan North"
		],
		[
			50061,
			26989,
			"EPSG",
			"NAD83 / Michigan Central"
		],
		[
			50062,
			26990,
			"EPSG",
			"NAD83 / Michigan South"
		],
		[
			50063,
			26991,
			"EPSG",
			"NAD83 / Minnesota North"
		],
		[
			50064,
			26992,
			"EPSG",
			"NAD83 / Minnesota Central"
		],
		[
			50065,
			26993,
			"EPSG",
			"NAD83 / Minnesota South"
		],
		[
			50066,
			26994,
			"EPSG",
			"NAD83 / Mississippi East"
		],
		[
			50067,
			26995,
			"EPSG",
			"NAD83 / Mississippi West"
		],
		[
			50068,
			26996,
			"EPSG",
			"NAD83 / Missouri East"
		],
		[
			50069,
			26997,
			"EPSG",
			"NAD83 / Missouri Central"
		],
		[
			50070,
			26998,
			"EPSG",
			"NAD83 / Missouri West"
		],
		[
			50071,
			32100,
			"EPSG",
			"NAD83 / Montana"
		],
		[
			50072,
			32104,
			"EPSG",
			"NAD83 / Nebraska"
		],
		[
			50073,
			32107,
			"EPSG",
			"NAD83 / Nevada East"
		],
		[
			50074,
			32108,
			"EPSG",
			"NAD83 / Nevada Central"
		],
		[
			50075,
			32109,
			"EPSG",
			"NAD83 / Nevada West"
		],
		[
			50076,
			32110,
			"EPSG",
			"NAD83 / New Hampshire"
		],
		[
			50077,
			32111,
			"EPSG",
			"NAD83 / New Jersey"
		],
		[
			50078,
			32112,
			"EPSG",
			"NAD83 / New Mexico East"
		],
		[
			50079,
			32113,
			"EPSG",
			"NAD83 / New Mexico Central"
		],
		[
			50080,
			32114,
			"EPSG",
			"NAD83 / New Mexico West"
		],
		[
			50081,
			32115,
			"EPSG",
			"NAD83 / New York East"
		],
		[
			50082,
			32116,
			"EPSG",
			"NAD83 / New York Central"
		],
		[
			50083,
			32117,
			"EPSG",
			"NAD83 / New York West"
		],
		[
			50084,
			32118,
			"EPSG",
			"NAD83 / New York Long Island"
		],
		[
			50085,
			32119,
			"EPSG",
			"NAD83 / North Carolina"
		],
		[
			50086,
			32120,
			"EPSG",
			"NAD83 / North Dakota North"
		],
		[
			50087,
			32121,
			"EPSG",
			"NAD83 / North Dakota South"
		],
		[
			50088,
			32122,
			"EPSG",
			"NAD83 / Ohio North"
		],
		[
			50089,
			32123,
			"EPSG",
			"NAD83 / Ohio South"
		],
		[
			50090,
			32124,
			"EPSG",
			"NAD83 / Oklahoma North"
		],
		[
			50091,
			32125,
			"EPSG",
			"NAD83 / Oklahoma South"
		],
		[
			50092,
			32126,
			"EPSG",
			"NAD83 / Oregon North"
		],
		[
			50093,
			32127,
			"EPSG",
			"NAD83 / Oregon South"
		],
		[
			50094,
			32128,
			"EPSG",
			"NAD83 / Pennsylvania North"
		],
		[
			50095,
			32129,
			"EPSG",
			"NAD83 / Pennsylvania South"
		],
		[
			50096,
			32130,
			"EPSG",
			"NAD83 / Rhode Island"
		],
		[
			50097,
			32133,
			"EPSG",
			"NAD83 / South Carolina"
		],
		[
			50098,
			32134,
			"EPSG",
			"NAD83 / South Dakota North"
		],
		[
			50099,
			32135,
			"EPSG",
			"NAD83 / South Dakota South"
		],
		[
			50100,
			32136,
			"EPSG",
			"NAD83 / Tennessee"
		],
		[
			50101,
			32137,
			"EPSG",
			"NAD83 / Texas North"
		],
		[
			50102,
			32138,
			"EPSG",
			"NAD83 / Texas North Central"
		],
		[
			50103,
			32139,
			"EPSG",
			"NAD83 / Texas Central"
		],
		[
			50104,
			32140,
			"EPSG",
			"NAD83 / Texas South Central"
		],
		[
			50105,
			32141,
			"EPSG",
			"NAD83 / Texas South"
		],
		[
			50106,
			32142,
			"EPSG",
			"NAD83 / Utah North"
		],
		[
			50107,
			32143,
			"EPSG",
			"NAD83 / Utah Central"
		],
		[
			50108,
			32144,
			"EPSG",
			"NAD83 / Utah South"
		],
		[
			50109,
			32145,
			"EPSG",
			"NAD83 / Vermont"
		],
		[
			50110,
			32146,
			"EPSG",
			"NAD83 / Virginia North"
		],
		[
			50111,
			32147,
			"EPSG",
			"NAD83 / Virginia South"
		],
		[
			50112,
			32148,
			"EPSG",
			"NAD83 / Washington North"
		],
		[
			50113,
			32149,
			"EPSG",
			"NAD83 / Washington South"
		],
		[
			50114,
			32150,
			"EPSG",
			"NAD83 / West Virginia North"
		],
		[
			50115,
			32151,
			"EPSG",
			"NAD83 / West Virginia South"
		],
		[
			50116,
			32152,
			"EPSG",
			"NAD83 / Wisconsin North"
		],
		[
			50117,
			32153,
			"EPSG",
			"NAD83 / Wisconsin Central"
		],
		[
			50118,
			32154,
			"EPSG",
			"NAD83 / Wisconsin South"
		],
		[
			50119,
			32155,
			"EPSG",
			"NAD83 / Wyoming East"
		],
		[
			50120,
			32156,
			"EPSG",
			"NAD83 / Wyoming East Central"
		],
		[
			50121,
			32157,
			"EPSG",
			"NAD83 / Wyoming West Central"
		],
		[
			50122,
			32158,
			"EPSG",
			"NAD83 / Wyoming West"
		],
		[
			50123,
			32158,
			"EPSG",
			"NAD83 / Wyoming West"
		],
		[
			51e3,
			0,
			";",
			"abon Datum GRS_1980;#CRS-Code missing in OCA"
		],
		[
			52001,
			0,
			";",
			"razil UTM Corrego Alegre Fuso 18;#CRS-Code missing in OCA"
		],
		[
			52002,
			0,
			";",
			"razil UTM Corrego Alegre Fuso 19;#CRS-Code missing in OCA"
		],
		[
			52003,
			0,
			";",
			"razil UTM Corrego Alegre Fuso 20;#CRS-Code missing in OCA"
		],
		[
			52004,
			22521,
			"EPSG",
			"Corrego Alegre 1970-72 / UTM zone 21S"
		],
		[
			52005,
			22522,
			"EPSG",
			"Corrego Alegre 1970-72 / UTM zone 22S"
		],
		[
			52006,
			22523,
			"EPSG",
			"Corrego Alegre 1970-72 / UTM zone 23S"
		],
		[
			52007,
			22524,
			"EPSG",
			"Corrego Alegre 1970-72 / UTM zone 24S"
		],
		[
			52008,
			22525,
			"EPSG",
			"Corrego Alegre 1970-72 / UTM zone 25S"
		],
		[
			52009,
			0,
			";",
			"razil UTM SAD69 Fuso 18;#CRS-Code missing in OCA"
		],
		[
			52010,
			0,
			";",
			"razil UTM SAD69 Fuso 19;#CRS-Code missing in OCA"
		],
		[
			52011,
			0,
			";",
			"razil UTM SAD69 Fuso 20;#CRS-Code missing in OCA"
		],
		[
			52012,
			0,
			";",
			"razil UTM SAD69 Fuso 21;#CRS-Code missing in OCA"
		],
		[
			52013,
			0,
			";",
			"razil UTM SAD69 Fuso 22;#CRS-Code missing in OCA"
		],
		[
			52014,
			0,
			";",
			"razil UTM SAD69 Fuso 23;#CRS-Code missing in OCA"
		],
		[
			52015,
			0,
			";",
			"razil UTM SAD69 Fuso 24;#CRS-Code missing in OCA"
		],
		[
			52016,
			0,
			";",
			"razil UTM SAD69 Fuso 25;#CRS-Code missing in OCA"
		],
		[
			52017,
			31978,
			"EPSG",
			"SIRGAS 2000 / UTM zone 18S"
		],
		[
			52018,
			31979,
			"EPSG",
			"SIRGAS 2000 / UTM zone 19S"
		],
		[
			52019,
			31980,
			"EPSG",
			"SIRGAS 2000 / UTM zone 20S"
		],
		[
			52020,
			31981,
			"EPSG",
			"SIRGAS 2000 / UTM zone 21S"
		],
		[
			52021,
			31982,
			"EPSG",
			"SIRGAS 2000 / UTM zone 22S"
		],
		[
			52022,
			31983,
			"EPSG",
			"SIRGAS 2000 / UTM zone 23S"
		],
		[
			52023,
			31984,
			"EPSG",
			"SIRGAS 2000 / UTM zone 24S"
		],
		[
			52024,
			31985,
			"EPSG",
			"SIRGAS 2000 / UTM zone 25S"
		],
		[
			53e3,
			2039,
			"EPSG",
			"Israel 1993 / Israeli TM Grid"
		],
		[
			54001,
			3375,
			"EPSG",
			"GDM2000 / Peninsula RSO"
		],
		[
			54002,
			3376,
			"EPSG",
			"GDM2000 / East Malaysia BRSO"
		],
		[
			55e3,
			0,
			";",
			"serDefined Lambert;#CRS-Code missing in OCA"
		],
		[
			56e3,
			3857,
			"EPSG",
			"WGS 84 / Pseudo-Mercator;#OpenLayers:90091"
		],
		[
			57e3,
			2041,
			"EPSG",
			"Abidjan 1987 / UTM zone 30N"
		],
		[
			58001,
			0,
			";",
			"ri Lanka Datum 1999;#CRS-Code missing in OCA"
		],
		[
			58002,
			0,
			";",
			"ri Lanka Datum 1999;#CRS-Code missing in OCA"
		],
		[
			6e4,
			0,
			";",
			"serDefined;#CRS-Code missing in OCA"
		],
		[
			61e3,
			2326,
			"EPSG",
			"Hong Kong 1980 Grid System"
		],
		[
			62001,
			26901,
			"EPSG",
			"NAD83 / UTM zone 1N"
		],
		[
			62002,
			26902,
			"EPSG",
			"NAD83 / UTM zone 2N"
		],
		[
			62004,
			26903,
			"EPSG",
			"NAD83 / UTM zone 3N"
		],
		[
			62005,
			26904,
			"EPSG",
			"NAD83 / UTM zone 4N"
		],
		[
			62006,
			26905,
			"EPSG",
			"NAD83 / UTM zone 5N"
		],
		[
			62007,
			26906,
			"EPSG",
			"NAD83 / UTM zone 6N"
		],
		[
			62008,
			26907,
			"EPSG",
			"NAD83 / UTM zone 7N"
		],
		[
			62009,
			26908,
			"EPSG",
			"NAD83 / UTM zone 8N"
		],
		[
			62010,
			26909,
			"EPSG",
			"NAD83 / UTM zone 9N"
		],
		[
			62011,
			26910,
			"EPSG",
			"NAD83 / UTM zone 10N"
		],
		[
			62012,
			26911,
			"EPSG",
			"NAD83 / UTM zone 11N"
		],
		[
			62013,
			26912,
			"EPSG",
			"NAD83 / UTM zone 12N"
		],
		[
			62014,
			26913,
			"EPSG",
			"NAD83 / UTM zone 13N"
		],
		[
			62015,
			26914,
			"EPSG",
			"NAD83 / UTM zone 14N"
		],
		[
			62016,
			26915,
			"EPSG",
			"NAD83 / UTM zone 15N"
		],
		[
			62017,
			26916,
			"EPSG",
			"NAD83 / UTM zone 16N"
		],
		[
			62018,
			26917,
			"EPSG",
			"NAD83 / UTM zone 17N"
		],
		[
			62019,
			26918,
			"EPSG",
			"NAD83 / UTM zone 18N"
		],
		[
			62020,
			26919,
			"EPSG",
			"NAD83 / UTM zone 19N"
		],
		[
			62021,
			26920,
			"EPSG",
			"NAD83 / UTM zone 20N"
		],
		[
			62022,
			26921,
			"EPSG",
			"NAD83 / UTM zone 21N"
		],
		[
			62023,
			26922,
			"EPSG",
			"NAD83 / UTM zone 22N"
		],
		[
			62024,
			26923,
			"EPSG",
			"NAD83 / UTM zone 23N"
		],
		[
			63001,
			25828,
			"EPSG",
			"ETRS89 / UTM zone 28N"
		],
		[
			63002,
			25829,
			"EPSG",
			"ETRS89 / UTM zone 29N"
		],
		[
			63003,
			25830,
			"EPSG",
			"ETRS89 / UTM zone 30N"
		],
		[
			63004,
			25831,
			"EPSG",
			"ETRS89 / UTM zone 31N"
		],
		[
			63005,
			25832,
			"EPSG",
			"ETRS89 / UTM zone 32N"
		],
		[
			63006,
			25833,
			"EPSG",
			"ETRS89 / UTM zone 33N"
		],
		[
			63007,
			25834,
			"EPSG",
			"ETRS89 / UTM zone 34N"
		],
		[
			63008,
			25835,
			"EPSG",
			"ETRS89 / UTM zone 35N"
		],
		[
			63009,
			25836,
			"EPSG",
			"ETRS89 / UTM zone 36N"
		],
		[
			63010,
			25837,
			"EPSG",
			"ETRS89 / UTM zone 37N"
		],
		[
			63011,
			25838,
			"EPSG",
			"ETRS89 / UTM zone 38N"
		],
		[
			64e3,
			3785,
			"EPSG",
			"Popular Visualisation CRS / Mercator"
		],
		[
			65e3,
			3857,
			"EPSG",
			"WGS 84 / Pseudo-Mercator"
		],
		[
			66001,
			102629,
			"ESRI",
			"NAD 1983 StatePlane Alabama East FIPS 0101 Feet"
		],
		[
			66002,
			102630,
			"ESRI",
			"NAD 1983 StatePlane Alabama West FIPS 0102 Feet"
		],
		[
			66013,
			2222,
			"EPSG",
			"NAD83 / Arizona East (ft)"
		],
		[
			66014,
			2223,
			"EPSG",
			"NAD83 / Arizona Central (ft)"
		],
		[
			66015,
			2224,
			"EPSG",
			"NAD83 / Arizona West (ft)"
		],
		[
			66016,
			3433,
			"EPSG",
			"NAD83 / Arkansas North (ftUS)"
		],
		[
			66017,
			3434,
			"EPSG",
			"NAD83 / Arkansas South (ftUS)"
		],
		[
			66018,
			2225,
			"EPSG",
			"NAD83 / California zone 1 (ftUS)"
		],
		[
			66019,
			2226,
			"EPSG",
			"NAD83 / California zone 2 (ftUS)"
		],
		[
			66020,
			2227,
			"EPSG",
			"NAD83 / California zone 3 (ftUS)"
		],
		[
			66021,
			2228,
			"EPSG",
			"NAD83 / California zone 4 (ftUS)"
		],
		[
			66022,
			2229,
			"EPSG",
			"NAD83 / California zone 5 (ftUS)"
		],
		[
			66023,
			2230,
			"EPSG",
			"NAD83 / California zone 6 (ftUS)"
		],
		[
			66024,
			2231,
			"EPSG",
			"NAD83 / Colorado North (ftUS)"
		],
		[
			66025,
			2232,
			"EPSG",
			"NAD83 / Colorado Central (ftUS)"
		],
		[
			66026,
			2233,
			"EPSG",
			"NAD83 / Colorado South (ftUS)"
		],
		[
			66027,
			2234,
			"EPSG",
			"NAD83 / Connecticut (ftUS)"
		],
		[
			66028,
			2235,
			"EPSG",
			"NAD83 / Delaware (ftUS)"
		],
		[
			66029,
			2236,
			"EPSG",
			"NAD83 / Florida East (ftUS)"
		],
		[
			66030,
			2237,
			"EPSG",
			"NAD83 / Florida West (ftUS)"
		],
		[
			66031,
			2238,
			"EPSG",
			"NAD83 / Florida North (ftUS)"
		],
		[
			66032,
			2239,
			"EPSG",
			"NAD83 / Georgia East (ftUS)"
		],
		[
			66033,
			2240,
			"EPSG",
			"NAD83 / Georgia West (ftUS)"
		],
		[
			66036,
			3759,
			"EPSG",
			"NAD83 / Hawaii zone 3 (ftUS)"
		],
		[
			66039,
			2241,
			"EPSG",
			"NAD83 / Idaho East (ftUS)"
		],
		[
			66040,
			2242,
			"EPSG",
			"NAD83 / Idaho Central (ftUS)"
		],
		[
			66041,
			2243,
			"EPSG",
			"NAD83 / Idaho West (ftUS)"
		],
		[
			66042,
			3435,
			"EPSG",
			"NAD83 / Illinois East (ftUS)"
		],
		[
			66043,
			3436,
			"EPSG",
			"NAD83 / Illinois West (ftUS)"
		],
		[
			66044,
			2244,
			"EPSG",
			"NAD83 / Indiana East (ftUS);#Deprecated: Constituent projection deprecated. Use EPSG:2965 instead"
		],
		[
			66045,
			2245,
			"EPSG",
			"NAD83 / Indiana West (ftUS);#Deprecated: Constituent projection deprecated. Use EPSG:2966 instead"
		],
		[
			66046,
			3417,
			"EPSG",
			"NAD83 / Iowa North (ftUS)"
		],
		[
			66047,
			3418,
			"EPSG",
			"NAD83 / Iowa South (ftUS)"
		],
		[
			66048,
			3419,
			"EPSG",
			"NAD83 / Kansas North (ftUS)"
		],
		[
			66049,
			3420,
			"EPSG",
			"NAD83 / Kansas South (ftUS)"
		],
		[
			66050,
			2246,
			"EPSG",
			"NAD83 / Kentucky North (ftUS)"
		],
		[
			66051,
			2247,
			"EPSG",
			"NAD83 / Kentucky South (ftUS)"
		],
		[
			66052,
			3451,
			"EPSG",
			"NAD83 / Louisiana North (ftUS)"
		],
		[
			66053,
			3452,
			"EPSG",
			"NAD83 / Louisiana South (ftUS)"
		],
		[
			66054,
			3453,
			"EPSG",
			"NAD83 / Louisiana Offshore (ftUS)"
		],
		[
			66055,
			26847,
			"EPSG",
			"NAD83 / Maine East (ftUS)"
		],
		[
			66056,
			26848,
			"EPSG",
			"NAD83 / Maine West (ftUS)"
		],
		[
			66057,
			2248,
			"EPSG",
			"NAD83 / Maryland (ftUS)"
		],
		[
			66058,
			2249,
			"EPSG",
			"NAD83 / Massachusetts Mainland (ftUS)"
		],
		[
			66059,
			2250,
			"EPSG",
			"NAD83 / Massachusetts Island (ftUS)"
		],
		[
			66060,
			2251,
			"EPSG",
			"NAD83 / Michigan North (ft)"
		],
		[
			66061,
			2252,
			"EPSG",
			"NAD83 / Michigan Central (ft)"
		],
		[
			66062,
			2253,
			"EPSG",
			"NAD83 / Michigan South (ft)"
		],
		[
			66063,
			26849,
			"EPSG",
			"NAD83 / Minnesota North (ftUS)"
		],
		[
			66064,
			26850,
			"EPSG",
			"NAD83 / Minnesota Central (ftUS)"
		],
		[
			66065,
			26851,
			"EPSG",
			"NAD83 / Minnesota South (ftUS)"
		],
		[
			66066,
			2254,
			"EPSG",
			"NAD83 / Mississippi East (ftUS)"
		],
		[
			66067,
			2255,
			"EPSG",
			"NAD83 / Mississippi West (ftUS)"
		],
		[
			66068,
			102696,
			"ESRI",
			"NAD 1983 StatePlane Missouri East FIPS 2401 Feet"
		],
		[
			66069,
			102697,
			"ESRI",
			"NAD 1983 StatePlane Missouri Central FIPS 2402 Feet"
		],
		[
			66070,
			102698,
			"ESRI",
			"NAD 1983 StatePlane Missouri West FIPS 2403 Feet"
		],
		[
			66071,
			2256,
			"EPSG",
			"NAD83 / Montana (ft)"
		],
		[
			66072,
			26852,
			"EPSG",
			"NAD83 / Nebraska (ftUS)"
		],
		[
			66073,
			3421,
			"EPSG",
			"NAD83 / Nevada East (ftUS)"
		],
		[
			66074,
			3422,
			"EPSG",
			"NAD83 / Nevada Central (ftUS)"
		],
		[
			66075,
			3423,
			"EPSG",
			"NAD83 / Nevada West (ftUS)"
		],
		[
			66076,
			3437,
			"EPSG",
			"NAD83 / New Hampshire (ftUS)"
		],
		[
			66077,
			3424,
			"EPSG",
			"NAD83 / New Jersey (ftUS)"
		],
		[
			66078,
			2257,
			"EPSG",
			"NAD83 / New Mexico East (ftUS)"
		],
		[
			66079,
			2258,
			"EPSG",
			"NAD83 / New Mexico Central (ftUS)"
		],
		[
			66080,
			2259,
			"EPSG",
			"NAD83 / New Mexico West (ftUS)"
		],
		[
			66081,
			2260,
			"EPSG",
			"NAD83 / New York East (ftUS)"
		],
		[
			66082,
			2261,
			"EPSG",
			"NAD83 / New York Central (ftUS)"
		],
		[
			66083,
			2262,
			"EPSG",
			"NAD83 / New York West (ftUS)"
		],
		[
			66084,
			2263,
			"EPSG",
			"NAD83 / New York Long Island (ftUS)"
		],
		[
			66085,
			2264,
			"EPSG",
			"NAD83 / North Carolina (ftUS)"
		],
		[
			66086,
			2265,
			"EPSG",
			"NAD83 / North Dakota North (ft)"
		],
		[
			66087,
			2266,
			"EPSG",
			"NAD83 / North Dakota South (ft)"
		],
		[
			66088,
			3734,
			"EPSG",
			"NAD83 / Ohio North (ftUS)"
		],
		[
			66089,
			3735,
			"EPSG",
			"NAD83 / Ohio South (ftUS)"
		],
		[
			66090,
			2267,
			"EPSG",
			"NAD83 / Oklahoma North (ftUS)"
		],
		[
			66091,
			2268,
			"EPSG",
			"NAD83 / Oklahoma South (ftUS)"
		],
		[
			66092,
			2269,
			"EPSG",
			"NAD83 / Oregon North (ft)"
		],
		[
			66093,
			2270,
			"EPSG",
			"NAD83 / Oregon South (ft)"
		],
		[
			66094,
			2271,
			"EPSG",
			"NAD83 / Pennsylvania North (ftUS)"
		],
		[
			66095,
			2272,
			"EPSG",
			"NAD83 / Pennsylvania South (ftUS)"
		],
		[
			66096,
			3438,
			"EPSG",
			"NAD83 / Rhode Island (ftUS)"
		],
		[
			66097,
			2273,
			"EPSG",
			"NAD83 / South Carolina (ft)"
		],
		[
			66098,
			3454,
			"EPSG",
			"NAD83 / South Dakota North (ftUS)"
		],
		[
			66099,
			3455,
			"EPSG",
			"NAD83 / South Dakota South (ftUS)"
		],
		[
			66100,
			2274,
			"EPSG",
			"NAD83 / Tennessee (ftUS)"
		],
		[
			66101,
			2275,
			"EPSG",
			"NAD83 / Texas North (ftUS)"
		],
		[
			66102,
			2276,
			"EPSG",
			"NAD83 / Texas North Central (ftUS)"
		],
		[
			66103,
			2277,
			"EPSG",
			"NAD83 / Texas Central (ftUS)"
		],
		[
			66104,
			2278,
			"EPSG",
			"NAD83 / Texas South Central (ftUS)"
		],
		[
			66105,
			2279,
			"EPSG",
			"NAD83 / Texas South (ftUS)"
		],
		[
			66106,
			3560,
			"EPSG",
			"NAD83 / Utah North (ftUS)"
		],
		[
			66107,
			3566,
			"EPSG",
			"NAD83 / Utah Central (ftUS)"
		],
		[
			66108,
			3567,
			"EPSG",
			"NAD83 / Utah South (ftUS)"
		],
		[
			66109,
			102745,
			"ESRI",
			"NAD 1983 StatePlane Vermont FIPS 4400 Feet"
		],
		[
			66110,
			2283,
			"EPSG",
			"NAD83 / Virginia North (ftUS)"
		],
		[
			66111,
			2284,
			"EPSG",
			"NAD83 / Virginia South (ftUS)"
		],
		[
			66112,
			2285,
			"EPSG",
			"NAD83 / Washington North (ftUS)"
		],
		[
			66113,
			2286,
			"EPSG",
			"NAD83 / Washington South (ftUS)"
		],
		[
			66114,
			26853,
			"EPSG",
			"NAD83 / West Virginia North (ftUS)"
		],
		[
			66115,
			26854,
			"EPSG",
			"NAD83 / West Virginia South (ftUS)"
		],
		[
			66116,
			2287,
			"EPSG",
			"NAD83 / Wisconsin North (ftUS)"
		],
		[
			66117,
			2288,
			"EPSG",
			"NAD83 / Wisconsin Central (ftUS)"
		],
		[
			66118,
			2289,
			"EPSG",
			"NAD83 / Wisconsin South (ftUS)"
		],
		[
			66119,
			3736,
			"EPSG",
			"NAD83 / Wyoming East (ftUS)"
		],
		[
			66120,
			3737,
			"EPSG",
			"NAD83 / Wyoming East Central (ftUS)"
		],
		[
			66121,
			3738,
			"EPSG",
			"NAD83 / Wyoming West Central (ftUS)"
		],
		[
			66122,
			3739,
			"EPSG",
			"NAD83 / Wyoming West (ftUS)"
		],
		[
			66123,
			102761,
			"ESRI",
			"NAD 1983 StatePlane Puerto Rico Virgin Islands FIPS 5200 Feet"
		],
		[
			67e3,
			7392,
			"SR",
			"ORG;KosovaRef01"
		],
		[
			68e3,
			21037,
			"EPSG",
			"Arc 1960 / UTM zone 37S"
		],
		[
			69e3,
			2586,
			"EPSG",
			"Pulkovo 1942 / 3-degree Gauss-Kruger CM 33E"
		],
		[
			7e4,
			2019,
			"EPSG",
			"NAD27(76) / MTM zone 10"
		]
	];
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/crs.js
var require_crs = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var crsGrids = require_crs_grids();
	var TdPoly = require_td_poly();
	var hundredsMmToMeter = 1 / (100 * 1e3);
	module.exports = class Crs {
		/**
		* @type {number}
		*/
		easting;
		/**
		* @type {number}
		**/
		northing;
		/**
		* @type {number}
		**/
		scale;
		/**
		* @type {number}
		**/
		gridId;
		/**
		* @type {number}
		* @description Grivation in radians
		**/
		grivation;
		/**
		* @type {number}
		*/
		code;
		/**
		* @type {string}
		*/
		catalog;
		/**
		* @type {string}
		**/
		name;
		/**
		* @param {import('./parameter-string').ParameterStringValues} scalePar
		*/
		constructor(scalePar) {
			const { x: easting, y: northing, m: scale, i: gridId, a: grivation } = scalePar;
			this.easting = Number(easting);
			this.northing = Number(northing);
			this.scale = Number(scale);
			this.gridId = Number(gridId);
			this.grivation = Number(grivation) / 180 * Math.PI;
			this.grid = crsGrids.find((g) => g[0] === this.gridId);
			const [, code, catalog, name] = this.grid || [
				this.gridId,
				0,
				null,
				null
			];
			this.code = code;
			this.catalog = catalog;
			this.name = name;
		}
		/**
		* Converts an OCAD map coordinate (paper coordinates) to
		* a coordinate in this CRS.
		* @param {TdPoly|number[]} coord
		* @returns {TdPoly|number[]} the projected coordinate;
		* if the input is a TdPoly, the output is a TdPoly instance, otherwise just a coordinate array
		*/
		toProjectedCoord(coord) {
			coord = rotate(coord, -this.grivation);
			const projected = [coord[0] * hundredsMmToMeter * this.scale + this.easting, coord[1] * hundredsMmToMeter * this.scale + this.northing];
			return coord instanceof TdPoly ? new TdPoly(projected[0], projected[1], coord.xFlags, coord.yFlags) : projected;
		}
		/**
		* Converts a coordinate in this CRS to an OCAD map coordinate (paper coordinates).
		* @param {TdPoly|number[]} coord
		* @returns {TdPoly|number[]} the map coordinate;
		* if the input is a TdPoly, the output is a TdPoly instance, otherwise just a coordinate array
		*/
		toMapCoord(coord) {
			const map = [(coord[0] - this.easting) / hundredsMmToMeter / this.scale, (coord[1] - this.northing) / hundredsMmToMeter / this.scale];
			coord = rotate(coord, this.grivation);
			return coord instanceof TdPoly ? new TdPoly(map[0], map[1], coord.xFlags, coord.yFlags) : map;
		}
	};
	/**
	* Rotates a coordinate around the origin.
	*
	* @param {number[]|TdPoly} c the coordinate to rotate
	* @param {number} theta rotation angle in radians
	* @returns {number[]|TdPoly} the rotated coordinate;
	* if the input is a TdPoly, the output is a TdPoly instance, otherwise just a coordinate array
	*/
	function rotate(c, theta) {
		if (c instanceof TdPoly) return c.rotate(theta);
		else return [c[0] * Math.cos(theta) - c[1] * Math.sin(theta), c[0] * Math.sin(theta) + c[1] * Math.cos(theta)];
	}
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/ocad-file.js
var require_ocad_file = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getRgb = require_cmyk_to_rgb();
	var Crs = require_crs();
	/** @typedef {import('./file-header')} OcadHeader */
	/** @typedef {import('./tobject')} TObject */
	/** @typedef {import('./symbol').BaseSymbolDef} Symbol */
	/** @typedef {import('./parameter-string')} ParameterString */
	/** @typedef {import('./parameter-string').ParameterStringValues} ParameterStringValues */
	/**
	* @typedef {Object} Color
	* @property {number} number
	* @property {number[]} cmyk
	* @property {string} name
	* @property {string} rgb
	* @property {number} renderOrder
	* @property {Uint8ClampedArray} rgbArray
	*/
	module.exports = class OcadFile {
		/**
		* @type {OcadHeader}
		*/
		header;
		/**
		* @type {Object.<number, ParameterStringValues[]>}
		*/
		parameterStrings;
		/**
		* @type {TObject[]}
		*/
		objects;
		/**
		* @type {Symbol[]}
		*/
		symbols;
		/**
		* @type {string[]}
		*/
		warnings;
		/**
		* @type {Color[]}
		*/
		colors;
		/**
		* @param {OcadHeader} header
		* @param {Object.<number, ParameterStringValues[]>} parameterStrings
		* @param {import('./object-index').TObject[]} objects
		* @param {Symbol[]} symbols
		* @param {string[]} warnings
		*/
		constructor(header, parameterStrings, objects, symbols, warnings) {
			this.header = header;
			this.parameterStrings = parameterStrings;
			this.objects = objects;
			this.symbols = symbols;
			this.warnings = warnings;
			this.colors = [];
			const colorDefs = parameterStrings[9] || [];
			for (let i = 0; i < colorDefs.length; i++) {
				const colorDef = colorDefs[i];
				const cmyk = [
					colorDef.c || 0,
					colorDef.m || 0,
					colorDef.y || 0,
					colorDef.k || 0
				].map(Number);
				const rgb = getRgb(cmyk);
				/**
				* @type {Color}
				*/
				const color = {
					number: Number(colorDef.n),
					cmyk,
					name: colorDef._first,
					rgb: `rgb(${rgb[0]}, ${rgb[1]}, ${rgb[2]})`,
					renderOrder: i,
					rgbArray: rgb
				};
				this.colors[Number(color.number)] = color;
			}
		}
		getCrs() {
			return new Crs(this.parameterStrings["1039"] ? this.parameterStrings["1039"][0] : {
				x: "0",
				y: "0",
				m: "1",
				_first: "",
				_pairs: []
			});
		}
		getBounds(projection = (v) => v) {
			const bounds = [
				Number.MAX_VALUE,
				Number.MAX_VALUE,
				-Number.MAX_VALUE,
				-Number.MAX_VALUE
			];
			for (const [[x1, y1], [x2, y2]] of this.objects.map((o) => Object.values(o.objIndex.rc).map(projection))) {
				bounds[0] = Math.min(x1, x2, bounds[0]);
				bounds[1] = Math.min(y1, y2, bounds[1]);
				bounds[2] = Math.max(x1, x2, bounds[2]);
				bounds[3] = Math.max(y1, y2, bounds[3]);
			}
			return bounds;
		}
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/index.js
var require_ocad_reader = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var fs = require_browser_external_fs();
	var { Buffer } = require_buffer();
	var FileHeader = require_file_header();
	var SymbolIndex = require_symbol_index();
	var ObjectIndex = require_object_index();
	var StringIndex = require_string_index();
	var BufferReader = require_buffer_reader();
	var InvalidObjectIndexBlockException = require_invalid_object_index_block_exception();
	var OcadFile = require_ocad_file();
	module.exports = readOcad;
	/**
	* @typedef {Object} ReadOcadOptions
	* @property {boolean=} bypassVersionCheck bypass the version check and read the file anyway
	* @property {boolean=} quietWarnings do not print warnings to console
	* @property {boolean=} failOnWarning throw an error if a warning is encountered
	*/
	/**
	* Reads an OCAD file from the given path or `Buffer` object into an `OcadFile` object.
	*
	* @param {string|Buffer} path the path of the OCAD file or a binary buffer of the OCAD file contents
	* @param {ReadOcadOptions?} options
	* @returns {Promise<OcadFile>} a promise that resolves to an `OcadFile` object
	*/
	async function readOcad(path, options = {}) {
		if (Buffer.isBuffer(path)) return parseOcadBuffer(path, options);
		else return parseOcadBuffer(await new Promise((resolve, reject) => fs.readFile(path, (err, buffer) => {
			if (err) reject(err);
			resolve(buffer);
		})), options);
	}
	function parseOcadBuffer(buffer, options) {
		let warnings = [];
		const reader = new BufferReader(buffer);
		const header = new FileHeader(reader);
		if (!header.isValid()) throw new Error(`Not an OCAD file (invalid header ${header.ocadMark} !== 3245)`);
		if (header.version < 10 && !options.bypassVersionCheck) throw new Error(`Unsupport OCAD file version (${header.version}), only >= 10 supported for now.`);
		/**
		* @type {import('./symbol-index').Symbol[]}
		*/
		const symbols = [];
		let symbolIndexOffset = header.symbolIndexBlock;
		while (symbolIndexOffset) {
			reader.push(symbolIndexOffset);
			const symbolIndex = new SymbolIndex(reader, header.version, options);
			reader.pop();
			Array.prototype.push.apply(symbols, symbolIndex.parseSymbols(reader));
			warnings = warnings.concat(symbolIndex.warnings);
			symbolIndexOffset = symbolIndex.nextSymbolIndexBlock;
		}
		const objects = [];
		let objectIndexOffset = header.objectIndexBlock;
		let startIndex = 0;
		while (objectIndexOffset) {
			reader.push(objectIndexOffset);
			try {
				const objectIndex = new ObjectIndex(reader, startIndex, header.version);
				startIndex += 256;
				reader.pop();
				Array.prototype.push.apply(objects, objectIndex.readObjects(reader));
				objectIndexOffset = objectIndex.nextObjectIndexBlock;
			} catch (e) {
				if (e instanceof InvalidObjectIndexBlockException) warnings.push(e.toString());
			}
		}
		/**
		* @type {Object.<number, import('./parameter-string').ParameterStringValues[]>}
		*/
		const parameterStrings = {};
		let stringIndexOffset = header.stringIndexBlock;
		while (stringIndexOffset) {
			reader.push(stringIndexOffset);
			const stringIndex = new StringIndex(reader);
			reader.pop();
			const strings = stringIndex.getStrings(reader);
			for (const recType of Object.keys(strings)) {
				const typeStrings = strings[recType];
				parameterStrings[recType] = (parameterStrings[recType] || []).concat(typeStrings.map((s) => s.values));
			}
			stringIndexOffset = stringIndex.nextStringIndexBlock;
		}
		if (!options.quietWarnings) warnings.forEach((w) => console.warn(w));
		return new OcadFile(header, parameterStrings, objects, symbols, warnings);
	}
}));
//#endregion
//#region node_modules/@turf/helpers/dist/cjs/index.cjs
var require_cjs$3 = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	var earthRadius = 6371008.8;
	var factors = {
		centimeters: earthRadius * 100,
		centimetres: earthRadius * 100,
		degrees: 360 / (2 * Math.PI),
		feet: earthRadius * 3.28084,
		inches: earthRadius * 39.37,
		kilometers: earthRadius / 1e3,
		kilometres: earthRadius / 1e3,
		meters: earthRadius,
		metres: earthRadius,
		miles: earthRadius / 1609.344,
		millimeters: earthRadius * 1e3,
		millimetres: earthRadius * 1e3,
		nauticalmiles: earthRadius / 1852,
		radians: 1,
		yards: earthRadius * 1.0936
	};
	var areaFactors = {
		acres: 247105e-9,
		centimeters: 1e4,
		centimetres: 1e4,
		feet: 10.763910417,
		hectares: 1e-4,
		inches: 1550.003100006,
		kilometers: 1e-6,
		kilometres: 1e-6,
		meters: 1,
		metres: 1,
		miles: 386e-9,
		nauticalmiles: 2.9155334959812285e-7,
		millimeters: 1e6,
		millimetres: 1e6,
		yards: 1.195990046
	};
	function feature(geom, properties, options = {}) {
		const feat = { type: "Feature" };
		if (options.id === 0 || options.id) feat.id = options.id;
		if (options.bbox) feat.bbox = options.bbox;
		feat.properties = properties || {};
		feat.geometry = geom;
		return feat;
	}
	function geometry(type, coordinates, _options = {}) {
		switch (type) {
			case "Point": return point(coordinates).geometry;
			case "LineString": return lineString(coordinates).geometry;
			case "Polygon": return polygon(coordinates).geometry;
			case "MultiPoint": return multiPoint(coordinates).geometry;
			case "MultiLineString": return multiLineString(coordinates).geometry;
			case "MultiPolygon": return multiPolygon(coordinates).geometry;
			default: throw new Error(type + " is invalid");
		}
	}
	function point(coordinates, properties, options = {}) {
		if (!coordinates) throw new Error("coordinates is required");
		if (!Array.isArray(coordinates)) throw new Error("coordinates must be an Array");
		if (coordinates.length < 2) throw new Error("coordinates must be at least 2 numbers long");
		if (!isNumber(coordinates[0]) || !isNumber(coordinates[1])) throw new Error("coordinates must contain numbers");
		return feature({
			type: "Point",
			coordinates
		}, properties, options);
	}
	function points(coordinates, properties, options = {}) {
		return featureCollection(coordinates.map((coords) => {
			return point(coords, properties);
		}), options);
	}
	function polygon(coordinates, properties, options = {}) {
		for (const ring of coordinates) {
			if (ring.length < 4) throw new Error("Each LinearRing of a Polygon must have 4 or more Positions.");
			if (ring[ring.length - 1].length !== ring[0].length) throw new Error("First and last Position are not equivalent.");
			for (let j = 0; j < ring[ring.length - 1].length; j++) if (ring[ring.length - 1][j] !== ring[0][j]) throw new Error("First and last Position are not equivalent.");
		}
		return feature({
			type: "Polygon",
			coordinates
		}, properties, options);
	}
	function polygons(coordinates, properties, options = {}) {
		return featureCollection(coordinates.map((coords) => {
			return polygon(coords, properties);
		}), options);
	}
	function lineString(coordinates, properties, options = {}) {
		if (coordinates.length < 2) throw new Error("coordinates must be an array of two or more positions");
		return feature({
			type: "LineString",
			coordinates
		}, properties, options);
	}
	function lineStrings(coordinates, properties, options = {}) {
		return featureCollection(coordinates.map((coords) => {
			return lineString(coords, properties);
		}), options);
	}
	function featureCollection(features, options = {}) {
		const fc = { type: "FeatureCollection" };
		if (options.id) fc.id = options.id;
		if (options.bbox) fc.bbox = options.bbox;
		fc.features = features;
		return fc;
	}
	function multiLineString(coordinates, properties, options = {}) {
		return feature({
			type: "MultiLineString",
			coordinates
		}, properties, options);
	}
	function multiPoint(coordinates, properties, options = {}) {
		return feature({
			type: "MultiPoint",
			coordinates
		}, properties, options);
	}
	function multiPolygon(coordinates, properties, options = {}) {
		return feature({
			type: "MultiPolygon",
			coordinates
		}, properties, options);
	}
	function geometryCollection(geometries, properties, options = {}) {
		return feature({
			type: "GeometryCollection",
			geometries
		}, properties, options);
	}
	function round(num, precision = 0) {
		if (precision && !(precision >= 0)) throw new Error("precision must be a positive number");
		const multiplier = Math.pow(10, precision || 0);
		return Math.round(num * multiplier) / multiplier;
	}
	function radiansToLength(radians, units = "kilometers") {
		const factor = factors[units];
		if (!factor) throw new Error(units + " units is invalid");
		return radians * factor;
	}
	function lengthToRadians(distance, units = "kilometers") {
		const factor = factors[units];
		if (!factor) throw new Error(units + " units is invalid");
		return distance / factor;
	}
	function lengthToDegrees(distance, units) {
		return radiansToDegrees(lengthToRadians(distance, units));
	}
	function bearingToAzimuth(bearing) {
		let angle = bearing % 360;
		if (angle < 0) angle += 360;
		return angle;
	}
	function azimuthToBearing(angle) {
		angle = angle % 360;
		if (angle > 180) return angle - 360;
		else if (angle < -180) return angle + 360;
		return angle;
	}
	function radiansToDegrees(radians) {
		return radians % (2 * Math.PI) * 180 / Math.PI;
	}
	function degreesToRadians(degrees) {
		return degrees % 360 * Math.PI / 180;
	}
	function convertLength(length, originalUnit = "kilometers", finalUnit = "kilometers") {
		if (!(length >= 0)) throw new Error("length must be a positive number");
		return radiansToLength(lengthToRadians(length, originalUnit), finalUnit);
	}
	function convertArea(area, originalUnit = "meters", finalUnit = "kilometers") {
		if (!(area >= 0)) throw new Error("area must be a positive number");
		const startFactor = areaFactors[originalUnit];
		if (!startFactor) throw new Error("invalid original units");
		const finalFactor = areaFactors[finalUnit];
		if (!finalFactor) throw new Error("invalid final units");
		return area / startFactor * finalFactor;
	}
	function isNumber(num) {
		return !isNaN(num) && num !== null && !Array.isArray(num);
	}
	function isObject(input) {
		return input !== null && typeof input === "object" && !Array.isArray(input);
	}
	function validateBBox(bbox) {
		if (!bbox) throw new Error("bbox is required");
		if (!Array.isArray(bbox)) throw new Error("bbox must be an Array");
		if (bbox.length !== 4 && bbox.length !== 6) throw new Error("bbox must be an Array of 4 or 6 numbers");
		bbox.forEach((num) => {
			if (!isNumber(num)) throw new Error("bbox must only contain numbers");
		});
	}
	function validateId(id) {
		if (!id) throw new Error("id is required");
		if (["string", "number"].indexOf(typeof id) === -1) throw new Error("id must be a number or a string");
	}
	exports.areaFactors = areaFactors;
	exports.azimuthToBearing = azimuthToBearing;
	exports.bearingToAzimuth = bearingToAzimuth;
	exports.convertArea = convertArea;
	exports.convertLength = convertLength;
	exports.degreesToRadians = degreesToRadians;
	exports.earthRadius = earthRadius;
	exports.factors = factors;
	exports.feature = feature;
	exports.featureCollection = featureCollection;
	exports.geometry = geometry;
	exports.geometryCollection = geometryCollection;
	exports.isNumber = isNumber;
	exports.isObject = isObject;
	exports.lengthToDegrees = lengthToDegrees;
	exports.lengthToRadians = lengthToRadians;
	exports.lineString = lineString;
	exports.lineStrings = lineStrings;
	exports.multiLineString = multiLineString;
	exports.multiPoint = multiPoint;
	exports.multiPolygon = multiPolygon;
	exports.point = point;
	exports.points = points;
	exports.polygon = polygon;
	exports.polygons = polygons;
	exports.radiansToDegrees = radiansToDegrees;
	exports.radiansToLength = radiansToLength;
	exports.round = round;
	exports.validateBBox = validateBBox;
	exports.validateId = validateId;
}));
//#endregion
//#region node_modules/@turf/meta/dist/cjs/index.cjs
var require_cjs$2 = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	var _helpers = require_cjs$3();
	function coordEach(geojson, callback, excludeWrapCoord) {
		if (geojson === null) return;
		var j, k, l, geometry, stopG, coords, geometryMaybeCollection, wrapShrink = 0, coordIndex = 0, isGeometryCollection, type = geojson.type, isFeatureCollection = type === "FeatureCollection", isFeature = type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
		for (var featureIndex = 0; featureIndex < stop; featureIndex++) {
			geometryMaybeCollection = isFeatureCollection ? geojson.features[featureIndex].geometry : isFeature ? geojson.geometry : geojson;
			isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
			stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
			for (var geomIndex = 0; geomIndex < stopG; geomIndex++) {
				var multiFeatureIndex = 0;
				var geometryIndex = 0;
				geometry = isGeometryCollection ? geometryMaybeCollection.geometries[geomIndex] : geometryMaybeCollection;
				if (geometry === null) continue;
				coords = geometry.coordinates;
				var geomType = geometry.type;
				wrapShrink = excludeWrapCoord && (geomType === "Polygon" || geomType === "MultiPolygon") ? 1 : 0;
				switch (geomType) {
					case null: break;
					case "Point":
						if (callback(coords, coordIndex, featureIndex, multiFeatureIndex, geometryIndex) === false) return false;
						coordIndex++;
						multiFeatureIndex++;
						break;
					case "LineString":
					case "MultiPoint":
						for (j = 0; j < coords.length; j++) {
							if (callback(coords[j], coordIndex, featureIndex, multiFeatureIndex, geometryIndex) === false) return false;
							coordIndex++;
							if (geomType === "MultiPoint") multiFeatureIndex++;
						}
						if (geomType === "LineString") multiFeatureIndex++;
						break;
					case "Polygon":
					case "MultiLineString":
						for (j = 0; j < coords.length; j++) {
							for (k = 0; k < coords[j].length - wrapShrink; k++) {
								if (callback(coords[j][k], coordIndex, featureIndex, multiFeatureIndex, geometryIndex) === false) return false;
								coordIndex++;
							}
							if (geomType === "MultiLineString") multiFeatureIndex++;
							if (geomType === "Polygon") geometryIndex++;
						}
						if (geomType === "Polygon") multiFeatureIndex++;
						break;
					case "MultiPolygon":
						for (j = 0; j < coords.length; j++) {
							geometryIndex = 0;
							for (k = 0; k < coords[j].length; k++) {
								for (l = 0; l < coords[j][k].length - wrapShrink; l++) {
									if (callback(coords[j][k][l], coordIndex, featureIndex, multiFeatureIndex, geometryIndex) === false) return false;
									coordIndex++;
								}
								geometryIndex++;
							}
							multiFeatureIndex++;
						}
						break;
					case "GeometryCollection":
						for (j = 0; j < geometry.geometries.length; j++) if (coordEach(geometry.geometries[j], callback, excludeWrapCoord) === false) return false;
						break;
					default: throw new Error("Unknown Geometry Type");
				}
			}
		}
	}
	function coordReduce(geojson, callback, initialValue, excludeWrapCoord) {
		var previousValue = initialValue;
		coordEach(geojson, function(currentCoord, coordIndex, featureIndex, multiFeatureIndex, geometryIndex) {
			if (coordIndex === 0 && initialValue === void 0) previousValue = currentCoord;
			else previousValue = callback(previousValue, currentCoord, coordIndex, featureIndex, multiFeatureIndex, geometryIndex);
		}, excludeWrapCoord);
		return previousValue;
	}
	function propEach(geojson, callback) {
		var i;
		switch (geojson.type) {
			case "FeatureCollection":
				for (i = 0; i < geojson.features.length; i++) if (callback(geojson.features[i].properties, i) === false) break;
				break;
			case "Feature":
				callback(geojson.properties, 0);
				break;
		}
	}
	function propReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		propEach(geojson, function(currentProperties, featureIndex) {
			if (featureIndex === 0 && initialValue === void 0) previousValue = currentProperties;
			else previousValue = callback(previousValue, currentProperties, featureIndex);
		});
		return previousValue;
	}
	function featureEach(geojson, callback) {
		if (geojson.type === "Feature") callback(geojson, 0);
		else if (geojson.type === "FeatureCollection") {
			for (var i = 0; i < geojson.features.length; i++) if (callback(geojson.features[i], i) === false) break;
		}
	}
	function featureReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		featureEach(geojson, function(currentFeature, featureIndex) {
			if (featureIndex === 0 && initialValue === void 0) previousValue = currentFeature;
			else previousValue = callback(previousValue, currentFeature, featureIndex);
		});
		return previousValue;
	}
	function coordAll(geojson) {
		var coords = [];
		coordEach(geojson, function(coord) {
			coords.push(coord);
		});
		return coords;
	}
	function geomEach(geojson, callback) {
		var i, j, g, geometry, stopG, geometryMaybeCollection, isGeometryCollection, featureProperties, featureBBox, featureId, featureIndex = 0, isFeatureCollection = geojson.type === "FeatureCollection", isFeature = geojson.type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
		for (i = 0; i < stop; i++) {
			geometryMaybeCollection = isFeatureCollection ? geojson.features[i].geometry : isFeature ? geojson.geometry : geojson;
			featureProperties = isFeatureCollection ? geojson.features[i].properties : isFeature ? geojson.properties : {};
			featureBBox = isFeatureCollection ? geojson.features[i].bbox : isFeature ? geojson.bbox : void 0;
			featureId = isFeatureCollection ? geojson.features[i].id : isFeature ? geojson.id : void 0;
			isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
			stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
			for (g = 0; g < stopG; g++) {
				geometry = isGeometryCollection ? geometryMaybeCollection.geometries[g] : geometryMaybeCollection;
				if (geometry === null) {
					if (callback(null, featureIndex, featureProperties, featureBBox, featureId) === false) return false;
					continue;
				}
				switch (geometry.type) {
					case "Point":
					case "LineString":
					case "MultiPoint":
					case "Polygon":
					case "MultiLineString":
					case "MultiPolygon":
						if (callback(geometry, featureIndex, featureProperties, featureBBox, featureId) === false) return false;
						break;
					case "GeometryCollection":
						for (j = 0; j < geometry.geometries.length; j++) if (callback(geometry.geometries[j], featureIndex, featureProperties, featureBBox, featureId) === false) return false;
						break;
					default: throw new Error("Unknown Geometry Type");
				}
			}
			featureIndex++;
		}
	}
	function geomReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		geomEach(geojson, function(currentGeometry, featureIndex, featureProperties, featureBBox, featureId) {
			if (featureIndex === 0 && initialValue === void 0) previousValue = currentGeometry;
			else previousValue = callback(previousValue, currentGeometry, featureIndex, featureProperties, featureBBox, featureId);
		});
		return previousValue;
	}
	function flattenEach(geojson, callback) {
		geomEach(geojson, function(geometry, featureIndex, properties, bbox, id) {
			var type = geometry === null ? null : geometry.type;
			switch (type) {
				case null:
				case "Point":
				case "LineString":
				case "Polygon":
					if (callback(_helpers.feature.call(void 0, geometry, properties, {
						bbox,
						id
					}), featureIndex, 0) === false) return false;
					return;
			}
			var geomType;
			switch (type) {
				case "MultiPoint":
					geomType = "Point";
					break;
				case "MultiLineString":
					geomType = "LineString";
					break;
				case "MultiPolygon":
					geomType = "Polygon";
					break;
			}
			for (var multiFeatureIndex = 0; multiFeatureIndex < geometry.coordinates.length; multiFeatureIndex++) {
				var coordinate = geometry.coordinates[multiFeatureIndex];
				var geom = {
					type: geomType,
					coordinates: coordinate
				};
				if (callback(_helpers.feature.call(void 0, geom, properties), featureIndex, multiFeatureIndex) === false) return false;
			}
		});
	}
	function flattenReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		flattenEach(geojson, function(currentFeature, featureIndex, multiFeatureIndex) {
			if (featureIndex === 0 && multiFeatureIndex === 0 && initialValue === void 0) previousValue = currentFeature;
			else previousValue = callback(previousValue, currentFeature, featureIndex, multiFeatureIndex);
		});
		return previousValue;
	}
	function segmentEach(geojson, callback) {
		flattenEach(geojson, function(feature2, featureIndex, multiFeatureIndex) {
			var segmentIndex = 0;
			if (!feature2.geometry) return;
			var type = feature2.geometry.type;
			if (type === "Point" || type === "MultiPoint") return;
			var previousCoords;
			var previousFeatureIndex = 0;
			var previousMultiIndex = 0;
			var prevGeomIndex = 0;
			if (coordEach(feature2, function(currentCoord, coordIndex, featureIndexCoord, multiPartIndexCoord, geometryIndex) {
				if (previousCoords === void 0 || featureIndex > previousFeatureIndex || multiPartIndexCoord > previousMultiIndex || geometryIndex > prevGeomIndex) {
					previousCoords = currentCoord;
					previousFeatureIndex = featureIndex;
					previousMultiIndex = multiPartIndexCoord;
					prevGeomIndex = geometryIndex;
					segmentIndex = 0;
					return;
				}
				if (callback(_helpers.lineString.call(void 0, [previousCoords, currentCoord], feature2.properties), featureIndex, multiFeatureIndex, geometryIndex, segmentIndex) === false) return false;
				segmentIndex++;
				previousCoords = currentCoord;
			}) === false) return false;
		});
	}
	function segmentReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		var started = false;
		segmentEach(geojson, function(currentSegment, featureIndex, multiFeatureIndex, geometryIndex, segmentIndex) {
			if (started === false && initialValue === void 0) previousValue = currentSegment;
			else previousValue = callback(previousValue, currentSegment, featureIndex, multiFeatureIndex, geometryIndex, segmentIndex);
			started = true;
		});
		return previousValue;
	}
	function lineEach(geojson, callback) {
		if (!geojson) throw new Error("geojson is required");
		flattenEach(geojson, function(feature2, featureIndex, multiFeatureIndex) {
			if (feature2.geometry === null) return;
			var type = feature2.geometry.type;
			var coords = feature2.geometry.coordinates;
			switch (type) {
				case "LineString":
					if (callback(feature2, featureIndex, multiFeatureIndex, 0, 0) === false) return false;
					break;
				case "Polygon":
					for (var geometryIndex = 0; geometryIndex < coords.length; geometryIndex++) if (callback(_helpers.lineString.call(void 0, coords[geometryIndex], feature2.properties), featureIndex, multiFeatureIndex, geometryIndex) === false) return false;
					break;
			}
		});
	}
	function lineReduce(geojson, callback, initialValue) {
		var previousValue = initialValue;
		lineEach(geojson, function(currentLine, featureIndex, multiFeatureIndex, geometryIndex) {
			if (featureIndex === 0 && initialValue === void 0) previousValue = currentLine;
			else previousValue = callback(previousValue, currentLine, featureIndex, multiFeatureIndex, geometryIndex);
		});
		return previousValue;
	}
	function findSegment(geojson, options) {
		options = options || {};
		if (!_helpers.isObject.call(void 0, options)) throw new Error("options is invalid");
		var featureIndex = options.featureIndex || 0;
		var multiFeatureIndex = options.multiFeatureIndex || 0;
		var geometryIndex = options.geometryIndex || 0;
		var segmentIndex = options.segmentIndex || 0;
		var properties = options.properties;
		var geometry;
		switch (geojson.type) {
			case "FeatureCollection":
				if (featureIndex < 0) featureIndex = geojson.features.length + featureIndex;
				properties = properties || geojson.features[featureIndex].properties;
				geometry = geojson.features[featureIndex].geometry;
				break;
			case "Feature":
				properties = properties || geojson.properties;
				geometry = geojson.geometry;
				break;
			case "Point":
			case "MultiPoint": return null;
			case "LineString":
			case "Polygon":
			case "MultiLineString":
			case "MultiPolygon":
				geometry = geojson;
				break;
			default: throw new Error("geojson is invalid");
		}
		if (geometry === null) return null;
		var coords = geometry.coordinates;
		switch (geometry.type) {
			case "Point":
			case "MultiPoint": return null;
			case "LineString":
				if (segmentIndex < 0) segmentIndex = coords.length + segmentIndex - 1;
				return _helpers.lineString.call(void 0, [coords[segmentIndex], coords[segmentIndex + 1]], properties, options);
			case "Polygon":
				if (geometryIndex < 0) geometryIndex = coords.length + geometryIndex;
				if (segmentIndex < 0) segmentIndex = coords[geometryIndex].length + segmentIndex - 1;
				return _helpers.lineString.call(void 0, [coords[geometryIndex][segmentIndex], coords[geometryIndex][segmentIndex + 1]], properties, options);
			case "MultiLineString":
				if (multiFeatureIndex < 0) multiFeatureIndex = coords.length + multiFeatureIndex;
				if (segmentIndex < 0) segmentIndex = coords[multiFeatureIndex].length + segmentIndex - 1;
				return _helpers.lineString.call(void 0, [coords[multiFeatureIndex][segmentIndex], coords[multiFeatureIndex][segmentIndex + 1]], properties, options);
			case "MultiPolygon":
				if (multiFeatureIndex < 0) multiFeatureIndex = coords.length + multiFeatureIndex;
				if (geometryIndex < 0) geometryIndex = coords[multiFeatureIndex].length + geometryIndex;
				if (segmentIndex < 0) segmentIndex = coords[multiFeatureIndex][geometryIndex].length - segmentIndex - 1;
				return _helpers.lineString.call(void 0, [coords[multiFeatureIndex][geometryIndex][segmentIndex], coords[multiFeatureIndex][geometryIndex][segmentIndex + 1]], properties, options);
		}
		throw new Error("geojson is invalid");
	}
	function findPoint(geojson, options) {
		options = options || {};
		if (!_helpers.isObject.call(void 0, options)) throw new Error("options is invalid");
		var featureIndex = options.featureIndex || 0;
		var multiFeatureIndex = options.multiFeatureIndex || 0;
		var geometryIndex = options.geometryIndex || 0;
		var coordIndex = options.coordIndex || 0;
		var properties = options.properties;
		var geometry;
		switch (geojson.type) {
			case "FeatureCollection":
				if (featureIndex < 0) featureIndex = geojson.features.length + featureIndex;
				properties = properties || geojson.features[featureIndex].properties;
				geometry = geojson.features[featureIndex].geometry;
				break;
			case "Feature":
				properties = properties || geojson.properties;
				geometry = geojson.geometry;
				break;
			case "Point":
			case "MultiPoint": return null;
			case "LineString":
			case "Polygon":
			case "MultiLineString":
			case "MultiPolygon":
				geometry = geojson;
				break;
			default: throw new Error("geojson is invalid");
		}
		if (geometry === null) return null;
		var coords = geometry.coordinates;
		switch (geometry.type) {
			case "Point": return _helpers.point.call(void 0, coords, properties, options);
			case "MultiPoint":
				if (multiFeatureIndex < 0) multiFeatureIndex = coords.length + multiFeatureIndex;
				return _helpers.point.call(void 0, coords[multiFeatureIndex], properties, options);
			case "LineString":
				if (coordIndex < 0) coordIndex = coords.length + coordIndex;
				return _helpers.point.call(void 0, coords[coordIndex], properties, options);
			case "Polygon":
				if (geometryIndex < 0) geometryIndex = coords.length + geometryIndex;
				if (coordIndex < 0) coordIndex = coords[geometryIndex].length + coordIndex;
				return _helpers.point.call(void 0, coords[geometryIndex][coordIndex], properties, options);
			case "MultiLineString":
				if (multiFeatureIndex < 0) multiFeatureIndex = coords.length + multiFeatureIndex;
				if (coordIndex < 0) coordIndex = coords[multiFeatureIndex].length + coordIndex;
				return _helpers.point.call(void 0, coords[multiFeatureIndex][coordIndex], properties, options);
			case "MultiPolygon":
				if (multiFeatureIndex < 0) multiFeatureIndex = coords.length + multiFeatureIndex;
				if (geometryIndex < 0) geometryIndex = coords[multiFeatureIndex].length + geometryIndex;
				if (coordIndex < 0) coordIndex = coords[multiFeatureIndex][geometryIndex].length - coordIndex;
				return _helpers.point.call(void 0, coords[multiFeatureIndex][geometryIndex][coordIndex], properties, options);
		}
		throw new Error("geojson is invalid");
	}
	exports.coordAll = coordAll;
	exports.coordEach = coordEach;
	exports.coordReduce = coordReduce;
	exports.featureEach = featureEach;
	exports.featureReduce = featureReduce;
	exports.findPoint = findPoint;
	exports.findSegment = findSegment;
	exports.flattenEach = flattenEach;
	exports.flattenReduce = flattenReduce;
	exports.geomEach = geomEach;
	exports.geomReduce = geomReduce;
	exports.lineEach = lineEach;
	exports.lineReduce = lineReduce;
	exports.propEach = propEach;
	exports.propReduce = propReduce;
	exports.segmentEach = segmentEach;
	exports.segmentReduce = segmentReduce;
}));
//#endregion
//#region node_modules/bezier-js/lib/utils.js
var require_utils = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function() {
		"use strict";
		var abs = Math.abs, cos = Math.cos, sin = Math.sin, acos = Math.acos, atan2 = Math.atan2, sqrt = Math.sqrt, pow = Math.pow, crt = function(v) {
			return v < 0 ? -pow(-v, 1 / 3) : pow(v, 1 / 3);
		}, pi = Math.PI, tau = 2 * pi, quart = pi / 2, epsilon = 1e-6, nMax = Number.MAX_SAFE_INTEGER || 9007199254740991, nMin = Number.MIN_SAFE_INTEGER || -9007199254740991, ZERO = {
			x: 0,
			y: 0,
			z: 0
		};
		var utils = {
			Tvalues: [
				-.06405689286260563,
				.06405689286260563,
				-.1911188674736163,
				.1911188674736163,
				-.3150426796961634,
				.3150426796961634,
				-.4337935076260451,
				.4337935076260451,
				-.5454214713888396,
				.5454214713888396,
				-.6480936519369755,
				.6480936519369755,
				-.7401241915785544,
				.7401241915785544,
				-.820001985973903,
				.820001985973903,
				-.8864155270044011,
				.8864155270044011,
				-.9382745520027328,
				.9382745520027328,
				-.9747285559713095,
				.9747285559713095,
				-.9951872199970213,
				.9951872199970213
			],
			Cvalues: [
				.12793819534675216,
				.12793819534675216,
				.1258374563468283,
				.1258374563468283,
				.12167047292780339,
				.12167047292780339,
				.1155056680537256,
				.1155056680537256,
				.10744427011596563,
				.10744427011596563,
				.09761865210411388,
				.09761865210411388,
				.08619016153195327,
				.08619016153195327,
				.0733464814110803,
				.0733464814110803,
				.05929858491543678,
				.05929858491543678,
				.04427743881741981,
				.04427743881741981,
				.028531388628933663,
				.028531388628933663,
				.0123412297999872,
				.0123412297999872
			],
			arcfn: function(t, derivativeFn) {
				var d = derivativeFn(t);
				var l = d.x * d.x + d.y * d.y;
				if (typeof d.z !== "undefined") l += d.z * d.z;
				return sqrt(l);
			},
			compute: function(t, points, _3d) {
				if (t === 0) return points[0];
				var order = points.length - 1;
				if (t === 1) return points[order];
				var p = points;
				var mt = 1 - t;
				if (order === 0) return points[0];
				if (order === 1) {
					ret = {
						x: mt * p[0].x + t * p[1].x,
						y: mt * p[0].y + t * p[1].y
					};
					if (_3d) ret.z = mt * p[0].z + t * p[1].z;
					return ret;
				}
				if (order < 4) {
					var mt2 = mt * mt, t2 = t * t, a, b, c, d = 0;
					if (order === 2) {
						p = [
							p[0],
							p[1],
							p[2],
							ZERO
						];
						a = mt2;
						b = mt * t * 2;
						c = t2;
					} else if (order === 3) {
						a = mt2 * mt;
						b = mt2 * t * 3;
						c = mt * t2 * 3;
						d = t * t2;
					}
					var ret = {
						x: a * p[0].x + b * p[1].x + c * p[2].x + d * p[3].x,
						y: a * p[0].y + b * p[1].y + c * p[2].y + d * p[3].y
					};
					if (_3d) ret.z = a * p[0].z + b * p[1].z + c * p[2].z + d * p[3].z;
					return ret;
				}
				var dCpts = JSON.parse(JSON.stringify(points));
				while (dCpts.length > 1) {
					for (var i = 0; i < dCpts.length - 1; i++) {
						dCpts[i] = {
							x: dCpts[i].x + (dCpts[i + 1].x - dCpts[i].x) * t,
							y: dCpts[i].y + (dCpts[i + 1].y - dCpts[i].y) * t
						};
						if (typeof dCpts[i].z !== "undefined") dCpts[i] = dCpts[i].z + (dCpts[i + 1].z - dCpts[i].z) * t;
					}
					dCpts.splice(dCpts.length - 1, 1);
				}
				return dCpts[0];
			},
			computeWithRatios: function(t, points, ratios, _3d) {
				var mt = 1 - t, r = ratios, p = points, d;
				var f1 = r[0], f2 = r[1], f3 = r[2], f4 = r[3];
				f1 *= mt;
				f2 *= t;
				if (p.length === 2) {
					d = f1 + f2;
					return {
						x: (f1 * p[0].x + f2 * p[1].x) / d,
						y: (f1 * p[0].y + f2 * p[1].y) / d,
						z: !_3d ? false : (f1 * p[0].z + f2 * p[1].z) / d
					};
				}
				f1 *= mt;
				f2 *= 2 * mt;
				f3 *= t * t;
				if (p.length === 3) {
					d = f1 + f2 + f3;
					return {
						x: (f1 * p[0].x + f2 * p[1].x + f3 * p[2].x) / d,
						y: (f1 * p[0].y + f2 * p[1].y + f3 * p[2].y) / d,
						z: !_3d ? false : (f1 * p[0].z + f2 * p[1].z + f3 * p[2].z) / d
					};
				}
				f1 *= mt;
				f2 *= 1.5 * mt;
				f3 *= 3 * mt;
				f4 *= t * t * t;
				if (p.length === 4) {
					d = f1 + f2 + f3 + f4;
					return {
						x: (f1 * p[0].x + f2 * p[1].x + f3 * p[2].x + f4 * p[3].x) / d,
						y: (f1 * p[0].y + f2 * p[1].y + f3 * p[2].y + f4 * p[3].y) / d,
						z: !_3d ? false : (f1 * p[0].z + f2 * p[1].z + f3 * p[2].z + f4 * p[3].z) / d
					};
				}
			},
			derive: function(points, _3d) {
				var dpoints = [];
				for (var p = points, d = p.length, c = d - 1; d > 1; d--, c--) {
					var list = [];
					for (var j = 0, dpt; j < c; j++) {
						dpt = {
							x: c * (p[j + 1].x - p[j].x),
							y: c * (p[j + 1].y - p[j].y)
						};
						if (_3d) dpt.z = c * (p[j + 1].z - p[j].z);
						list.push(dpt);
					}
					dpoints.push(list);
					p = list;
				}
				return dpoints;
			},
			between: function(v, m, M) {
				return m <= v && v <= M || utils.approximately(v, m) || utils.approximately(v, M);
			},
			approximately: function(a, b, precision) {
				return abs(a - b) <= (precision || epsilon);
			},
			length: function(derivativeFn) {
				var z = .5, sum = 0, len = utils.Tvalues.length, i, t;
				for (i = 0; i < len; i++) {
					t = z * utils.Tvalues[i] + z;
					sum += utils.Cvalues[i] * utils.arcfn(t, derivativeFn);
				}
				return z * sum;
			},
			map: function(v, ds, de, ts, te) {
				var d1 = de - ds;
				return ts + (te - ts) * ((v - ds) / d1);
			},
			lerp: function(r, v1, v2) {
				var ret = {
					x: v1.x + r * (v2.x - v1.x),
					y: v1.y + r * (v2.y - v1.y)
				};
				if (!!v1.z && !!v2.z) ret.z = v1.z + r * (v2.z - v1.z);
				return ret;
			},
			pointToString: function(p) {
				var s = p.x + "/" + p.y;
				if (typeof p.z !== "undefined") s += "/" + p.z;
				return s;
			},
			pointsToString: function(points) {
				return "[" + points.map(utils.pointToString).join(", ") + "]";
			},
			copy: function(obj) {
				return JSON.parse(JSON.stringify(obj));
			},
			angle: function(o, v1, v2) {
				var dx1 = v1.x - o.x, dy1 = v1.y - o.y, dx2 = v2.x - o.x, dy2 = v2.y - o.y;
				return atan2(dx1 * dy2 - dy1 * dx2, dx1 * dx2 + dy1 * dy2);
			},
			round: function(v, d) {
				var s = "" + v;
				var pos = s.indexOf(".");
				return parseFloat(s.substring(0, pos + 1 + d));
			},
			dist: function(p1, p2) {
				var dx = p1.x - p2.x, dy = p1.y - p2.y;
				return sqrt(dx * dx + dy * dy);
			},
			closest: function(LUT, point) {
				var mdist = pow(2, 63), mpos, d;
				LUT.forEach(function(p, idx) {
					d = utils.dist(point, p);
					if (d < mdist) {
						mdist = d;
						mpos = idx;
					}
				});
				return {
					mdist,
					mpos
				};
			},
			abcratio: function(t, n) {
				if (n !== 2 && n !== 3) return false;
				if (typeof t === "undefined") t = .5;
				else if (t === 0 || t === 1) return t;
				var bottom = pow(t, n) + pow(1 - t, n);
				return abs((bottom - 1) / bottom);
			},
			projectionratio: function(t, n) {
				if (n !== 2 && n !== 3) return false;
				if (typeof t === "undefined") t = .5;
				else if (t === 0 || t === 1) return t;
				var top = pow(1 - t, n);
				return top / (pow(t, n) + top);
			},
			lli8: function(x1, y1, x2, y2, x3, y3, x4, y4) {
				var nx = (x1 * y2 - y1 * x2) * (x3 - x4) - (x1 - x2) * (x3 * y4 - y3 * x4), ny = (x1 * y2 - y1 * x2) * (y3 - y4) - (y1 - y2) * (x3 * y4 - y3 * x4), d = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);
				if (d == 0) return false;
				return {
					x: nx / d,
					y: ny / d
				};
			},
			lli4: function(p1, p2, p3, p4) {
				var x1 = p1.x, y1 = p1.y, x2 = p2.x, y2 = p2.y, x3 = p3.x, y3 = p3.y, x4 = p4.x, y4 = p4.y;
				return utils.lli8(x1, y1, x2, y2, x3, y3, x4, y4);
			},
			lli: function(v1, v2) {
				return utils.lli4(v1, v1.c, v2, v2.c);
			},
			makeline: function(p1, p2) {
				var Bezier = require_bezier();
				var x1 = p1.x, y1 = p1.y, x2 = p2.x, y2 = p2.y, dx = (x2 - x1) / 3, dy = (y2 - y1) / 3;
				return new Bezier(x1, y1, x1 + dx, y1 + dy, x1 + 2 * dx, y1 + 2 * dy, x2, y2);
			},
			findbbox: function(sections) {
				var mx = nMax, my = nMax, MX = nMin, MY = nMin;
				sections.forEach(function(s) {
					var bbox = s.bbox();
					if (mx > bbox.x.min) mx = bbox.x.min;
					if (my > bbox.y.min) my = bbox.y.min;
					if (MX < bbox.x.max) MX = bbox.x.max;
					if (MY < bbox.y.max) MY = bbox.y.max;
				});
				return {
					x: {
						min: mx,
						mid: (mx + MX) / 2,
						max: MX,
						size: MX - mx
					},
					y: {
						min: my,
						mid: (my + MY) / 2,
						max: MY,
						size: MY - my
					}
				};
			},
			shapeintersections: function(s1, bbox1, s2, bbox2, curveIntersectionThreshold) {
				if (!utils.bboxoverlap(bbox1, bbox2)) return [];
				var intersections = [];
				var a1 = [
					s1.startcap,
					s1.forward,
					s1.back,
					s1.endcap
				];
				var a2 = [
					s2.startcap,
					s2.forward,
					s2.back,
					s2.endcap
				];
				a1.forEach(function(l1) {
					if (l1.virtual) return;
					a2.forEach(function(l2) {
						if (l2.virtual) return;
						var iss = l1.intersects(l2, curveIntersectionThreshold);
						if (iss.length > 0) {
							iss.c1 = l1;
							iss.c2 = l2;
							iss.s1 = s1;
							iss.s2 = s2;
							intersections.push(iss);
						}
					});
				});
				return intersections;
			},
			makeshape: function(forward, back, curveIntersectionThreshold) {
				var bpl = back.points.length;
				var fpl = forward.points.length;
				var start = utils.makeline(back.points[bpl - 1], forward.points[0]);
				var end = utils.makeline(forward.points[fpl - 1], back.points[0]);
				var shape = {
					startcap: start,
					forward,
					back,
					endcap: end,
					bbox: utils.findbbox([
						start,
						forward,
						back,
						end
					])
				};
				var self = utils;
				shape.intersections = function(s2) {
					return self.shapeintersections(shape, shape.bbox, s2, s2.bbox, curveIntersectionThreshold);
				};
				return shape;
			},
			getminmax: function(curve, d, list) {
				if (!list) return {
					min: 0,
					max: 0
				};
				var min = nMax, max = nMin, t, c;
				if (list.indexOf(0) === -1) list = [0].concat(list);
				if (list.indexOf(1) === -1) list.push(1);
				for (var i = 0, len = list.length; i < len; i++) {
					t = list[i];
					c = curve.get(t);
					if (c[d] < min) min = c[d];
					if (c[d] > max) max = c[d];
				}
				return {
					min,
					mid: (min + max) / 2,
					max,
					size: max - min
				};
			},
			align: function(points, line) {
				var tx = line.p1.x, ty = line.p1.y, a = -atan2(line.p2.y - ty, line.p2.x - tx), d = function(v) {
					return {
						x: (v.x - tx) * cos(a) - (v.y - ty) * sin(a),
						y: (v.x - tx) * sin(a) + (v.y - ty) * cos(a)
					};
				};
				return points.map(d);
			},
			roots: function(points, line) {
				line = line || {
					p1: {
						x: 0,
						y: 0
					},
					p2: {
						x: 1,
						y: 0
					}
				};
				var order = points.length - 1;
				var p = utils.align(points, line);
				var reduce = function(t) {
					return 0 <= t && t <= 1;
				};
				if (order === 2) {
					var a = p[0].y, b = p[1].y, c = p[2].y, d = a - 2 * b + c;
					if (d !== 0) {
						var m1 = -sqrt(b * b - a * c), m2 = -a + b, v1 = -(m1 + m2) / d, v2 = -(-m1 + m2) / d;
						return [v1, v2].filter(reduce);
					} else if (b !== c && d === 0) return [(2 * b - c) / (2 * b - 2 * c)].filter(reduce);
					return [];
				}
				var pa = p[0].y, pb = p[1].y, pc = p[2].y, pd = p[3].y, d = -pa + 3 * pb - 3 * pc + pd, a = 3 * pa - 6 * pb + 3 * pc, b = -3 * pa + 3 * pb, c = pa;
				if (utils.approximately(d, 0)) {
					if (utils.approximately(a, 0)) {
						if (utils.approximately(b, 0)) return [];
						return [-c / b].filter(reduce);
					}
					var q = sqrt(b * b - 4 * a * c), a2 = 2 * a;
					return [(q - b) / a2, (-b - q) / a2].filter(reduce);
				}
				a /= d;
				b /= d;
				c /= d;
				var p = (3 * b - a * a) / 3, p3 = p / 3, q = (2 * a * a * a - 9 * a * b + 27 * c) / 27, q2 = q / 2, discriminant = q2 * q2 + p3 * p3 * p3, u1, v1, x1, x2, x3;
				if (discriminant < 0) {
					var mp3 = -p / 3, r = sqrt(mp3 * mp3 * mp3), t = -q / (2 * r), phi = acos(t < -1 ? -1 : t > 1 ? 1 : t), t1 = 2 * crt(r);
					x1 = t1 * cos(phi / 3) - a / 3;
					x2 = t1 * cos((phi + tau) / 3) - a / 3;
					x3 = t1 * cos((phi + 2 * tau) / 3) - a / 3;
					return [
						x1,
						x2,
						x3
					].filter(reduce);
				} else if (discriminant === 0) {
					u1 = q2 < 0 ? crt(-q2) : -crt(q2);
					x1 = 2 * u1 - a / 3;
					x2 = -u1 - a / 3;
					return [x1, x2].filter(reduce);
				} else {
					var sd = sqrt(discriminant);
					u1 = crt(-q2 + sd);
					v1 = crt(q2 + sd);
					return [u1 - v1 - a / 3].filter(reduce);
				}
			},
			droots: function(p) {
				if (p.length === 3) {
					var a = p[0], b = p[1], c = p[2], d = a - 2 * b + c;
					if (d !== 0) {
						var m1 = -sqrt(b * b - a * c), m2 = -a + b;
						return [-(m1 + m2) / d, -(-m1 + m2) / d];
					} else if (b !== c && d === 0) return [(2 * b - c) / (2 * (b - c))];
					return [];
				}
				if (p.length === 2) {
					var a = p[0], b = p[1];
					if (a !== b) return [a / (a - b)];
					return [];
				}
			},
			curvature: function(t, points, _3d, kOnly) {
				var dpoints = utils.derive(points);
				var d1 = dpoints[0];
				var d2 = dpoints[1];
				var num, dnm, adk, dk, k = 0, r = 0;
				var d = utils.compute(t, d1);
				var dd = utils.compute(t, d2);
				var qdsum = d.x * d.x + d.y * d.y;
				if (_3d) {
					num = sqrt(pow(d.y * dd.z - dd.y * d.z, 2) + pow(d.z * dd.x - dd.z * d.x, 2) + pow(d.x * dd.y - dd.x * d.y, 2));
					dnm = pow(qdsum + d.z * d.z, 3 / 2);
				} else {
					num = d.x * dd.y - d.y * dd.x;
					dnm = pow(qdsum, 3 / 2);
				}
				if (num === 0 || dnm === 0) return {
					k: 0,
					r: 0
				};
				k = num / dnm;
				r = dnm / num;
				if (!kOnly) {
					var pk = utils.curvature(t - .001, points, _3d, true).k;
					var nk = utils.curvature(t + .001, points, _3d, true).k;
					dk = (nk - k + (k - pk)) / 2;
					adk = (abs(nk - k) + abs(k - pk)) / 2;
				}
				return {
					k,
					r,
					dk,
					adk
				};
			},
			inflections: function(points) {
				if (points.length < 4) return [];
				var p = utils.align(points, {
					p1: points[0],
					p2: points.slice(-1)[0]
				}), a = p[2].x * p[1].y, b = p[3].x * p[1].y, c = p[1].x * p[2].y, d = p[3].x * p[2].y, v1 = 18 * (-3 * a + 2 * b + 3 * c - d), v2 = 18 * (3 * a - b - 3 * c), v3 = 18 * (c - a);
				if (utils.approximately(v1, 0)) {
					if (!utils.approximately(v2, 0)) {
						var t = -v3 / v2;
						if (0 <= t && t <= 1) return [t];
					}
					return [];
				}
				var trm = v2 * v2 - 4 * v1 * v3, sq = Math.sqrt(trm), d = 2 * v1;
				if (utils.approximately(d, 0)) return [];
				return [(sq - v2) / d, -(v2 + sq) / d].filter(function(r) {
					return 0 <= r && r <= 1;
				});
			},
			bboxoverlap: function(b1, b2) {
				var dims = ["x", "y"], len = dims.length, i, dim, l, t, d;
				for (i = 0; i < len; i++) {
					dim = dims[i];
					l = b1[dim].mid;
					t = b2[dim].mid;
					d = (b1[dim].size + b2[dim].size) / 2;
					if (abs(l - t) >= d) return false;
				}
				return true;
			},
			expandbox: function(bbox, _bbox) {
				if (_bbox.x.min < bbox.x.min) bbox.x.min = _bbox.x.min;
				if (_bbox.y.min < bbox.y.min) bbox.y.min = _bbox.y.min;
				if (_bbox.z && _bbox.z.min < bbox.z.min) bbox.z.min = _bbox.z.min;
				if (_bbox.x.max > bbox.x.max) bbox.x.max = _bbox.x.max;
				if (_bbox.y.max > bbox.y.max) bbox.y.max = _bbox.y.max;
				if (_bbox.z && _bbox.z.max > bbox.z.max) bbox.z.max = _bbox.z.max;
				bbox.x.mid = (bbox.x.min + bbox.x.max) / 2;
				bbox.y.mid = (bbox.y.min + bbox.y.max) / 2;
				if (bbox.z) bbox.z.mid = (bbox.z.min + bbox.z.max) / 2;
				bbox.x.size = bbox.x.max - bbox.x.min;
				bbox.y.size = bbox.y.max - bbox.y.min;
				if (bbox.z) bbox.z.size = bbox.z.max - bbox.z.min;
			},
			pairiteration: function(c1, c2, curveIntersectionThreshold) {
				var c1b = c1.bbox(), c2b = c2.bbox(), r = 1e5, threshold = curveIntersectionThreshold || .5;
				if (c1b.x.size + c1b.y.size < threshold && c2b.x.size + c2b.y.size < threshold) return [(r * (c1._t1 + c1._t2) / 2 | 0) / r + "/" + (r * (c2._t1 + c2._t2) / 2 | 0) / r];
				var cc1 = c1.split(.5), cc2 = c2.split(.5), pairs = [
					{
						left: cc1.left,
						right: cc2.left
					},
					{
						left: cc1.left,
						right: cc2.right
					},
					{
						left: cc1.right,
						right: cc2.right
					},
					{
						left: cc1.right,
						right: cc2.left
					}
				];
				pairs = pairs.filter(function(pair) {
					return utils.bboxoverlap(pair.left.bbox(), pair.right.bbox());
				});
				var results = [];
				if (pairs.length === 0) return results;
				pairs.forEach(function(pair) {
					results = results.concat(utils.pairiteration(pair.left, pair.right, threshold));
				});
				results = results.filter(function(v, i) {
					return results.indexOf(v) === i;
				});
				return results;
			},
			getccenter: function(p1, p2, p3) {
				var dx1 = p2.x - p1.x, dy1 = p2.y - p1.y, dx2 = p3.x - p2.x, dy2 = p3.y - p2.y;
				var dx1p = dx1 * cos(quart) - dy1 * sin(quart), dy1p = dx1 * sin(quart) + dy1 * cos(quart), dx2p = dx2 * cos(quart) - dy2 * sin(quart), dy2p = dx2 * sin(quart) + dy2 * cos(quart);
				var mx1 = (p1.x + p2.x) / 2, my1 = (p1.y + p2.y) / 2, mx2 = (p2.x + p3.x) / 2, my2 = (p2.y + p3.y) / 2;
				var mx1n = mx1 + dx1p, my1n = my1 + dy1p, mx2n = mx2 + dx2p, my2n = my2 + dy2p;
				var arc = utils.lli8(mx1, my1, mx1n, my1n, mx2, my2, mx2n, my2n), r = utils.dist(arc, p1), s = atan2(p1.y - arc.y, p1.x - arc.x), m = atan2(p2.y - arc.y, p2.x - arc.x), e = atan2(p3.y - arc.y, p3.x - arc.x), _;
				if (s < e) {
					if (s > m || m > e) s += tau;
					if (s > e) {
						_ = e;
						e = s;
						s = _;
					}
				} else if (e < m && m < s) {
					_ = e;
					e = s;
					s = _;
				} else e += tau;
				arc.s = s;
				arc.e = e;
				arc.r = r;
				return arc;
			},
			numberSort: function(a, b) {
				return a - b;
			}
		};
		module.exports = utils;
	})();
}));
//#endregion
//#region node_modules/bezier-js/lib/poly-bezier.js
var require_poly_bezier = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function() {
		"use strict";
		var utils = require_utils();
		/**
		* Poly Bezier
		* @param {[type]} curves [description]
		*/
		var PolyBezier = function(curves) {
			this.curves = [];
			this._3d = false;
			if (!!curves) {
				this.curves = curves;
				this._3d = this.curves[0]._3d;
			}
		};
		PolyBezier.prototype = {
			valueOf: function() {
				return this.toString();
			},
			toString: function() {
				return "[" + this.curves.map(function(curve) {
					return utils.pointsToString(curve.points);
				}).join(", ") + "]";
			},
			addCurve: function(curve) {
				this.curves.push(curve);
				this._3d = this._3d || curve._3d;
			},
			length: function() {
				return this.curves.map(function(v) {
					return v.length();
				}).reduce(function(a, b) {
					return a + b;
				});
			},
			curve: function(idx) {
				return this.curves[idx];
			},
			bbox: function() {
				var c = this.curves;
				var bbox = c[0].bbox();
				for (var i = 1; i < c.length; i++) utils.expandbox(bbox, c[i].bbox());
				return bbox;
			},
			offset: function(d) {
				var offset = [];
				this.curves.forEach(function(v) {
					offset = offset.concat(v.offset(d));
				});
				return new PolyBezier(offset);
			}
		};
		module.exports = PolyBezier;
	})();
}));
//#endregion
//#region node_modules/bezier-js/lib/normalise-svg.js
var require_normalise_svg = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Normalise an SVG path to absolute coordinates
	* and full commands, rather than relative coordinates
	* and/or shortcut commands.
	*/
	function normalizePath(d) {
		d = d.replace(/,/g, " ").replace(/-/g, " - ").replace(/-\s+/g, "-").replace(/([a-zA-Z])/g, " $1 ");
		var instructions = d.replace(/([a-zA-Z])\s?/g, "|$1").split("|"), instructionLength = instructions.length, i, instruction, op, lop, args = [], alen, a, sx = 0, sy = 0, x = 0, y = 0, cx = 0, cy = 0, cx2 = 0, cy2 = 0, normalized = "";
		for (i = 1; i < instructionLength; i++) {
			instruction = instructions[i];
			op = instruction.substring(0, 1);
			lop = op.toLowerCase();
			args = instruction.replace(op, "").trim().split(" ");
			args = args.filter(function(v) {
				return v !== "";
			}).map(parseFloat);
			alen = args.length;
			if (lop === "m") {
				normalized += "M ";
				if (op === "m") {
					x += args[0];
					y += args[1];
				} else {
					x = args[0];
					y = args[1];
				}
				sx = x;
				sy = y;
				normalized += x + " " + y + " ";
				if (alen > 2) for (a = 0; a < alen; a += 2) {
					if (op === "m") {
						x += args[a];
						y += args[a + 1];
					} else {
						x = args[a];
						y = args[a + 1];
					}
					normalized += [
						"L",
						x,
						y,
						""
					].join(" ");
				}
			} else if (lop === "l") for (a = 0; a < alen; a += 2) {
				if (op === "l") {
					x += args[a];
					y += args[a + 1];
				} else {
					x = args[a];
					y = args[a + 1];
				}
				normalized += [
					"L",
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "h") for (a = 0; a < alen; a++) {
				if (op === "h") x += args[a];
				else x = args[a];
				normalized += [
					"L",
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "v") for (a = 0; a < alen; a++) {
				if (op === "v") y += args[a];
				else y = args[a];
				normalized += [
					"L",
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "q") for (a = 0; a < alen; a += 4) {
				if (op === "q") {
					cx = x + args[a];
					cy = y + args[a + 1];
					x += args[a + 2];
					y += args[a + 3];
				} else {
					cx = args[a];
					cy = args[a + 1];
					x = args[a + 2];
					y = args[a + 3];
				}
				normalized += [
					"Q",
					cx,
					cy,
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "t") for (a = 0; a < alen; a += 2) {
				cx = x + (x - cx);
				cy = y + (y - cy);
				if (op === "t") {
					x += args[a];
					y += args[a + 1];
				} else {
					x = args[a];
					y = args[a + 1];
				}
				normalized += [
					"Q",
					cx,
					cy,
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "c") for (a = 0; a < alen; a += 6) {
				if (op === "c") {
					cx = x + args[a];
					cy = y + args[a + 1];
					cx2 = x + args[a + 2];
					cy2 = y + args[a + 3];
					x += args[a + 4];
					y += args[a + 5];
				} else {
					cx = args[a];
					cy = args[a + 1];
					cx2 = args[a + 2];
					cy2 = args[a + 3];
					x = args[a + 4];
					y = args[a + 5];
				}
				normalized += [
					"C",
					cx,
					cy,
					cx2,
					cy2,
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "s") for (a = 0; a < alen; a += 4) {
				cx = x + (x - cx2);
				cy = y + (y - cy2);
				if (op === "s") {
					cx2 = x + args[a];
					cy2 = y + args[a + 1];
					x += args[a + 2];
					y += args[a + 3];
				} else {
					cx2 = args[a];
					cy2 = args[a + 1];
					x = args[a + 2];
					y = args[a + 3];
				}
				normalized += [
					"C",
					cx,
					cy,
					cx2,
					cy2,
					x,
					y,
					""
				].join(" ");
			}
			else if (lop === "z") {
				normalized += "Z ";
				x = sx;
				y = sy;
			}
		}
		return normalized.trim();
	}
	module.exports = normalizePath;
}));
//#endregion
//#region node_modules/bezier-js/lib/svg-to-beziers.js
var require_svg_to_beziers = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var normalise = require_normalise_svg();
	var M = {
		x: false,
		y: false
	};
	function makeBezier(Bezier, term, values) {
		if (term === "Z") return;
		if (term === "M") {
			M = {
				x: values[0],
				y: values[1]
			};
			return;
		}
		var cvalues = [
			false,
			M.x,
			M.y
		].concat(values);
		var curve = new (Bezier.bind.apply(Bezier, cvalues))();
		var last = values.slice(-2);
		M = {
			x: last[0],
			y: last[1]
		};
		return curve;
	}
	function convertPath(Bezier, d) {
		var terms = normalise(d).split(" "), term, matcher = /* @__PURE__ */ new RegExp("[MLCQZ]", ""), segment, values, segments = [], ARGS = {
			"C": 6,
			"Q": 4,
			"L": 2,
			"M": 2
		};
		while (terms.length) {
			term = terms.splice(0, 1)[0];
			if (matcher.test(term)) {
				values = terms.splice(0, ARGS[term]).map(parseFloat);
				segment = makeBezier(Bezier, term, values);
				if (segment) segments.push(segment);
			}
		}
		return new Bezier.PolyBezier(segments);
	}
	module.exports = convertPath;
}));
//#endregion
//#region node_modules/bezier-js/lib/bezier.js
var require_bezier = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	A javascript Bezier curve library by Pomax.
	
	Based on http://pomax.github.io/bezierinfo
	
	This code is MIT licensed.
	**/
	(function() {
		"use strict";
		var abs = Math.abs, min = Math.min, max = Math.max, cos = Math.cos, sin = Math.sin, acos = Math.acos, sqrt = Math.sqrt, pi = Math.PI, ZERO = {
			x: 0,
			y: 0,
			z: 0
		};
		var utils = require_utils();
		var PolyBezier = require_poly_bezier();
		/**
		* Bezier curve constructor. The constructor argument can be one of three things:
		*
		* 1. array/4 of {x:..., y:..., z:...}, z optional
		* 2. numerical array/8 ordered x1,y1,x2,y2,x3,y3,x4,y4
		* 3. numerical array/12 ordered x1,y1,z1,x2,y2,z2,x3,y3,z3,x4,y4,z4
		*
		*/
		var Bezier = function(coords) {
			var args = coords && coords.forEach ? coords : [].slice.call(arguments);
			var coordlen = false;
			if (typeof args[0] === "object") {
				coordlen = args.length;
				var newargs = [];
				args.forEach(function(point) {
					[
						"x",
						"y",
						"z"
					].forEach(function(d) {
						if (typeof point[d] !== "undefined") newargs.push(point[d]);
					});
				});
				args = newargs;
			}
			var higher = false;
			var len = args.length;
			if (coordlen) {
				if (coordlen > 4) {
					if (arguments.length !== 1) throw new Error("Only new Bezier(point[]) is accepted for 4th and higher order curves");
					higher = true;
				}
			} else if (len !== 6 && len !== 8 && len !== 9 && len !== 12) {
				if (arguments.length !== 1) throw new Error("Only new Bezier(point[]) is accepted for 4th and higher order curves");
			}
			var _3d = !higher && (len === 9 || len === 12) || coords && coords[0] && typeof coords[0].z !== "undefined";
			this._3d = _3d;
			var points = [];
			for (var idx = 0, step = _3d ? 3 : 2; idx < len; idx += step) {
				var point = {
					x: args[idx],
					y: args[idx + 1]
				};
				if (_3d) point.z = args[idx + 2];
				points.push(point);
			}
			this.order = points.length - 1;
			this.points = points;
			var dims = ["x", "y"];
			if (_3d) dims.push("z");
			this.dims = dims;
			this.dimlen = dims.length;
			(function(curve) {
				var order = curve.order;
				var points = curve.points;
				var a = utils.align(points, {
					p1: points[0],
					p2: points[order]
				});
				for (var i = 0; i < a.length; i++) if (abs(a[i].y) > 1e-4) {
					curve._linear = false;
					return;
				}
				curve._linear = true;
			})(this);
			this._t1 = 0;
			this._t2 = 1;
			this.update();
		};
		var svgToBeziers = require_svg_to_beziers();
		/**
		* turn an svg <path> d attribute into a sequence of Bezier segments.
		*/
		Bezier.SVGtoBeziers = function(d) {
			return svgToBeziers(Bezier, d);
		};
		function getABC(n, S, B, E, t) {
			if (typeof t === "undefined") t = .5;
			var u = utils.projectionratio(t, n), um = 1 - u, C = {
				x: u * S.x + um * E.x,
				y: u * S.y + um * E.y
			}, s = utils.abcratio(t, n);
			return {
				A: {
					x: B.x + (B.x - C.x) / s,
					y: B.y + (B.y - C.y) / s
				},
				B,
				C
			};
		}
		Bezier.quadraticFromPoints = function(p1, p2, p3, t) {
			if (typeof t === "undefined") t = .5;
			if (t === 0) return new Bezier(p2, p2, p3);
			if (t === 1) return new Bezier(p1, p2, p2);
			return new Bezier(p1, getABC(2, p1, p2, p3, t).A, p3);
		};
		Bezier.cubicFromPoints = function(S, B, E, t, d1) {
			if (typeof t === "undefined") t = .5;
			var abc = getABC(3, S, B, E, t);
			if (typeof d1 === "undefined") d1 = utils.dist(B, abc.C);
			var d2 = d1 * (1 - t) / t;
			var selen = utils.dist(S, E), lx = (E.x - S.x) / selen, ly = (E.y - S.y) / selen, bx1 = d1 * lx, by1 = d1 * ly, bx2 = d2 * lx, by2 = d2 * ly;
			var e1 = {
				x: B.x - bx1,
				y: B.y - by1
			}, e2 = {
				x: B.x + bx2,
				y: B.y + by2
			}, A = abc.A, v1 = {
				x: A.x + (e1.x - A.x) / (1 - t),
				y: A.y + (e1.y - A.y) / (1 - t)
			}, v2 = {
				x: A.x + (e2.x - A.x) / t,
				y: A.y + (e2.y - A.y) / t
			};
			return new Bezier(S, {
				x: S.x + (v1.x - S.x) / t,
				y: S.y + (v1.y - S.y) / t
			}, {
				x: E.x + (v2.x - E.x) / (1 - t),
				y: E.y + (v2.y - E.y) / (1 - t)
			}, E);
		};
		var getUtils = function() {
			return utils;
		};
		Bezier.getUtils = getUtils;
		Bezier.PolyBezier = PolyBezier;
		Bezier.prototype = {
			getUtils,
			valueOf: function() {
				return this.toString();
			},
			toString: function() {
				return utils.pointsToString(this.points);
			},
			toSVG: function(relative) {
				if (this._3d) return false;
				var p = this.points, s = [
					"M",
					p[0].x,
					p[0].y,
					this.order === 2 ? "Q" : "C"
				];
				for (var i = 1, last = p.length; i < last; i++) {
					s.push(p[i].x);
					s.push(p[i].y);
				}
				return s.join(" ");
			},
			setRatios: function(ratios) {
				if (ratios.length !== this.points.length) throw new Error("incorrect number of ratio values");
				this.ratios = ratios;
				this._lut = [];
			},
			verify: function() {
				var print = this.coordDigest();
				if (print !== this._print) {
					this._print = print;
					this.update();
				}
			},
			coordDigest: function() {
				return this.points.map(function(c, pos) {
					return "" + pos + c.x + c.y + (c.z ? c.z : 0);
				}).join("");
			},
			update: function(newprint) {
				this._lut = [];
				this.dpoints = utils.derive(this.points, this._3d);
				this.computedirection();
			},
			computedirection: function() {
				var points = this.points;
				var angle = utils.angle(points[0], points[this.order], points[1]);
				this.clockwise = angle > 0;
			},
			length: function() {
				return utils.length(this.derivative.bind(this));
			},
			_lut: [],
			getLUT: function(steps) {
				this.verify();
				steps = steps || 100;
				if (this._lut.length === steps) return this._lut;
				this._lut = [];
				steps--;
				for (var t = 0; t <= steps; t++) this._lut.push(this.compute(t / steps));
				return this._lut;
			},
			on: function(point, error) {
				error = error || 5;
				var lut = this.getLUT(), hits = [], c, t = 0;
				for (var i = 0; i < lut.length; i++) {
					c = lut[i];
					if (utils.dist(c, point) < error) {
						hits.push(c);
						t += i / lut.length;
					}
				}
				if (!hits.length) return false;
				return t /= hits.length;
			},
			project: function(point) {
				var LUT = this.getLUT(), l = LUT.length - 1, closest = utils.closest(LUT, point), mdist = closest.mdist, mpos = closest.mpos;
				var ft, t, p, d, t1 = (mpos - 1) / l, t2 = (mpos + 1) / l, step = .1 / l;
				mdist += 1;
				for (t = t1, ft = t; t < t2 + step; t += step) {
					p = this.compute(t);
					d = utils.dist(point, p);
					if (d < mdist) {
						mdist = d;
						ft = t;
					}
				}
				p = this.compute(ft);
				p.t = ft;
				p.d = mdist;
				return p;
			},
			get: function(t) {
				return this.compute(t);
			},
			point: function(idx) {
				return this.points[idx];
			},
			compute: function(t) {
				if (this.ratios) return utils.computeWithRatios(t, this.points, this.ratios, this._3d);
				return utils.compute(t, this.points, this._3d, this.ratios);
			},
			raise: function() {
				var p = this.points, np = [p[0]], i, k = p.length, pi, pim;
				for (var i = 1; i < k; i++) {
					pi = p[i];
					pim = p[i - 1];
					np[i] = {
						x: (k - i) / k * pi.x + i / k * pim.x,
						y: (k - i) / k * pi.y + i / k * pim.y
					};
				}
				np[k] = p[k - 1];
				return new Bezier(np);
			},
			derivative: function(t) {
				var mt = 1 - t, a, b, c = 0, p = this.dpoints[0];
				if (this.order === 2) {
					p = [
						p[0],
						p[1],
						ZERO
					];
					a = mt;
					b = t;
				}
				if (this.order === 3) {
					a = mt * mt;
					b = mt * t * 2;
					c = t * t;
				}
				var ret = {
					x: a * p[0].x + b * p[1].x + c * p[2].x,
					y: a * p[0].y + b * p[1].y + c * p[2].y
				};
				if (this._3d) ret.z = a * p[0].z + b * p[1].z + c * p[2].z;
				return ret;
			},
			curvature: function(t) {
				return utils.curvature(t, this.points, this._3d);
			},
			inflections: function() {
				return utils.inflections(this.points);
			},
			normal: function(t) {
				return this._3d ? this.__normal3(t) : this.__normal2(t);
			},
			__normal2: function(t) {
				var d = this.derivative(t);
				var q = sqrt(d.x * d.x + d.y * d.y);
				return {
					x: -d.y / q,
					y: d.x / q
				};
			},
			__normal3: function(t) {
				var r1 = this.derivative(t), r2 = this.derivative(t + .01), q1 = sqrt(r1.x * r1.x + r1.y * r1.y + r1.z * r1.z), q2 = sqrt(r2.x * r2.x + r2.y * r2.y + r2.z * r2.z);
				r1.x /= q1;
				r1.y /= q1;
				r1.z /= q1;
				r2.x /= q2;
				r2.y /= q2;
				r2.z /= q2;
				var c = {
					x: r2.y * r1.z - r2.z * r1.y,
					y: r2.z * r1.x - r2.x * r1.z,
					z: r2.x * r1.y - r2.y * r1.x
				};
				var m = sqrt(c.x * c.x + c.y * c.y + c.z * c.z);
				c.x /= m;
				c.y /= m;
				c.z /= m;
				var R = [
					c.x * c.x,
					c.x * c.y - c.z,
					c.x * c.z + c.y,
					c.x * c.y + c.z,
					c.y * c.y,
					c.y * c.z - c.x,
					c.x * c.z - c.y,
					c.y * c.z + c.x,
					c.z * c.z
				];
				return {
					x: R[0] * r1.x + R[1] * r1.y + R[2] * r1.z,
					y: R[3] * r1.x + R[4] * r1.y + R[5] * r1.z,
					z: R[6] * r1.x + R[7] * r1.y + R[8] * r1.z
				};
			},
			hull: function(t) {
				var p = this.points, _p = [], pt, q = [], idx = 0, i = 0, l = 0;
				q[idx++] = p[0];
				q[idx++] = p[1];
				q[idx++] = p[2];
				if (this.order === 3) q[idx++] = p[3];
				while (p.length > 1) {
					_p = [];
					for (i = 0, l = p.length - 1; i < l; i++) {
						pt = utils.lerp(t, p[i], p[i + 1]);
						q[idx++] = pt;
						_p.push(pt);
					}
					p = _p;
				}
				return q;
			},
			split: function(t1, t2) {
				if (t1 === 0 && !!t2) return this.split(t2).left;
				if (t2 === 1) return this.split(t1).right;
				var q = this.hull(t1);
				var result = {
					left: this.order === 2 ? new Bezier([
						q[0],
						q[3],
						q[5]
					]) : new Bezier([
						q[0],
						q[4],
						q[7],
						q[9]
					]),
					right: this.order === 2 ? new Bezier([
						q[5],
						q[4],
						q[2]
					]) : new Bezier([
						q[9],
						q[8],
						q[6],
						q[3]
					]),
					span: q
				};
				result.left._t1 = utils.map(0, 0, 1, this._t1, this._t2);
				result.left._t2 = utils.map(t1, 0, 1, this._t1, this._t2);
				result.right._t1 = utils.map(t1, 0, 1, this._t1, this._t2);
				result.right._t2 = utils.map(1, 0, 1, this._t1, this._t2);
				if (!t2) return result;
				t2 = utils.map(t2, t1, 1, 0, 1);
				return result.right.split(t2).left;
			},
			extrema: function() {
				var dims = this.dims, result = {}, roots = [], p, mfn;
				dims.forEach(function(dim) {
					mfn = function(v) {
						return v[dim];
					};
					p = this.dpoints[0].map(mfn);
					result[dim] = utils.droots(p);
					if (this.order === 3) {
						p = this.dpoints[1].map(mfn);
						result[dim] = result[dim].concat(utils.droots(p));
					}
					result[dim] = result[dim].filter(function(t) {
						return t >= 0 && t <= 1;
					});
					roots = roots.concat(result[dim].sort(utils.numberSort));
				}.bind(this));
				roots = roots.sort(utils.numberSort).filter(function(v, idx) {
					return roots.indexOf(v) === idx;
				});
				result.values = roots;
				return result;
			},
			bbox: function() {
				var extrema = this.extrema(), result = {};
				this.dims.forEach(function(d) {
					result[d] = utils.getminmax(this, d, extrema[d]);
				}.bind(this));
				return result;
			},
			overlaps: function(curve) {
				var lbbox = this.bbox(), tbbox = curve.bbox();
				return utils.bboxoverlap(lbbox, tbbox);
			},
			offset: function(t, d) {
				if (typeof d !== "undefined") {
					var c = this.get(t);
					var n = this.normal(t);
					var ret = {
						c,
						n,
						x: c.x + n.x * d,
						y: c.y + n.y * d
					};
					if (this._3d) ret.z = c.z + n.z * d;
					return ret;
				}
				if (this._linear) {
					var nv = this.normal(0);
					return [new Bezier(this.points.map(function(p) {
						var ret = {
							x: p.x + t * nv.x,
							y: p.y + t * nv.y
						};
						if (p.z && n.z) ret.z = p.z + t * nv.z;
						return ret;
					}))];
				}
				return this.reduce().map(function(s) {
					if (s._linear) return s.offset(t)[0];
					return s.scale(t);
				});
			},
			simple: function() {
				if (this.order === 3) {
					var a1 = utils.angle(this.points[0], this.points[3], this.points[1]);
					var a2 = utils.angle(this.points[0], this.points[3], this.points[2]);
					if (a1 > 0 && a2 < 0 || a1 < 0 && a2 > 0) return false;
				}
				var n1 = this.normal(0);
				var n2 = this.normal(1);
				var s = n1.x * n2.x + n1.y * n2.y;
				if (this._3d) s += n1.z * n2.z;
				return abs(acos(s)) < pi / 3;
			},
			reduce: function() {
				var i, t1 = 0, t2 = 0, step = .01, segment, pass1 = [], pass2 = [];
				var extrema = this.extrema().values;
				if (extrema.indexOf(0) === -1) extrema = [0].concat(extrema);
				if (extrema.indexOf(1) === -1) extrema.push(1);
				for (t1 = extrema[0], i = 1; i < extrema.length; i++) {
					t2 = extrema[i];
					segment = this.split(t1, t2);
					segment._t1 = t1;
					segment._t2 = t2;
					pass1.push(segment);
					t1 = t2;
				}
				pass1.forEach(function(p1) {
					t1 = 0;
					t2 = 0;
					while (t2 <= 1) for (t2 = t1 + step; t2 <= 1 + step; t2 += step) {
						segment = p1.split(t1, t2);
						if (!segment.simple()) {
							t2 -= step;
							if (abs(t1 - t2) < step) return [];
							segment = p1.split(t1, t2);
							segment._t1 = utils.map(t1, 0, 1, p1._t1, p1._t2);
							segment._t2 = utils.map(t2, 0, 1, p1._t1, p1._t2);
							pass2.push(segment);
							t1 = t2;
							break;
						}
					}
					if (t1 < 1) {
						segment = p1.split(t1, 1);
						segment._t1 = utils.map(t1, 0, 1, p1._t1, p1._t2);
						segment._t2 = p1._t2;
						pass2.push(segment);
					}
				});
				return pass2;
			},
			scale: function(d) {
				var order = this.order;
				var distanceFn = false;
				if (typeof d === "function") distanceFn = d;
				if (distanceFn && order === 2) return this.raise().scale(distanceFn);
				var clockwise = this.clockwise;
				var r1 = distanceFn ? distanceFn(0) : d;
				var r2 = distanceFn ? distanceFn(1) : d;
				var v = [this.offset(0, 10), this.offset(1, 10)];
				var o = utils.lli4(v[0], v[0].c, v[1], v[1].c);
				if (!o) throw new Error("cannot scale this curve. Try reducing it first.");
				var points = this.points, np = [];
				[0, 1].forEach(function(t) {
					var p = np[t * order] = utils.copy(points[t * order]);
					p.x += (t ? r2 : r1) * v[t].n.x;
					p.y += (t ? r2 : r1) * v[t].n.y;
				}.bind(this));
				if (!distanceFn) {
					[0, 1].forEach(function(t) {
						if (this.order === 2 && !!t) return;
						var p = np[t * order];
						var d = this.derivative(t);
						var p2 = {
							x: p.x + d.x,
							y: p.y + d.y
						};
						np[t + 1] = utils.lli4(p, p2, o, points[t + 1]);
					}.bind(this));
					return new Bezier(np);
				}
				[0, 1].forEach(function(t) {
					if (this.order === 2 && !!t) return;
					var p = points[t + 1];
					var ov = {
						x: p.x - o.x,
						y: p.y - o.y
					};
					var rc = distanceFn ? distanceFn((t + 1) / order) : d;
					if (distanceFn && !clockwise) rc = -rc;
					var m = sqrt(ov.x * ov.x + ov.y * ov.y);
					ov.x /= m;
					ov.y /= m;
					np[t + 1] = {
						x: p.x + rc * ov.x,
						y: p.y + rc * ov.y
					};
				}.bind(this));
				return new Bezier(np);
			},
			outline: function(d1, d2, d3, d4) {
				d2 = typeof d2 === "undefined" ? d1 : d2;
				var reduced = this.reduce(), len = reduced.length, fcurves = [], bcurves = [], p, alen = 0, tlen = this.length();
				var graduated = typeof d3 !== "undefined" && typeof d4 !== "undefined";
				function linearDistanceFunction(s, e, tlen, alen, slen) {
					return function(v) {
						var f1 = alen / tlen, f2 = (alen + slen) / tlen, d = e - s;
						return utils.map(v, 0, 1, s + f1 * d, s + f2 * d);
					};
				}
				reduced.forEach(function(segment) {
					slen = segment.length();
					if (graduated) {
						fcurves.push(segment.scale(linearDistanceFunction(d1, d3, tlen, alen, slen)));
						bcurves.push(segment.scale(linearDistanceFunction(-d2, -d4, tlen, alen, slen)));
					} else {
						fcurves.push(segment.scale(d1));
						bcurves.push(segment.scale(-d2));
					}
					alen += slen;
				});
				bcurves = bcurves.map(function(s) {
					p = s.points;
					if (p[3]) s.points = [
						p[3],
						p[2],
						p[1],
						p[0]
					];
					else s.points = [
						p[2],
						p[1],
						p[0]
					];
					return s;
				}).reverse();
				var fs = fcurves[0].points[0], fe = fcurves[len - 1].points[fcurves[len - 1].points.length - 1], bs = bcurves[len - 1].points[bcurves[len - 1].points.length - 1], be = bcurves[0].points[0], ls = utils.makeline(bs, fs), le = utils.makeline(fe, be), segments = [ls].concat(fcurves).concat([le]).concat(bcurves), slen = segments.length;
				return new PolyBezier(segments);
			},
			outlineshapes: function(d1, d2, curveIntersectionThreshold) {
				d2 = d2 || d1;
				var outline = this.outline(d1, d2).curves;
				var shapes = [];
				for (var i = 1, len = outline.length; i < len / 2; i++) {
					var shape = utils.makeshape(outline[i], outline[len - i], curveIntersectionThreshold);
					shape.startcap.virtual = i > 1;
					shape.endcap.virtual = i < len / 2 - 1;
					shapes.push(shape);
				}
				return shapes;
			},
			intersects: function(curve, curveIntersectionThreshold) {
				if (!curve) return this.selfintersects(curveIntersectionThreshold);
				if (curve.p1 && curve.p2) return this.lineIntersects(curve);
				if (curve instanceof Bezier) curve = curve.reduce();
				return this.curveintersects(this.reduce(), curve, curveIntersectionThreshold);
			},
			lineIntersects: function(line) {
				var mx = min(line.p1.x, line.p2.x), my = min(line.p1.y, line.p2.y), MX = max(line.p1.x, line.p2.x), MY = max(line.p1.y, line.p2.y), self = this;
				return utils.roots(this.points, line).filter(function(t) {
					var p = self.get(t);
					return utils.between(p.x, mx, MX) && utils.between(p.y, my, MY);
				});
			},
			selfintersects: function(curveIntersectionThreshold) {
				var reduced = this.reduce();
				var i, len = reduced.length - 2, results = [], result, left, right;
				for (i = 0; i < len; i++) {
					left = reduced.slice(i, i + 1);
					right = reduced.slice(i + 2);
					result = this.curveintersects(left, right, curveIntersectionThreshold);
					results = results.concat(result);
				}
				return results;
			},
			curveintersects: function(c1, c2, curveIntersectionThreshold) {
				var pairs = [];
				c1.forEach(function(l) {
					c2.forEach(function(r) {
						if (l.overlaps(r)) pairs.push({
							left: l,
							right: r
						});
					});
				});
				var intersections = [];
				pairs.forEach(function(pair) {
					var result = utils.pairiteration(pair.left, pair.right, curveIntersectionThreshold);
					if (result.length > 0) intersections = intersections.concat(result);
				});
				return intersections;
			},
			arcs: function(errorThreshold) {
				errorThreshold = errorThreshold || .5;
				return this._iterate(errorThreshold, []);
			},
			_error: function(pc, np1, s, e) {
				var q = (e - s) / 4, c1 = this.get(s + q), c2 = this.get(e - q), ref = utils.dist(pc, np1), d1 = utils.dist(pc, c1), d2 = utils.dist(pc, c2);
				return abs(d1 - ref) + abs(d2 - ref);
			},
			_iterate: function(errorThreshold, circles) {
				var t_s = 0, t_e = 1, safety;
				do {
					safety = 0;
					t_e = 1;
					var np1 = this.get(t_s), np2, np3, arc, prev_arc;
					var curr_good = false, prev_good = false, done;
					var t_m = t_e, prev_e = 1, step = 0;
					do {
						prev_good = curr_good;
						prev_arc = arc;
						t_m = (t_s + t_e) / 2;
						step++;
						np2 = this.get(t_m);
						np3 = this.get(t_e);
						arc = utils.getccenter(np1, np2, np3);
						arc.interval = {
							start: t_s,
							end: t_e
						};
						curr_good = this._error(arc, np1, t_s, t_e) <= errorThreshold;
						done = prev_good && !curr_good;
						if (!done) prev_e = t_e;
						if (curr_good) {
							if (t_e >= 1) {
								arc.interval.end = prev_e = 1;
								prev_arc = arc;
								if (t_e > 1) {
									var d = {
										x: arc.x + arc.r * cos(arc.e),
										y: arc.y + arc.r * sin(arc.e)
									};
									arc.e += utils.angle({
										x: arc.x,
										y: arc.y
									}, d, this.get(1));
								}
								break;
							}
							t_e = t_e + (t_e - t_s) / 2;
						} else t_e = t_m;
					} while (!done && safety++ < 100);
					if (safety >= 100) break;
					prev_arc = prev_arc ? prev_arc : arc;
					circles.push(prev_arc);
					t_s = prev_e;
				} while (t_e < 1);
				return circles;
			}
		};
		module.exports = Bezier;
	})();
}));
//#endregion
//#region node_modules/bezier-js/index.js
var require_bezier_js = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_bezier();
}));
//#endregion
//#region node_modules/arr-flatten/index.js
/*!
* arr-flatten <https://github.com/jonschlinkert/arr-flatten>
*
* Copyright (c) 2014-2017, Jon Schlinkert.
* Released under the MIT License.
*/
var require_arr_flatten = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function(arr) {
		return flat(arr, []);
	};
	function flat(arr, res) {
		var i = 0, cur;
		var len = arr.length;
		for (; i < len; i++) {
			cur = arr[i];
			Array.isArray(cur) ? flat(cur, res) : res.push(cur);
		}
		return res;
	}
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/object-types.js
var require_object_types = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		PointObjectType: 1,
		LineObjectType: 2,
		AreaObjectType: 3,
		UnformattedTextObjectType: 4,
		FormattedTextObjectType: 5,
		LineTextObjectType: 6,
		RectangleObjectType: 7
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-reader/symbol-element-types.js
var require_symbol_element_types = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		LineElementType: 1,
		AreaElementType: 2,
		CircleElementType: 3,
		DotElementType: 4
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/transform-features.js
var require_transform_features = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { PointSymbolType, LineSymbolType } = require_symbol_types();
	var defaultOptions = {
		generateSymbolElements: true,
		exportHidden: false
	};
	module.exports = transformFeatures;
	/** @typedef {import('./ocad-reader/symbol').BaseSymbolDef} Symbol */
	/** @typedef {import('./ocad-reader/symbol-element')} SymbolElement */
	/** @typedef {import('./ocad-reader/tobject').TObject} TObject */
	/** @typedef {import("./ocad-reader/ocad-file")} OcadFile */
	/**
	* @typedef {Object} TransformFeaturesOptions
	* @property {boolean=} generateSymbolElements generate features for symbol elements (default: `true`)
	* @property {boolean=} exportHidden export hidden objects (default: `false`)
	* @property {number[]=} includeSymbols only export features from the given symbols;
	*    symbols are identified by their OCAD internal symbol number (for example `40015`, not `400.15`);
	*    if undefined, all symbols will be exported
	* @property {TObject[]=} objects only export the given objects;
	*    if undefined, all objects, filtered by the `exportHidden` option, will be exported
	* @property {import('./ocad-reader/ocad-file').Color[]=} [colors] the colors of the OCAD file
	* @property {number=} [idCount] the current id count
	*/
	/**
	* @template {Object} T
	* @typedef {function(TransformFeaturesOptions, Record<number, Symbol>, TObject, number): T[]|null|undefined} CreateObjects
	*/
	/**
	* @template {Object} U
	* @typedef {(symbol: Symbol, name: string, index: number, element: SymbolElement, c: import('./ocad-reader/td-poly'), angle: number, options: TransformFeaturesOptions, object: TObject, objectId: number) => U|null|undefined} CreateElement
	*/
	/**
	* @template {Object} T result type
	* @template {Object} U element result type
	* @param {OcadFile} ocadFile
	* @param {CreateObjects<T>} createObjects
	* @param {CreateElement<U>} createElement
	* @param {TransformFeaturesOptions} options
	* @returns {T[]}
	*/
	function transformFeatures(ocadFile, createObjects, createElement, options) {
		options = {
			...defaultOptions,
			...options,
			colors: ocadFile.colors,
			idCount: ocadFile.objects.length
		};
		const symbols = ocadFile.symbols.filter((s) => !options.includeSymbols || options.includeSymbols.find((symNum) => symNum === s.symNum)).reduce((ss, s) => {
			ss[s.symNum] = s;
			return ss;
		}, {});
		const objects = options.objects || ocadFile.objects;
		let features = objects.map(createObjects.bind(null, options, symbols)).flat().filter(Boolean);
		if (options.generateSymbolElements) {
			const elementFeatures = objects.map(generateSymbolElements.bind(null, createElement, options, symbols)).flat();
			features = features.concat(elementFeatures).filter(Boolean);
		}
		return features;
	}
	var generateSymbolElements = (createElement, options, symbols, object, objectIndex) => {
		const symbol = symbols[object.sym];
		let elements = [];
		if (!symbol || !options.exportHidden && symbol.isHidden()) return;
		switch (symbol.type) {
			case PointSymbolType: {
				const angle = object.ang ? object.ang / 10 / 180 * Math.PI : 0;
				elements = symbol.elements.map((e, i) => createElement(symbol, "element", i, e, object.coordinates[0], angle, options, object, objectIndex));
				break;
			}
			case LineSymbolType:
				if (symbol.primSymElements.length > 0 && symbol.mainLength > 0) {
					const coords = object.coordinates;
					const endLength = symbol.endLength;
					const mainLength = symbol.mainLength;
					const spotDist = symbol.primSymDist;
					let d = endLength;
					for (let i = 1; i < coords.length; i++) {
						const c0 = coords[i - 1];
						const v = coords[i].sub(c0);
						const angle = Math.atan2(v[1], v[0]);
						const u = v.unit();
						const segmentLength = v.vLength();
						let c = c0.add(u.mul(d));
						let j = 0;
						while (d < segmentLength) {
							elements = elements.concat(symbol.primSymElements.map((e, i) => createElement(symbol, "prim", i, e, c, angle, options, object, objectIndex)));
							j++;
							const step = spotDist && j % symbol.nPrimSym ? spotDist : mainLength;
							c = c.add(u.mul(step));
							d += step;
						}
						d -= segmentLength;
					}
				}
				if (symbol.cornerSymElements.length > 0) {
					const coords = object.coordinates;
					for (let i = 1; i < coords.length - 1; i++) {
						const c0 = coords[i - 1];
						const c1 = coords[i];
						const v = c1.sub(c0);
						const angle = Math.atan2(v[1], v[0]);
						elements = elements.concat(symbol.cornerSymElements.map((e, i) => createElement(symbol, "corner", i, e, c1, angle, options, object, objectIndex)));
					}
				}
				if (symbol.startSymElements.length > 0 && object.coordinates.length > 1) {
					const coords = object.coordinates;
					const c0 = coords[0];
					const v = coords[1].sub(c0);
					const angle = Math.atan2(v[1], v[0]);
					elements = elements.concat(symbol.startSymElements.map((e, i) => createElement(symbol, "start", i, e, object.coordinates[0], angle, options, object, objectIndex)));
				}
				if (symbol.endSymElements.length > 0 && object.coordinates.length > 1) {
					const coords = object.coordinates;
					const c0 = coords[coords.length - 2];
					const c1 = coords[coords.length - 1];
					const v = c1.sub(c0);
					const angle = Math.atan2(v[1], v[0]);
					elements = elements.concat(symbol.endSymElements.map((e, i) => createElement(symbol, "start", i, e, c1, angle, options, object, objectIndex)));
				}
		}
		return elements;
	};
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-to-geojson.js
var require_ocad_to_geojson = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { coordEach } = require_cjs$2();
	var { featureCollection } = require_cjs$3();
	var Bezier = require_bezier_js();
	var flatten = require_arr_flatten();
	var { PointObjectType, LineObjectType, AreaObjectType, UnformattedTextObjectType, FormattedTextObjectType, LineTextObjectType } = require_object_types();
	var { LineElementType, AreaElementType, CircleElementType, DotElementType } = require_symbol_element_types();
	var transformFeatures = require_transform_features();
	var TdPoly = require_td_poly();
	var defaultOptions = {
		applyCrs: true,
		generateSymbolElements: true,
		exportHidden: false,
		coordinatePrecision: 6
	};
	module.exports = ocadToGeoJson;
	/**
	* @typedef {import("./ocad-reader/tobject").TObject} TObject
	*/
	/** @typedef {import("geojson").Geometry} Geometry */
	/**
	* @template {Geometry} TGeometry
	* @template {Object} TProperties
	* @typedef {import("geojson").FeatureCollection<TGeometry, TProperties>} FeatureCollection<TGeometry, TProperties>
	*/
	/**
	* @template {Geometry} TGeometry
	* @template {Object} TProperties
	* @typedef {import("geojson").Feature<TGeometry, TProperties>} Feature<TGeometry, TProperties>
	*/
	/**
	* @typedef {import("./transform-features").TransformFeaturesOptions} TransformFeaturesOptions
	*
	* @typedef {object} OcadToGeoJsonOptionsProps
	* @property {boolean=} applyCrs transform coordinates to the file's geographic coordinates (default: `true`)
	* @property {number=} coordinatePrecision number of digits after the decimal point (default: `6`)
	*
	* @typedef {TransformFeaturesOptions & OcadToGeoJsonOptionsProps} OcadToGeoJsonOptions
	*/
	/**
	* @typedef {Object} OcadObjectProperties
	* @property {number} sym
	* @property {number} otp
	* @property {boolean} unicode
	* @property {number} ang
	* @property {number} col
	* @property {number} lineWidth
	* @property {number} diamFlags
	* @property {number} serverObjectId
	* @property {number} height
	* @property {number} creationDate
	* @property {number} multirepresentationId
	* @property {number} modificationDate
	* @property {number} nItem
	* @property {number} nText
	* @property {number} nObjectString
	* @property {number} nDatabaseString
	* @property {number} objectStringType
	* @property {number} res1
	* @property {string} text
	* @property {string|undefined} objectString
	* @property {string|undefined} databaseString
	* @property {number} objectIndex
	*/
	/**
	* @typedef {Object} ElementProperties
	* @property {string} element
	* @property {number} parentId
	*/
	/**
	* Given an `OcadFile` object, returns a GeoJSON `FeatureCollection` of the file's objects.
	* @param {import("./ocad-reader/ocad-file")} ocadFile the OCAD file
	* @param {OcadToGeoJsonOptions=} options options
	* @returns {FeatureCollection<Geometry, OcadObjectProperties>}
	*/
	function ocadToGeoJson(ocadFile, options) {
		options = {
			...defaultOptions,
			...options
		};
		const result = featureCollection(transformFeatures(ocadFile, tObjectToGeoJson, createElement, options));
		if (options.applyCrs) applyCrs(result, ocadFile.getCrs());
		coordEach(result, (c) => {
			c[0] = formatNum(c[0], options.coordinatePrecision);
			c[1] = formatNum(c[1], options.coordinatePrecision);
		});
		return result;
	}
	/**
	*
	* @param {OcadToGeoJsonOptions} options
	* @param {Record<number, import('./ocad-reader/symbol').BaseSymbolDef>} symbols
	* @param {TObject} object
	* @param {number} i
	* @returns {Feature<Geometry, OcadObjectProperties>[]}
	*/
	var tObjectToGeoJson = (options, symbols, object, i) => {
		const symbol = symbols[object.sym];
		if (!symbol || !options.exportHidden && symbol.isHidden()) return;
		/** @type Geometry */
		let geometry;
		switch (object.objType) {
			case PointObjectType:
				geometry = {
					type: "Point",
					coordinates: object.coordinates[0].slice()
				};
				break;
			case LineObjectType:
				geometry = {
					type: "LineString",
					coordinates: extractCoords(object.coordinates).map((c) => c.slice())
				};
				break;
			case AreaObjectType:
				geometry = {
					type: "Polygon",
					coordinates: coordinatesToRings(object.coordinates)
				};
				break;
			case UnformattedTextObjectType:
			case FormattedTextObjectType:
			case LineTextObjectType: {
				if (!("fontSize" in symbol)) throw new Error(`Text object's symbol is not a text symbol`);
				const lineHeight = symbol.fontSize / 10 * .352778 * 100;
				geometry = {
					type: "Point",
					coordinates: [object.coordinates[0][0], object.coordinates[0][1] + lineHeight]
				};
				break;
			}
			default: return;
		}
		return [{
			type: "Feature",
			properties: getProperties(object),
			id: i + 1,
			geometry
		}];
	};
	var extractCoords = (coords) => {
		const cs = [];
		let lastC;
		let cp1;
		let cp2;
		for (let i = 0; i < coords.length; i++) {
			const c = coords[i];
			if (c.isFirstBezier()) cp1 = c;
			else if (c.isSecondBezier()) cp2 = c;
			else if (cp1 && cp2) {
				const l = cp2.sub(cp1).vLength();
				const bezierCoords = new Bezier(flatten([
					lastC,
					cp1,
					cp2,
					c
				])).getLUT(Math.round(l / 2)).map((bc) => TdPoly.fromCoords(bc.x, bc.y));
				cs.push.apply(cs, bezierCoords.slice(1));
				cp1 = cp2 = void 0;
				lastC = c;
			} else {
				cs.push(c);
				lastC = c;
			}
		}
		return cs;
	};
	/**
	* @param {import('./ocad-reader/symbol').BaseSymbolDef} symbol
	* @param {string} name
	* @param {number} index
	* @param {import('./ocad-reader/symbol-element')} element
	* @param {TdPoly} c
	* @param {number} angle
	* @param {TransformFeaturesOptions} options
	* @param {TObject} object
	* @param {number} objectId
	* @returns {Feature<Geometry, ElementProperties>}
	*/
	var createElement = (symbol, name, index, element, c, angle, options, object, objectId) => {
		/** @type Geometry */
		let geometry;
		const coords = extractCoords(element.coords);
		const translatedCoords = (angle ? coords.map((lc) => lc.rotate(angle)) : coords).map((lc) => lc.add(c));
		switch (element.type) {
			case LineElementType:
				geometry = {
					type: "LineString",
					coordinates: translatedCoords
				};
				break;
			case AreaElementType:
				geometry = {
					type: "Polygon",
					coordinates: coordinatesToRings(translatedCoords)
				};
				break;
			case CircleElementType:
			case DotElementType:
				geometry = {
					type: "Point",
					coordinates: translatedCoords[0]
				};
				break;
		}
		return {
			type: "Feature",
			properties: {
				element: `${symbol.symNum}-${name}-${index}`,
				parentId: objectId + 1
			},
			id: ++options.idCount,
			geometry
		};
	};
	var applyCrs = (featureCollection, crs) => {
		coordEach(featureCollection, (coord) => {
			const crsCoord = crs.toProjectedCoord(coord);
			coord[0] = crsCoord[0];
			coord[1] = crsCoord[1];
		});
	};
	function formatNum(num, digits) {
		const pow = Math.pow(10, digits === void 0 ? 6 : digits);
		return Math.round(num * pow) / pow;
	}
	var coordinatesToRings = (coordinates) => {
		const rings = [];
		let currentRing = [];
		rings.push(currentRing);
		for (let i = 0; i < coordinates.length; i++) {
			const c = coordinates[i];
			if (c.isFirstHolePoint()) {
				currentRing.push(currentRing[0].slice());
				currentRing = [];
				rings.push(currentRing);
			}
			currentRing.push(c.slice());
		}
		currentRing.push(currentRing[0].slice());
		return rings;
	};
	/**
	* Returns the GeoJSON properties for this object.
	* @param {TObject} object
	* @returns {OcadObjectProperties}
	*/
	function getProperties(object) {
		return {
			sym: object.sym,
			otp: object.otp,
			unicode: object.unicode,
			ang: object.ang,
			col: object.col,
			lineWidth: object.lineWidth,
			diamFlags: object.diamFlags,
			serverObjectId: object.serverObjectId,
			height: object.height,
			creationDate: object.creationDate,
			multirepresentationId: object.multirepresentationId,
			modificationDate: object.modificationDate,
			nItem: object.nItem,
			nText: object.nText,
			nObjectString: object.nObjectString,
			nDatabaseString: object.nDatabaseString,
			objectStringType: object.objectStringType,
			res1: object.res1,
			text: object.text,
			objectString: object.objectString,
			databaseString: object.databaseString,
			objectIndex: object.objIndex._index
		};
	}
}));
//#endregion
//#region node_modules/@turf/invariant/dist/cjs/index.cjs
var require_cjs$1 = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	var _helpers = require_cjs$3();
	function getCoord(coord) {
		if (!coord) throw new Error("coord is required");
		if (!Array.isArray(coord)) {
			if (coord.type === "Feature" && coord.geometry !== null && coord.geometry.type === "Point") return [...coord.geometry.coordinates];
			if (coord.type === "Point") return [...coord.coordinates];
		}
		if (Array.isArray(coord) && coord.length >= 2 && !Array.isArray(coord[0]) && !Array.isArray(coord[1])) return [...coord];
		throw new Error("coord must be GeoJSON Point or an Array of numbers");
	}
	function getCoords(coords) {
		if (Array.isArray(coords)) return coords;
		if (coords.type === "Feature") {
			if (coords.geometry !== null) return coords.geometry.coordinates;
		} else if (coords.coordinates) return coords.coordinates;
		throw new Error("coords must be GeoJSON Feature, Geometry Object or an Array");
	}
	function containsNumber(coordinates) {
		if (coordinates.length > 1 && _helpers.isNumber.call(void 0, coordinates[0]) && _helpers.isNumber.call(void 0, coordinates[1])) return true;
		if (Array.isArray(coordinates[0]) && coordinates[0].length) return containsNumber(coordinates[0]);
		throw new Error("coordinates must only contain numbers");
	}
	function geojsonType(value, type, name) {
		if (!type || !name) throw new Error("type and name required");
		if (!value || value.type !== type) throw new Error("Invalid input to " + name + ": must be a " + type + ", given " + value.type);
	}
	function featureOf(feature, type, name) {
		if (!feature) throw new Error("No feature passed");
		if (!name) throw new Error(".featureOf() requires a name");
		if (!feature || feature.type !== "Feature" || !feature.geometry) throw new Error("Invalid input to " + name + ", Feature with geometry required");
		if (!feature.geometry || feature.geometry.type !== type) throw new Error("Invalid input to " + name + ": must be a " + type + ", given " + feature.geometry.type);
	}
	function collectionOf(featureCollection, type, name) {
		if (!featureCollection) throw new Error("No featureCollection passed");
		if (!name) throw new Error(".collectionOf() requires a name");
		if (!featureCollection || featureCollection.type !== "FeatureCollection") throw new Error("Invalid input to " + name + ", FeatureCollection required");
		for (const feature of featureCollection.features) {
			if (!feature || feature.type !== "Feature" || !feature.geometry) throw new Error("Invalid input to " + name + ", Feature with geometry required");
			if (!feature.geometry || feature.geometry.type !== type) throw new Error("Invalid input to " + name + ": must be a " + type + ", given " + feature.geometry.type);
		}
	}
	function getGeom(geojson) {
		if (geojson.type === "Feature") return geojson.geometry;
		return geojson;
	}
	function getType(geojson, _name) {
		if (geojson.type === "FeatureCollection") return "FeatureCollection";
		if (geojson.type === "GeometryCollection") return "GeometryCollection";
		if (geojson.type === "Feature" && geojson.geometry !== null) return geojson.geometry.type;
		return geojson.type;
	}
	exports.collectionOf = collectionOf;
	exports.containsNumber = containsNumber;
	exports.featureOf = featureOf;
	exports.geojsonType = geojsonType;
	exports.getCoord = getCoord;
	exports.getCoords = getCoords;
	exports.getGeom = getGeom;
	exports.getType = getType;
}));
//#endregion
//#region node_modules/@turf/line-offset/dist/cjs/index.cjs
var require_cjs = /* @__PURE__ */ __commonJSMin(((exports) => {
	Object.defineProperty(exports, "__esModule", { value: true });
	var _meta = require_cjs$2();
	var _invariant = require_cjs$1();
	var _helpers = require_cjs$3();
	function ab(segment) {
		var start = segment[0];
		var end = segment[1];
		return [end[0] - start[0], end[1] - start[1]];
	}
	function crossProduct(v1, v2) {
		return v1[0] * v2[1] - v2[0] * v1[1];
	}
	function add(v1, v2) {
		return [v1[0] + v2[0], v1[1] + v2[1]];
	}
	function sub(v1, v2) {
		return [v1[0] - v2[0], v1[1] - v2[1]];
	}
	function scalarMult(s, v) {
		return [s * v[0], s * v[1]];
	}
	function intersectSegments(a, b) {
		var p = a[0];
		var r = ab(a);
		var q = b[0];
		var s = ab(b);
		var cross = crossProduct(r, s);
		return add(p, scalarMult(crossProduct(sub(q, p), s) / cross, r));
	}
	function isParallel(a, b) {
		return crossProduct(ab(a), ab(b)) === 0;
	}
	function intersection(a, b) {
		if (isParallel(a, b)) return false;
		return intersectSegments(a, b);
	}
	function lineOffset(geojson, distance, options = {}) {
		options = options || {};
		if (!_helpers.isObject.call(void 0, options)) throw new Error("options is invalid");
		const { units = "kilometers" } = options;
		if (!geojson) throw new Error("geojson is required");
		if (distance === void 0 || distance === null || isNaN(distance)) throw new Error("distance is required");
		var type = _invariant.getType.call(void 0, geojson);
		var properties = geojson.type === "Feature" ? geojson.properties : {};
		switch (type) {
			case "LineString": return lineOffsetFeature(geojson, distance, units);
			case "MultiLineString":
				var coords = [];
				_meta.flattenEach.call(void 0, geojson, function(feature) {
					coords.push(lineOffsetFeature(feature, distance, units).geometry.coordinates);
				});
				return _helpers.multiLineString.call(void 0, coords, properties);
			default: throw new Error("geometry " + type + " is not supported");
		}
	}
	function lineOffsetFeature(line, distance, units) {
		var segments = [];
		var offsetDegrees = _helpers.lengthToDegrees.call(void 0, distance, units);
		var coords = _invariant.getCoords.call(void 0, line);
		var finalCoords = [];
		coords.forEach(function(currentCoords, index) {
			if (index !== coords.length - 1) {
				var segment = processSegment(currentCoords, coords[index + 1], offsetDegrees);
				segments.push(segment);
				if (index > 0) {
					var seg2Coords = segments[index - 1];
					var intersects = intersection(segment, seg2Coords);
					if (intersects !== false) {
						seg2Coords[1] = intersects;
						segment[0] = intersects;
					}
					finalCoords.push(seg2Coords[0]);
					if (index === coords.length - 2) {
						finalCoords.push(segment[0]);
						finalCoords.push(segment[1]);
					}
				}
				if (coords.length === 2) {
					finalCoords.push(segment[0]);
					finalCoords.push(segment[1]);
				}
			}
		});
		return _helpers.lineString.call(void 0, finalCoords, line.type === "Feature" ? line.properties : {});
	}
	function processSegment(point1, point2, offset) {
		var L = Math.sqrt((point1[0] - point2[0]) * (point1[0] - point2[0]) + (point1[1] - point2[1]) * (point1[1] - point2[1]));
		var out1x = point1[0] + offset * (point2[1] - point1[1]) / L;
		var out2x = point2[0] + offset * (point2[1] - point1[1]) / L;
		var out1y = point1[1] + offset * (point1[0] - point2[0]) / L;
		var out2y = point2[1] + offset * (point1[0] - point2[0]) / L;
		return [[out1x, out1y], [out2x, out2y]];
	}
	exports.default = lineOffset;
	exports.lineOffset = lineOffset;
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-to-svg.js
var require_ocad_to_svg = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { AreaSymbolType, DblFillColorOn } = require_symbol_types();
	var { LineObjectType, AreaObjectType, LineTextObjectType, FormattedTextObjectType, UnformattedTextObjectType } = require_object_types();
	var { LineElementType, AreaElementType, CircleElementType, DotElementType } = require_symbol_element_types();
	var transformFeatures = require_transform_features();
	var flatten = require_arr_flatten();
	var _lineOffset = require_cjs();
	var lineOffset = _lineOffset.default || _lineOffset;
	var TdPoly = require_td_poly();
	var { HorizontalAlignCenter, HorizontalAlignLeft, VerticalAlignTop, VerticalAlignBottom } = require_text_symbol();
	var svgNamespace = "http://www.w3.org/2000/svg";
	var defaultOptions = {
		generateSymbolElements: true,
		exportHidden: false,
		fill: "transparent"
	};
	var patternToSvg = (colors, s) => {
		const patterns = [];
		if (s.hatchMode) {
			const height = s.hatchDist;
			const width = 10;
			const a1 = s.hatchAngle1;
			const a2 = s.hatchAngle2;
			patterns.push({
				id: `hatch-fill-${s.symNum}-1`,
				"data-symbol-name": s.name,
				type: "pattern",
				attrs: {
					patternUnits: "userSpaceOnUse",
					patternTransform: `rotate(${a1 / 10})`,
					width,
					height
				},
				children: [{
					type: "rect",
					attrs: {
						x: 0,
						y: 0,
						width,
						height: s.hatchLineWidth,
						fill: colors[s.hatchColor].rgb
					}
				}]
			});
			if (s.hatchMode === 2) patterns.push({
				id: `hatch-fill-${s.symNum}-2`,
				"data-symbol-name": s.name,
				type: "pattern",
				attrs: {
					patternUnits: "userSpaceOnUse",
					patternTransform: `rotate(${a2 / 10})`,
					width,
					height
				},
				children: [{
					type: "rect",
					attrs: {
						x: 0,
						y: 0,
						width,
						height: s.hatchLineWidth,
						fill: colors[s.hatchColor].rgb
					}
				}]
			});
		}
		if (s.structMode) {
			const width = s.structWidth;
			const height = s.structHeight * (s.structMode === 2 ? 2 : 1);
			patterns.push({
				id: `struct-fill-${s.symNum}`,
				"data-symbol-name": s.name,
				type: "pattern",
				attrs: {
					patternUnits: "userSpaceOnUse",
					patternTransform: `rotate(${s.structAngle / 10})`,
					width,
					height
				},
				children: s.elements.map((e, i) => elementToSvg(s, "", i, e, [s.structWidth * .5, -s.structHeight * .5], 0, { colors })).concat(s.structMode === 2 ? s.elements.map((e, i) => elementToSvg(s, "", i, e, [s.structWidth, -s.structHeight * 1.5], 0, { colors })).concat(s.elements.map((e, i) => elementToSvg(s, "", i, e, [0, -s.structHeight * 1.5], 0, { colors }))) : []).filter(Boolean)
			});
		}
		return patterns;
	};
	/**
	* @typedef {object} SvgNodeDef
	* @property {string} type
	* @property {string=} text
	* @property {string=} id
	* @property {number=} order
	* @property {Record<string, string>=} attrs
	* @property {SvgNodeDef[]=} children
	*/
	/**
	*
	* @param {Document} document
	* @param {SvgNodeDef} n
	* @returns
	*/
	var createSvgNode = (document, n) => {
		let node;
		if (n.text === void 0) {
			node = document.createElementNS(svgNamespace, n.type);
			const xmlnss = Object.entries(n.attrs || []).filter(([key, _]) => key.startsWith("xmlns"));
			for (const [ns, uri] of xmlnss) node.setAttributeNS("http://www.w3.org/2000/xmlns/", ns, uri);
			n.id && (node.id = n.id);
			n.attrs && Object.keys(n.attrs).forEach((attrName) => node.setAttribute(attrName, n.attrs[attrName]));
		} else node = document.createTextNode(n.text);
		n.children && n.children.filter(Boolean).forEach((child) => node.appendChild(createSvgNode(document, child)));
		return node;
	};
	module.exports = {
		/**
		*
		* @param {import('./ocad-reader/ocad-file')} ocadFile
		* @param {*} options
		* @returns
		*/
		ocadToSvg: function(ocadFile, options) {
			options = {
				...defaultOptions,
				...options
			};
			const patterns = flatten(usedSymbolNumbers(options.objects || ocadFile.objects).map((symNum) => ocadFile.symbols.find((s) => symNum === s.symNum)).filter((s) => s).filter((s) => s.type === AreaSymbolType && "hatchMode" in s && (s.hatchMode || s.structMode)).map(patternToSvg.bind(null, ocadFile.colors)));
			const bounds = ocadFile.getBounds();
			const childNodes = transformFeatures(ocadFile, objectToSvg, elementToSvg, options);
			let children;
			if (options.fromColor == null && options.toColor == null) children = childNodes;
			else children = childNodes.filter((node) => (options.fromColor == null || node.order >= options.fromColor) && (options.toColor == null || node.order <= options.toColor));
			children.sort((a, b) => b.order - a.order);
			const root = {
				type: "svg",
				attrs: {
					xmlns: svgNamespace,
					fill: options.fill,
					viewBox: bounds.slice(0, 2) + "," + (bounds[2] - bounds[0]) + "," + (bounds[3] - bounds[1])
				},
				children: [{
					type: "defs",
					children: patterns
				}].concat([{
					type: "g",
					attrs: {
						xmlns: svgNamespace,
						transform: `translate(0, ${bounds[1] + bounds[3]})`
					},
					children
				}])
			};
			return createSvgNode(options.document || window.document, root);
		},
		patternToSvg,
		createSvgNode
	};
	var usedSymbolNumbers = (objects) => Array.from(objects.reduce((seen, f) => {
		seen.add(f.sym);
		return seen;
	}, /* @__PURE__ */ new Set()));
	/**
	*
	* @param {*} options
	* @param {Record<number, import('./ocad-reader/symbol').BaseSymbolDef>} symbols
	* @param {import('./ocad-reader/tobject').TObject} object
	* @returns
	*/
	var objectToSvg = (options, symbols, object) => {
		const symbol = symbols[options.sym || object.sym];
		if (!symbol || !options.exportHidden && symbol.isHidden()) return;
		/** @type {SvgNodeDef[]} */
		const nodes = [];
		switch (options.objType || object.objType) {
			case LineObjectType: {
				if (symbol.type !== 2) return nodes;
				const dashPattern = getDashPattern(symbol.mainGap, symbol.secGap, symbol.mainLength, symbol.endLength, symbol.endGap);
				const dbl = symbol.doubleLine;
				const dblMode = dbl?.dblMode ?? 0;
				const totalFillWidth = (dbl?.dblLeftWidth ?? 0) + (dbl?.dblWidth ?? 0) + (dbl?.dblRightWidth ?? 0);
				if (dblMode === 2 && dbl.dblFillColor != null && totalFillWidth > 0) nodes.push(lineToPath(object.coordinates, totalFillWidth, options.colors[dbl.dblFillColor], null, symbol.lineStyle, options.closePath));
				if (dblMode === 1 && dbl.dblFlags & DblFillColorOn) {
					if (dbl.dblLeftWidth > 0 && dbl.dblRightWidth > 0) nodes.push(lineToPath(object.coordinates, dbl.dblLeftWidth + dbl.dblRightWidth + dbl.dblWidth, options.colors[dbl.dblLeftColor], null, symbol.lineStyle, options.closePath));
					nodes.push(lineToPath(object.coordinates, dbl.dblWidth, options.colors[dbl.dblFillColor], null, symbol.lineStyle, options.closePath));
				} else if (dblMode === 1 && !(dbl?.dblFlags & DblFillColorOn)) nodes.push(...[[
					-dbl.dblWidth / 2 - dbl.dblLeftWidth / 2,
					dbl.dblLeftWidth,
					dbl.dblLeftColor
				], [
					dbl.dblWidth / 2 + dbl.dblRightWidth / 2,
					dbl.dblRightWidth,
					dbl.dblRightColor
				]].map(([offset, width, color]) => {
					const a = object.coordinates.map((c) => ({
						type: "circle",
						attrs: {
							cx: c[0].toString(),
							cy: (-c[1]).toString(),
							r: "3",
							fill: "red"
						}
					}));
					return offsetLineCoordinates(object.coordinates, offset, options.closePath).map((lineCoords) => {
						return lineToPath(lineCoords, width, options.colors[color], null, symbol.lineStyle, options.closePath);
					}).concat(a);
				}).flat());
				if (symbol.frWidth > 0) nodes.push(lineToPath(object.coordinates, symbol.frWidth, options.colors[symbol.frColor], dashPattern, symbol.frStyle, options.closePath));
				if (symbol.lineWidth > 0) nodes.push(lineToPath(object.coordinates, symbol.lineWidth, options.colors[symbol.lineColor], dashPattern, symbol.lineStyle, options.closePath));
				break;
			}
			case AreaObjectType: {
				if (symbol.type !== 3) throw new Error("Symbol mismatch: area object with non-area symbol");
				const fillColorIndex = symbol.fillOn ? symbol.fillColor : symbol.colors[0];
				const fillPattern = symbol.hatchMode && `url(#hatch-fill-${symbol.symNum}-1)` || symbol.structMode && `url(#struct-fill-${symbol.symNum})`;
				if (symbol.fillOn) {
					if (fillColorIndex != null) nodes.push(areaToPath(object.coordinates, null, options.colors[fillColorIndex]));
				}
				if (fillPattern) {
					const patternColor = symbol.hatchMode ? symbol.hatchColor : symbol.elements.length ? Math.min(...symbol.elements.map((e) => e.color)) : symbol.fillColor;
					nodes.push(areaToPath(object.coordinates, fillPattern, options.colors[patternColor]));
					if (symbol.hatchMode === 2) nodes.push(areaToPath(object.coordinates, `url(#hatch-fill-${symbol.symNum}-2)`, options.colors[patternColor]));
				}
				if (symbol.borderSym) nodes.push(...objectToSvg({
					...options,
					sym: symbol.borderSym,
					objType: LineObjectType,
					closePath: true
				}, symbols, object));
				break;
			}
			case UnformattedTextObjectType:
			case FormattedTextObjectType:
			case LineTextObjectType: {
				if (symbol.type !== 4) throw new Error("Symbol mismatch: text object with non-text symbol");
				const horizontalAlign = symbol.getHorizontalAlignment();
				const verticalAlign = symbol.getVerticalAlignment();
				const [x, y] = object.coordinates[0];
				const fontSize = symbol.fontSize * 3.52778;
				const lineHeight = fontSize * 1.2;
				const textColor = options.colors[symbol.fontColor];
				const node = {
					type: "text",
					attrs: {
						x: x.toString(),
						y: (-y).toString(),
						fill: textColor.rgb,
						"font-family": symbol.fontName,
						"font-style": symbol.italic ? "italic" : "",
						"font-weight": symbol.weight > 400 ? "bold" : "",
						"font-size": fontSize.toString()
					},
					children: ocadTextToSvg(object.coordinates[0], object.text, horizontalAlign, verticalAlign, lineHeight),
					order: textColor.renderOrder
				};
				nodes.push(node);
				break;
			}
		}
		return nodes;
	};
	var elementToSvg = (symbol, name, index, element, c, angle, options) => {
		let node;
		const translatedCoords = (angle ? element.coords.map((lc) => lc.rotate(angle)) : element.coords).map((lc) => lc.add(c));
		switch (element.type) {
			case LineElementType:
				node = lineToPath(translatedCoords, element.lineWidth, options.colors[element.color], getDashPattern(element.mainGap, element.secGap, element.mainLength, element.endLength, element.endGap), element.flags === 1 ? 1 : 0);
				break;
			case AreaElementType:
				node = areaToPath(translatedCoords, null, options.colors[element.color]);
				break;
			case CircleElementType:
			case DotElementType:
				node = {
					type: "circle",
					attrs: {
						cx: c[0],
						cy: -c[1],
						r: element.diameter / 2
					},
					order: options.colors[element.color].renderOrder
				};
				node.attrs[element.type === CircleElementType ? "stroke" : "fill"] = options.colors[element.color].rgb;
				if (element.type === CircleElementType) node.attrs["stroke-width"] = element.lineWidth;
				break;
		}
		return node;
	};
	/**
	* Create a SVG node definition from a set of line coordinates and line styling.
	*
	* @param {TdPoly[]} coordinates
	* @param {number} width
	* @param {import('./ocad-reader/ocad-file').Color} color
	* @param {string|null} dashPattern
	* @param {number=} lineStyle
	* @param {boolean=} closePath
	* @returns {SvgNodeDef}
	*/
	function lineToPath(coordinates, width, color, dashPattern, lineStyle, closePath) {
		if (width > 0) return {
			type: "path",
			attrs: {
				d: coordsToPath(coordinates, closePath),
				style: `stroke: ${color.rgb}; stroke-width: ${width}; ${dashPattern ? `stroke-dasharray: ${dashPattern};` : ""} stroke-linejoin: ${linejoin(lineStyle)}; stroke-linecap: ${linecap(lineStyle)};`
			},
			order: color.renderOrder
		};
	}
	function getDashPattern(mainGap, secGap, mainLength, endLength, endGap) {
		let dashLength;
		let breakLength;
		let dashesInGroup;
		let inGroupBreakLength;
		if (mainGap || secGap) if (!mainLength) {} else if (secGap && !mainGap) {
			dashLength = mainLength - secGap;
			breakLength = secGap;
			if (endLength) {}
		} else {
			dashLength = mainLength;
			breakLength = mainGap;
			if (endLength && endLength !== mainLength) {
				if (mainLength && endLength / mainLength < .75) {}
				if (Math.abs(mainLength - 2 * endLength) > 1) {}
			}
			if (secGap) {
				dashesInGroup = 2;
				inGroupBreakLength = secGap;
				dashLength = (dashLength - inGroupBreakLength) / 2;
			}
		}
		if (dashLength) if (dashesInGroup) return `${dashLength} ${inGroupBreakLength} ${dashLength} ${breakLength}`;
		else return `${dashLength} ${breakLength}`;
		else return null;
	}
	function linejoin(lineStyle) {
		lineStyle = lineStyle || 0;
		switch (lineStyle) {
			case 0: return "bevel";
			case 1: return "round";
			case 2: return "bevel";
			case 4: return "miter";
			default:
				console.warn(`Unknown line join style ${lineStyle}.`);
				return "";
		}
	}
	function linecap(lineStyle) {
		lineStyle = lineStyle || 0;
		switch (lineStyle) {
			case 0: return "butt";
			case 1: return "round";
			case 2: return "butt";
			case 4: return "butt";
			default:
				console.warn(`Unknown line cap style ${lineStyle}.`);
				return "";
		}
	}
	var areaToPath = (coordinates, fillPattern, color) => ({
		type: "path",
		attrs: {
			d: coordsToPath(coordinates),
			style: `fill: ${fillPattern || color.rgb};`,
			"fill-rule": "evenodd"
		},
		order: color.renderOrder
	});
	/**
	*
	* @param {import('./ocad-reader/td-poly')[]} coords
	* @param {boolean=} closePath
	* @returns {string}
	*/
	var coordsToPath = (coords, closePath) => {
		if (coords.length === 0) return "";
		const cs = [];
		let cp1;
		let cp2;
		let ringStart = coords[0];
		cs.push(`M ${coords[0][0]} ${-coords[0][1]}`);
		for (let i = 0; i < coords.length; i++) {
			const c = coords[i];
			if (c.isFirstBezier()) cp1 = c;
			else if (c.isSecondBezier()) cp2 = c;
			else if (c.isFirstHolePoint()) {
				if (closePath && !isClosed(c, ringStart)) cs.push(`L ${ringStart[0]} ${-ringStart[1]}`);
				ringStart = c;
				cs.push(`M ${c[0]} ${-c[1]}`);
			} else if (cp1 && cp2) {
				const bezier = `C ${cp1[0]} ${-cp1[1]} ${cp2[0]} ${-cp2[1]} ${c[0]} ${-c[1]}`;
				cp1 = cp2 = void 0;
				cs.push(bezier);
			} else if (i > 0) cs.push(`L ${c[0]} ${-c[1]}`);
		}
		if (closePath && !isClosed(coords[coords.length - 1], ringStart)) cs.push(`L ${ringStart[0]} ${-ringStart[1]}`);
		return cs.join(" ");
		function isClosed(cFirst, cLast) {
			return cFirst[0] === cLast[0] && cFirst[1] === cLast[1];
		}
	};
	/**
	* Given an array of `TdPoly`, splits it into LineStrings where there are holes,
	* and offsets each LineString by the specified number of units.
	*
	* @param {TdPoly[]} coordinates
	* @param {number} offset
	* @returns {TdPoly[][]}
	*/
	var offsetLineCoordinates = (coordinates, offset, isArea) => {
		/** @type {TdPoly[][]} */
		const result = [];
		/** @type {TdPoly[]} */
		let current = [];
		for (let i = 0; i < coordinates.length; i++) {
			const c = coordinates[i];
			if (!c.isFirstHolePoint()) current.push(c);
			else {
				result.push(offsetLineString(current));
				current = [c];
			}
		}
		if (current.length > 0) result.push(offsetLineString(current));
		return result;
		/**
		*
		* @param {TdPoly[]} coordinates
		* @returns {TdPoly[]}
		*/
		function offsetLineString(coordinates) {
			return lineOffset({
				type: "LineString",
				coordinates: isArea && coordinates[0].equalCoords(coordinates[coordinates.length - 1]) ? coordinates.slice(0, coordinates.length - 1) : coordinates
			}, offset, { units: "degrees" }).geometry.coordinates.map((c, i) => new TdPoly(c[0], c[1], coordinates[i].xFlags, coordinates[i].yFlags));
		}
	};
	var ocadTextToSvg = (coord, s, horizontalAlign, verticalAlign, lineHeight) => {
		const lines = s.split("\n");
		const baseY = verticalAlign === VerticalAlignTop ? lineHeight : verticalAlign === VerticalAlignBottom ? 0 : .5 * lineHeight;
		return lines.map((l, i) => ({
			type: "tspan",
			attrs: {
				x: coord[0],
				y: `${-coord[1] + baseY + i * lineHeight}`,
				"text-anchor": horizontalAlign === HorizontalAlignCenter ? "middle" : horizontalAlign === HorizontalAlignLeft ? "start" : "end"
			},
			children: [{ text: l }]
		}));
	};
}));
//#endregion
//#region node_modules/ocad2geojson/node_modules/uuid/lib/rng-browser.js
var require_rng_browser = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var getRandomValues = typeof crypto != "undefined" && crypto.getRandomValues && crypto.getRandomValues.bind(crypto) || typeof msCrypto != "undefined" && typeof window.msCrypto.getRandomValues == "function" && msCrypto.getRandomValues.bind(msCrypto);
	if (getRandomValues) {
		var rnds8 = new Uint8Array(16);
		module.exports = function whatwgRNG() {
			getRandomValues(rnds8);
			return rnds8;
		};
	} else {
		var rnds = new Array(16);
		module.exports = function mathRNG() {
			for (var i = 0, r; i < 16; i++) {
				if ((i & 3) === 0) r = Math.random() * 4294967296;
				rnds[i] = r >>> ((i & 3) << 3) & 255;
			}
			return rnds;
		};
	}
}));
//#endregion
//#region node_modules/ocad2geojson/node_modules/uuid/lib/bytesToUuid.js
var require_bytesToUuid = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Convert array of 16 byte values to UUID string format of the form:
	* XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX
	*/
	var byteToHex = [];
	for (var i = 0; i < 256; ++i) byteToHex[i] = (i + 256).toString(16).substr(1);
	function bytesToUuid(buf, offset) {
		var i = offset || 0;
		var bth = byteToHex;
		return [
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]],
			"-",
			bth[buf[i++]],
			bth[buf[i++]],
			"-",
			bth[buf[i++]],
			bth[buf[i++]],
			"-",
			bth[buf[i++]],
			bth[buf[i++]],
			"-",
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]],
			bth[buf[i++]]
		].join("");
	}
	module.exports = bytesToUuid;
}));
//#endregion
//#region node_modules/ocad2geojson/node_modules/uuid/v4.js
var require_v4 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var rng = require_rng_browser();
	var bytesToUuid = require_bytesToUuid();
	function v4(options, buf, offset) {
		var i = buf && offset || 0;
		if (typeof options == "string") {
			buf = options === "binary" ? new Array(16) : null;
			options = null;
		}
		options = options || {};
		var rnds = options.random || (options.rng || rng)();
		rnds[6] = rnds[6] & 15 | 64;
		rnds[8] = rnds[8] & 63 | 128;
		if (buf) for (var ii = 0; ii < 16; ++ii) buf[i + ii] = rnds[ii];
		return buf || bytesToUuid(rnds);
	}
	module.exports = v4;
}));
//#endregion
//#region node_modules/xmldom/lib/entities.js
var require_entities = /* @__PURE__ */ __commonJSMin(((exports) => {
	exports.entityMap = {
		lt: "<",
		gt: ">",
		amp: "&",
		quot: "\"",
		apos: "'",
		Agrave: "À",
		Aacute: "Á",
		Acirc: "Â",
		Atilde: "Ã",
		Auml: "Ä",
		Aring: "Å",
		AElig: "Æ",
		Ccedil: "Ç",
		Egrave: "È",
		Eacute: "É",
		Ecirc: "Ê",
		Euml: "Ë",
		Igrave: "Ì",
		Iacute: "Í",
		Icirc: "Î",
		Iuml: "Ï",
		ETH: "Ð",
		Ntilde: "Ñ",
		Ograve: "Ò",
		Oacute: "Ó",
		Ocirc: "Ô",
		Otilde: "Õ",
		Ouml: "Ö",
		Oslash: "Ø",
		Ugrave: "Ù",
		Uacute: "Ú",
		Ucirc: "Û",
		Uuml: "Ü",
		Yacute: "Ý",
		THORN: "Þ",
		szlig: "ß",
		agrave: "à",
		aacute: "á",
		acirc: "â",
		atilde: "ã",
		auml: "ä",
		aring: "å",
		aelig: "æ",
		ccedil: "ç",
		egrave: "è",
		eacute: "é",
		ecirc: "ê",
		euml: "ë",
		igrave: "ì",
		iacute: "í",
		icirc: "î",
		iuml: "ï",
		eth: "ð",
		ntilde: "ñ",
		ograve: "ò",
		oacute: "ó",
		ocirc: "ô",
		otilde: "õ",
		ouml: "ö",
		oslash: "ø",
		ugrave: "ù",
		uacute: "ú",
		ucirc: "û",
		uuml: "ü",
		yacute: "ý",
		thorn: "þ",
		yuml: "ÿ",
		nbsp: "\xA0",
		iexcl: "¡",
		cent: "¢",
		pound: "£",
		curren: "¤",
		yen: "¥",
		brvbar: "¦",
		sect: "§",
		uml: "¨",
		copy: "©",
		ordf: "ª",
		laquo: "«",
		not: "¬",
		shy: "­­",
		reg: "®",
		macr: "¯",
		deg: "°",
		plusmn: "±",
		sup2: "²",
		sup3: "³",
		acute: "´",
		micro: "µ",
		para: "¶",
		middot: "·",
		cedil: "¸",
		sup1: "¹",
		ordm: "º",
		raquo: "»",
		frac14: "¼",
		frac12: "½",
		frac34: "¾",
		iquest: "¿",
		times: "×",
		divide: "÷",
		forall: "∀",
		part: "∂",
		exist: "∃",
		empty: "∅",
		nabla: "∇",
		isin: "∈",
		notin: "∉",
		ni: "∋",
		prod: "∏",
		sum: "∑",
		minus: "−",
		lowast: "∗",
		radic: "√",
		prop: "∝",
		infin: "∞",
		ang: "∠",
		and: "∧",
		or: "∨",
		cap: "∩",
		cup: "∪",
		"int": "∫",
		there4: "∴",
		sim: "∼",
		cong: "≅",
		asymp: "≈",
		ne: "≠",
		equiv: "≡",
		le: "≤",
		ge: "≥",
		sub: "⊂",
		sup: "⊃",
		nsub: "⊄",
		sube: "⊆",
		supe: "⊇",
		oplus: "⊕",
		otimes: "⊗",
		perp: "⊥",
		sdot: "⋅",
		Alpha: "Α",
		Beta: "Β",
		Gamma: "Γ",
		Delta: "Δ",
		Epsilon: "Ε",
		Zeta: "Ζ",
		Eta: "Η",
		Theta: "Θ",
		Iota: "Ι",
		Kappa: "Κ",
		Lambda: "Λ",
		Mu: "Μ",
		Nu: "Ν",
		Xi: "Ξ",
		Omicron: "Ο",
		Pi: "Π",
		Rho: "Ρ",
		Sigma: "Σ",
		Tau: "Τ",
		Upsilon: "Υ",
		Phi: "Φ",
		Chi: "Χ",
		Psi: "Ψ",
		Omega: "Ω",
		alpha: "α",
		beta: "β",
		gamma: "γ",
		delta: "δ",
		epsilon: "ε",
		zeta: "ζ",
		eta: "η",
		theta: "θ",
		iota: "ι",
		kappa: "κ",
		lambda: "λ",
		mu: "μ",
		nu: "ν",
		xi: "ξ",
		omicron: "ο",
		pi: "π",
		rho: "ρ",
		sigmaf: "ς",
		sigma: "σ",
		tau: "τ",
		upsilon: "υ",
		phi: "φ",
		chi: "χ",
		psi: "ψ",
		omega: "ω",
		thetasym: "ϑ",
		upsih: "ϒ",
		piv: "ϖ",
		OElig: "Œ",
		oelig: "œ",
		Scaron: "Š",
		scaron: "š",
		Yuml: "Ÿ",
		fnof: "ƒ",
		circ: "ˆ",
		tilde: "˜",
		ensp: " ",
		emsp: " ",
		thinsp: " ",
		zwnj: "‌",
		zwj: "‍",
		lrm: "‎",
		rlm: "‏",
		ndash: "–",
		mdash: "—",
		lsquo: "‘",
		rsquo: "’",
		sbquo: "‚",
		ldquo: "“",
		rdquo: "”",
		bdquo: "„",
		dagger: "†",
		Dagger: "‡",
		bull: "•",
		hellip: "…",
		permil: "‰",
		prime: "′",
		Prime: "″",
		lsaquo: "‹",
		rsaquo: "›",
		oline: "‾",
		euro: "€",
		trade: "™",
		larr: "←",
		uarr: "↑",
		rarr: "→",
		darr: "↓",
		harr: "↔",
		crarr: "↵",
		lceil: "⌈",
		rceil: "⌉",
		lfloor: "⌊",
		rfloor: "⌋",
		loz: "◊",
		spades: "♠",
		clubs: "♣",
		hearts: "♥",
		diams: "♦"
	};
}));
//#endregion
//#region node_modules/xmldom/lib/sax.js
var require_sax = /* @__PURE__ */ __commonJSMin(((exports) => {
	var nameStartChar = /[A-Z_a-z\xC0-\xD6\xD8-\xF6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD]/;
	var nameChar = new RegExp("[\\-\\.0-9" + nameStartChar.source.slice(1, -1) + "\\u00B7\\u0300-\\u036F\\u203F-\\u2040]");
	var tagNamePattern = new RegExp("^" + nameStartChar.source + nameChar.source + "*(?::" + nameStartChar.source + nameChar.source + "*)?$");
	var S_TAG = 0;
	var S_ATTR = 1;
	var S_ATTR_SPACE = 2;
	var S_EQ = 3;
	var S_ATTR_NOQUOT_VALUE = 4;
	var S_ATTR_END = 5;
	var S_TAG_SPACE = 6;
	var S_TAG_CLOSE = 7;
	/**
	* Creates an error that will not be caught by XMLReader aka the SAX parser.
	*
	* @param {string} message
	* @param {any?} locator Optional, can provide details about the location in the source
	* @constructor
	*/
	function ParseError(message, locator) {
		this.message = message;
		this.locator = locator;
		if (Error.captureStackTrace) Error.captureStackTrace(this, ParseError);
	}
	ParseError.prototype = /* @__PURE__ */ new Error();
	ParseError.prototype.name = ParseError.name;
	function XMLReader() {}
	XMLReader.prototype = { parse: function(source, defaultNSMap, entityMap) {
		var domBuilder = this.domBuilder;
		domBuilder.startDocument();
		_copy(defaultNSMap, defaultNSMap = {});
		parse(source, defaultNSMap, entityMap, domBuilder, this.errorHandler);
		domBuilder.endDocument();
	} };
	function parse(source, defaultNSMapCopy, entityMap, domBuilder, errorHandler) {
		function fixedFromCharCode(code) {
			if (code > 65535) {
				code -= 65536;
				var surrogate1 = 55296 + (code >> 10), surrogate2 = 56320 + (code & 1023);
				return String.fromCharCode(surrogate1, surrogate2);
			} else return String.fromCharCode(code);
		}
		function entityReplacer(a) {
			var k = a.slice(1, -1);
			if (k in entityMap) return entityMap[k];
			else if (k.charAt(0) === "#") return fixedFromCharCode(parseInt(k.substr(1).replace("x", "0x")));
			else {
				errorHandler.error("entity not found:" + a);
				return a;
			}
		}
		function appendText(end) {
			if (end > start) {
				var xt = source.substring(start, end).replace(/&#?\w+;/g, entityReplacer);
				locator && position(start);
				domBuilder.characters(xt, 0, end - start);
				start = end;
			}
		}
		function position(p, m) {
			while (p >= lineEnd && (m = linePattern.exec(source))) {
				lineStart = m.index;
				lineEnd = lineStart + m[0].length;
				locator.lineNumber++;
			}
			locator.columnNumber = p - lineStart + 1;
		}
		var lineStart = 0;
		var lineEnd = 0;
		var linePattern = /.*(?:\r\n?|\n)|.*$/g;
		var locator = domBuilder.locator;
		var parseStack = [{ currentNSMap: defaultNSMapCopy }];
		var closeMap = {};
		var start = 0;
		while (true) {
			try {
				var tagStart = source.indexOf("<", start);
				if (tagStart < 0) {
					if (!source.substr(start).match(/^\s*$/)) {
						var doc = domBuilder.doc;
						var text = doc.createTextNode(source.substr(start));
						doc.appendChild(text);
						domBuilder.currentElement = text;
					}
					return;
				}
				if (tagStart > start) appendText(tagStart);
				switch (source.charAt(tagStart + 1)) {
					case "/":
						var end = source.indexOf(">", tagStart + 3);
						var tagName = source.substring(tagStart + 2, end);
						var config = parseStack.pop();
						if (end < 0) {
							tagName = source.substring(tagStart + 2).replace(/[\s<].*/, "");
							errorHandler.error("end tag name: " + tagName + " is not complete:" + config.tagName);
							end = tagStart + 1 + tagName.length;
						} else if (tagName.match(/\s</)) {
							tagName = tagName.replace(/[\s<].*/, "");
							errorHandler.error("end tag name: " + tagName + " maybe not complete");
							end = tagStart + 1 + tagName.length;
						}
						var localNSMap = config.localNSMap;
						var endMatch = config.tagName == tagName;
						if (endMatch || config.tagName && config.tagName.toLowerCase() == tagName.toLowerCase()) {
							domBuilder.endElement(config.uri, config.localName, tagName);
							if (localNSMap) for (var prefix in localNSMap) domBuilder.endPrefixMapping(prefix);
							if (!endMatch) errorHandler.fatalError("end tag name: " + tagName + " is not match the current start tagName:" + config.tagName);
						} else parseStack.push(config);
						end++;
						break;
					case "?":
						locator && position(tagStart);
						end = parseInstruction(source, tagStart, domBuilder);
						break;
					case "!":
						locator && position(tagStart);
						end = parseDCC(source, tagStart, domBuilder, errorHandler);
						break;
					default:
						locator && position(tagStart);
						var el = new ElementAttributes();
						var currentNSMap = parseStack[parseStack.length - 1].currentNSMap;
						var end = parseElementStartPart(source, tagStart, el, currentNSMap, entityReplacer, errorHandler);
						var len = el.length;
						if (!el.closed && fixSelfClosed(source, end, el.tagName, closeMap)) {
							el.closed = true;
							if (!entityMap.nbsp) errorHandler.warning("unclosed xml attribute");
						}
						if (locator && len) {
							var locator2 = copyLocator(locator, {});
							for (var i = 0; i < len; i++) {
								var a = el[i];
								position(a.offset);
								a.locator = copyLocator(locator, {});
							}
							domBuilder.locator = locator2;
							if (appendElement(el, domBuilder, currentNSMap)) parseStack.push(el);
							domBuilder.locator = locator;
						} else if (appendElement(el, domBuilder, currentNSMap)) parseStack.push(el);
						if (el.uri === "http://www.w3.org/1999/xhtml" && !el.closed) end = parseHtmlSpecialContent(source, end, el.tagName, entityReplacer, domBuilder);
						else end++;
				}
			} catch (e) {
				if (e instanceof ParseError) throw e;
				errorHandler.error("element parse error: " + e);
				end = -1;
			}
			if (end > start) start = end;
			else appendText(Math.max(tagStart, start) + 1);
		}
	}
	function copyLocator(f, t) {
		t.lineNumber = f.lineNumber;
		t.columnNumber = f.columnNumber;
		return t;
	}
	/**
	* @see #appendElement(source,elStartEnd,el,selfClosed,entityReplacer,domBuilder,parseStack);
	* @return end of the elementStartPart(end of elementEndPart for selfClosed el)
	*/
	function parseElementStartPart(source, start, el, currentNSMap, entityReplacer, errorHandler) {
		/**
		* @param {string} qname
		* @param {string} value
		* @param {number} startIndex
		*/
		function addAttribute(qname, value, startIndex) {
			if (qname in el.attributeNames) errorHandler.fatalError("Attribute " + qname + " redefined");
			el.addValue(qname, value, startIndex);
		}
		var attrName;
		var value;
		var p = ++start;
		var s = S_TAG;
		while (true) {
			var c = source.charAt(p);
			switch (c) {
				case "=":
					if (s === S_ATTR) {
						attrName = source.slice(start, p);
						s = S_EQ;
					} else if (s === S_ATTR_SPACE) s = S_EQ;
					else throw new Error("attribute equal must after attrName");
					break;
				case "'":
				case "\"":
					if (s === S_EQ || s === S_ATTR) {
						if (s === S_ATTR) {
							errorHandler.warning("attribute value must after \"=\"");
							attrName = source.slice(start, p);
						}
						start = p + 1;
						p = source.indexOf(c, start);
						if (p > 0) {
							value = source.slice(start, p).replace(/&#?\w+;/g, entityReplacer);
							addAttribute(attrName, value, start - 1);
							s = S_ATTR_END;
						} else throw new Error("attribute value no end '" + c + "' match");
					} else if (s == S_ATTR_NOQUOT_VALUE) {
						value = source.slice(start, p).replace(/&#?\w+;/g, entityReplacer);
						addAttribute(attrName, value, start);
						errorHandler.warning("attribute \"" + attrName + "\" missed start quot(" + c + ")!!");
						start = p + 1;
						s = S_ATTR_END;
					} else throw new Error("attribute value must after \"=\"");
					break;
				case "/":
					switch (s) {
						case S_TAG: el.setTagName(source.slice(start, p));
						case S_ATTR_END:
						case S_TAG_SPACE:
						case S_TAG_CLOSE:
							s = S_TAG_CLOSE;
							el.closed = true;
						case S_ATTR_NOQUOT_VALUE:
						case S_ATTR:
						case S_ATTR_SPACE: break;
						default: throw new Error("attribute invalid close char('/')");
					}
					break;
				case "":
					errorHandler.error("unexpected end of input");
					if (s == S_TAG) el.setTagName(source.slice(start, p));
					return p;
				case ">":
					switch (s) {
						case S_TAG: el.setTagName(source.slice(start, p));
						case S_ATTR_END:
						case S_TAG_SPACE:
						case S_TAG_CLOSE: break;
						case S_ATTR_NOQUOT_VALUE:
						case S_ATTR:
							value = source.slice(start, p);
							if (value.slice(-1) === "/") {
								el.closed = true;
								value = value.slice(0, -1);
							}
						case S_ATTR_SPACE:
							if (s === S_ATTR_SPACE) value = attrName;
							if (s == S_ATTR_NOQUOT_VALUE) {
								errorHandler.warning("attribute \"" + value + "\" missed quot(\")!");
								addAttribute(attrName, value.replace(/&#?\w+;/g, entityReplacer), start);
							} else {
								if (currentNSMap[""] !== "http://www.w3.org/1999/xhtml" || !value.match(/^(?:disabled|checked|selected)$/i)) errorHandler.warning("attribute \"" + value + "\" missed value!! \"" + value + "\" instead!!");
								addAttribute(value, value, start);
							}
							break;
						case S_EQ: throw new Error("attribute value missed!!");
					}
					return p;
				case "": c = " ";
				default: if (c <= " ") switch (s) {
					case S_TAG:
						el.setTagName(source.slice(start, p));
						s = S_TAG_SPACE;
						break;
					case S_ATTR:
						attrName = source.slice(start, p);
						s = S_ATTR_SPACE;
						break;
					case S_ATTR_NOQUOT_VALUE:
						var value = source.slice(start, p).replace(/&#?\w+;/g, entityReplacer);
						errorHandler.warning("attribute \"" + value + "\" missed quot(\")!!");
						addAttribute(attrName, value, start);
					case S_ATTR_END:
						s = S_TAG_SPACE;
						break;
				}
				else switch (s) {
					case S_ATTR_SPACE:
						el.tagName;
						if (currentNSMap[""] !== "http://www.w3.org/1999/xhtml" || !attrName.match(/^(?:disabled|checked|selected)$/i)) errorHandler.warning("attribute \"" + attrName + "\" missed value!! \"" + attrName + "\" instead2!!");
						addAttribute(attrName, attrName, start);
						start = p;
						s = S_ATTR;
						break;
					case S_ATTR_END: errorHandler.warning("attribute space is required\"" + attrName + "\"!!");
					case S_TAG_SPACE:
						s = S_ATTR;
						start = p;
						break;
					case S_EQ:
						s = S_ATTR_NOQUOT_VALUE;
						start = p;
						break;
					case S_TAG_CLOSE: throw new Error("elements closed character '/' and '>' must be connected to");
				}
			}
			p++;
		}
	}
	/**
	* @return true if has new namespace define
	*/
	function appendElement(el, domBuilder, currentNSMap) {
		var tagName = el.tagName;
		var localNSMap = null;
		var i = el.length;
		while (i--) {
			var a = el[i];
			var qName = a.qName;
			var value = a.value;
			var nsp = qName.indexOf(":");
			if (nsp > 0) {
				var prefix = a.prefix = qName.slice(0, nsp);
				var localName = qName.slice(nsp + 1);
				var nsPrefix = prefix === "xmlns" && localName;
			} else {
				localName = qName;
				prefix = null;
				nsPrefix = qName === "xmlns" && "";
			}
			a.localName = localName;
			if (nsPrefix !== false) {
				if (localNSMap == null) {
					localNSMap = {};
					_copy(currentNSMap, currentNSMap = {});
				}
				currentNSMap[nsPrefix] = localNSMap[nsPrefix] = value;
				a.uri = "http://www.w3.org/2000/xmlns/";
				domBuilder.startPrefixMapping(nsPrefix, value);
			}
		}
		var i = el.length;
		while (i--) {
			a = el[i];
			var prefix = a.prefix;
			if (prefix) {
				if (prefix === "xml") a.uri = "http://www.w3.org/XML/1998/namespace";
				if (prefix !== "xmlns") a.uri = currentNSMap[prefix || ""];
			}
		}
		var nsp = tagName.indexOf(":");
		if (nsp > 0) {
			prefix = el.prefix = tagName.slice(0, nsp);
			localName = el.localName = tagName.slice(nsp + 1);
		} else {
			prefix = null;
			localName = el.localName = tagName;
		}
		var ns = el.uri = currentNSMap[prefix || ""];
		domBuilder.startElement(ns, localName, tagName, el);
		if (el.closed) {
			domBuilder.endElement(ns, localName, tagName);
			if (localNSMap) for (prefix in localNSMap) domBuilder.endPrefixMapping(prefix);
		} else {
			el.currentNSMap = currentNSMap;
			el.localNSMap = localNSMap;
			return true;
		}
	}
	function parseHtmlSpecialContent(source, elStartEnd, tagName, entityReplacer, domBuilder) {
		if (/^(?:script|textarea)$/i.test(tagName)) {
			var elEndStart = source.indexOf("</" + tagName + ">", elStartEnd);
			var text = source.substring(elStartEnd + 1, elEndStart);
			if (/[&<]/.test(text)) {
				if (/^script$/i.test(tagName)) {
					domBuilder.characters(text, 0, text.length);
					return elEndStart;
				}
				text = text.replace(/&#?\w+;/g, entityReplacer);
				domBuilder.characters(text, 0, text.length);
				return elEndStart;
			}
		}
		return elStartEnd + 1;
	}
	function fixSelfClosed(source, elStartEnd, tagName, closeMap) {
		var pos = closeMap[tagName];
		if (pos == null) {
			pos = source.lastIndexOf("</" + tagName + ">");
			if (pos < elStartEnd) pos = source.lastIndexOf("</" + tagName);
			closeMap[tagName] = pos;
		}
		return pos < elStartEnd;
	}
	function _copy(source, target) {
		for (var n in source) target[n] = source[n];
	}
	function parseDCC(source, start, domBuilder, errorHandler) {
		switch (source.charAt(start + 2)) {
			case "-": if (source.charAt(start + 3) === "-") {
				var end = source.indexOf("-->", start + 4);
				if (end > start) {
					domBuilder.comment(source, start + 4, end - start - 4);
					return end + 3;
				} else {
					errorHandler.error("Unclosed comment");
					return -1;
				}
			} else return -1;
			default:
				if (source.substr(start + 3, 6) == "CDATA[") {
					var end = source.indexOf("]]>", start + 9);
					domBuilder.startCDATA();
					domBuilder.characters(source, start + 9, end - start - 9);
					domBuilder.endCDATA();
					return end + 3;
				}
				var matchs = split(source, start);
				var len = matchs.length;
				if (len > 1 && /!doctype/i.test(matchs[0][0])) {
					var name = matchs[1][0];
					var pubid = false;
					var sysid = false;
					if (len > 3) {
						if (/^public$/i.test(matchs[2][0])) {
							pubid = matchs[3][0];
							sysid = len > 4 && matchs[4][0];
						} else if (/^system$/i.test(matchs[2][0])) sysid = matchs[3][0];
					}
					var lastMatch = matchs[len - 1];
					domBuilder.startDTD(name, pubid, sysid);
					domBuilder.endDTD();
					return lastMatch.index + lastMatch[0].length;
				}
		}
		return -1;
	}
	function parseInstruction(source, start, domBuilder) {
		var end = source.indexOf("?>", start);
		if (end) {
			var match = source.substring(start, end).match(/^<\?(\S*)\s*([\s\S]*?)\s*$/);
			if (match) {
				match[0].length;
				domBuilder.processingInstruction(match[1], match[2]);
				return end + 2;
			} else return -1;
		}
		return -1;
	}
	function ElementAttributes() {
		this.attributeNames = {};
	}
	ElementAttributes.prototype = {
		setTagName: function(tagName) {
			if (!tagNamePattern.test(tagName)) throw new Error("invalid tagName:" + tagName);
			this.tagName = tagName;
		},
		addValue: function(qName, value, offset) {
			if (!tagNamePattern.test(qName)) throw new Error("invalid attribute:" + qName);
			this.attributeNames[qName] = this.length;
			this[this.length++] = {
				qName,
				value,
				offset
			};
		},
		length: 0,
		getLocalName: function(i) {
			return this[i].localName;
		},
		getLocator: function(i) {
			return this[i].locator;
		},
		getQName: function(i) {
			return this[i].qName;
		},
		getURI: function(i) {
			return this[i].uri;
		},
		getValue: function(i) {
			return this[i].value;
		}
	};
	function split(source, start) {
		var match;
		var buf = [];
		var reg = /'[^']+'|"[^"]+"|[^\s<>\/=]+=?|(\/?\s*>|<)/g;
		reg.lastIndex = start;
		reg.exec(source);
		while (match = reg.exec(source)) {
			buf.push(match);
			if (match[1]) return buf;
		}
	}
	exports.XMLReader = XMLReader;
	exports.ParseError = ParseError;
}));
//#endregion
//#region node_modules/xmldom/lib/dom.js
var require_dom = /* @__PURE__ */ __commonJSMin(((exports) => {
	function copy(src, dest) {
		for (var p in src) dest[p] = src[p];
	}
	/**
	^\w+\.prototype\.([_\w]+)\s*=\s*((?:.*\{\s*?[\r\n][\s\S]*?^})|\S.*?(?=[;\r\n]));?
	^\w+\.prototype\.([_\w]+)\s*=\s*(\S.*?(?=[;\r\n]));?
	*/
	function _extends(Class, Super) {
		var pt = Class.prototype;
		if (!(pt instanceof Super)) {
			function t() {}
			t.prototype = Super.prototype;
			t = new t();
			copy(pt, t);
			Class.prototype = pt = t;
		}
		if (pt.constructor != Class) {
			if (typeof Class != "function") console.error("unknow Class:" + Class);
			pt.constructor = Class;
		}
	}
	var htmlns = "http://www.w3.org/1999/xhtml";
	var NodeType = {};
	var ELEMENT_NODE = NodeType.ELEMENT_NODE = 1;
	var ATTRIBUTE_NODE = NodeType.ATTRIBUTE_NODE = 2;
	var TEXT_NODE = NodeType.TEXT_NODE = 3;
	var CDATA_SECTION_NODE = NodeType.CDATA_SECTION_NODE = 4;
	var ENTITY_REFERENCE_NODE = NodeType.ENTITY_REFERENCE_NODE = 5;
	var ENTITY_NODE = NodeType.ENTITY_NODE = 6;
	var PROCESSING_INSTRUCTION_NODE = NodeType.PROCESSING_INSTRUCTION_NODE = 7;
	var COMMENT_NODE = NodeType.COMMENT_NODE = 8;
	var DOCUMENT_NODE = NodeType.DOCUMENT_NODE = 9;
	var DOCUMENT_TYPE_NODE = NodeType.DOCUMENT_TYPE_NODE = 10;
	var DOCUMENT_FRAGMENT_NODE = NodeType.DOCUMENT_FRAGMENT_NODE = 11;
	var NOTATION_NODE = NodeType.NOTATION_NODE = 12;
	var ExceptionCode = {};
	var ExceptionMessage = {};
	ExceptionCode.INDEX_SIZE_ERR = (ExceptionMessage[1] = "Index size error", 1);
	ExceptionCode.DOMSTRING_SIZE_ERR = (ExceptionMessage[2] = "DOMString size error", 2);
	var HIERARCHY_REQUEST_ERR = ExceptionCode.HIERARCHY_REQUEST_ERR = (ExceptionMessage[3] = "Hierarchy request error", 3);
	ExceptionCode.WRONG_DOCUMENT_ERR = (ExceptionMessage[4] = "Wrong document", 4);
	ExceptionCode.INVALID_CHARACTER_ERR = (ExceptionMessage[5] = "Invalid character", 5);
	ExceptionCode.NO_DATA_ALLOWED_ERR = (ExceptionMessage[6] = "No data allowed", 6);
	ExceptionCode.NO_MODIFICATION_ALLOWED_ERR = (ExceptionMessage[7] = "No modification allowed", 7);
	var NOT_FOUND_ERR = ExceptionCode.NOT_FOUND_ERR = (ExceptionMessage[8] = "Not found", 8);
	ExceptionCode.NOT_SUPPORTED_ERR = (ExceptionMessage[9] = "Not supported", 9);
	var INUSE_ATTRIBUTE_ERR = ExceptionCode.INUSE_ATTRIBUTE_ERR = (ExceptionMessage[10] = "Attribute in use", 10);
	ExceptionCode.INVALID_STATE_ERR = (ExceptionMessage[11] = "Invalid state", 11);
	ExceptionCode.SYNTAX_ERR = (ExceptionMessage[12] = "Syntax error", 12);
	ExceptionCode.INVALID_MODIFICATION_ERR = (ExceptionMessage[13] = "Invalid modification", 13);
	ExceptionCode.NAMESPACE_ERR = (ExceptionMessage[14] = "Invalid namespace", 14);
	ExceptionCode.INVALID_ACCESS_ERR = (ExceptionMessage[15] = "Invalid access", 15);
	/**
	* DOM Level 2
	* Object DOMException
	* @see http://www.w3.org/TR/2000/REC-DOM-Level-2-Core-20001113/ecma-script-binding.html
	* @see http://www.w3.org/TR/REC-DOM-Level-1/ecma-script-language-binding.html
	*/
	function DOMException(code, message) {
		if (message instanceof Error) var error = message;
		else {
			error = this;
			Error.call(this, ExceptionMessage[code]);
			this.message = ExceptionMessage[code];
			if (Error.captureStackTrace) Error.captureStackTrace(this, DOMException);
		}
		error.code = code;
		if (message) this.message = this.message + ": " + message;
		return error;
	}
	DOMException.prototype = Error.prototype;
	copy(ExceptionCode, DOMException);
	/**
	* @see http://www.w3.org/TR/2000/REC-DOM-Level-2-Core-20001113/core.html#ID-536297177
	* The NodeList interface provides the abstraction of an ordered collection of nodes, without defining or constraining how this collection is implemented. NodeList objects in the DOM are live.
	* The items in the NodeList are accessible via an integral index, starting from 0.
	*/
	function NodeList() {}
	NodeList.prototype = {
		/**
		* The number of nodes in the list. The range of valid child node indices is 0 to length-1 inclusive.
		* @standard level1
		*/
		length: 0,
		/**
		* Returns the indexth item in the collection. If index is greater than or equal to the number of nodes in the list, this returns null.
		* @standard level1
		* @param index  unsigned long 
		*   Index into the collection.
		* @return Node
		* 	The node at the indexth position in the NodeList, or null if that is not a valid index. 
		*/
		item: function(index) {
			return this[index] || null;
		},
		toString: function(isHTML, nodeFilter) {
			for (var buf = [], i = 0; i < this.length; i++) serializeToString(this[i], buf, isHTML, nodeFilter);
			return buf.join("");
		}
	};
	function LiveNodeList(node, refresh) {
		this._node = node;
		this._refresh = refresh;
		_updateLiveList(this);
	}
	function _updateLiveList(list) {
		var inc = list._node._inc || list._node.ownerDocument._inc;
		if (list._inc != inc) {
			var ls = list._refresh(list._node);
			__set__(list, "length", ls.length);
			copy(ls, list);
			list._inc = inc;
		}
	}
	LiveNodeList.prototype.item = function(i) {
		_updateLiveList(this);
		return this[i];
	};
	_extends(LiveNodeList, NodeList);
	/**
	* 
	* Objects implementing the NamedNodeMap interface are used to represent collections of nodes that can be accessed by name. Note that NamedNodeMap does not inherit from NodeList; NamedNodeMaps are not maintained in any particular order. Objects contained in an object implementing NamedNodeMap may also be accessed by an ordinal index, but this is simply to allow convenient enumeration of the contents of a NamedNodeMap, and does not imply that the DOM specifies an order to these Nodes.
	* NamedNodeMap objects in the DOM are live.
	* used for attributes or DocumentType entities 
	*/
	function NamedNodeMap() {}
	function _findNodeIndex(list, node) {
		var i = list.length;
		while (i--) if (list[i] === node) return i;
	}
	function _addNamedNode(el, list, newAttr, oldAttr) {
		if (oldAttr) list[_findNodeIndex(list, oldAttr)] = newAttr;
		else list[list.length++] = newAttr;
		if (el) {
			newAttr.ownerElement = el;
			var doc = el.ownerDocument;
			if (doc) {
				oldAttr && _onRemoveAttribute(doc, el, oldAttr);
				_onAddAttribute(doc, el, newAttr);
			}
		}
	}
	function _removeNamedNode(el, list, attr) {
		var i = _findNodeIndex(list, attr);
		if (i >= 0) {
			var lastIndex = list.length - 1;
			while (i < lastIndex) list[i] = list[++i];
			list.length = lastIndex;
			if (el) {
				var doc = el.ownerDocument;
				if (doc) {
					_onRemoveAttribute(doc, el, attr);
					attr.ownerElement = null;
				}
			}
		} else throw DOMException(NOT_FOUND_ERR, /* @__PURE__ */ new Error(el.tagName + "@" + attr));
	}
	NamedNodeMap.prototype = {
		length: 0,
		item: NodeList.prototype.item,
		getNamedItem: function(key) {
			var i = this.length;
			while (i--) {
				var attr = this[i];
				if (attr.nodeName == key) return attr;
			}
		},
		setNamedItem: function(attr) {
			var el = attr.ownerElement;
			if (el && el != this._ownerElement) throw new DOMException(INUSE_ATTRIBUTE_ERR);
			var oldAttr = this.getNamedItem(attr.nodeName);
			_addNamedNode(this._ownerElement, this, attr, oldAttr);
			return oldAttr;
		},
		setNamedItemNS: function(attr) {
			var el = attr.ownerElement, oldAttr;
			if (el && el != this._ownerElement) throw new DOMException(INUSE_ATTRIBUTE_ERR);
			oldAttr = this.getNamedItemNS(attr.namespaceURI, attr.localName);
			_addNamedNode(this._ownerElement, this, attr, oldAttr);
			return oldAttr;
		},
		removeNamedItem: function(key) {
			var attr = this.getNamedItem(key);
			_removeNamedNode(this._ownerElement, this, attr);
			return attr;
		},
		removeNamedItemNS: function(namespaceURI, localName) {
			var attr = this.getNamedItemNS(namespaceURI, localName);
			_removeNamedNode(this._ownerElement, this, attr);
			return attr;
		},
		getNamedItemNS: function(namespaceURI, localName) {
			var i = this.length;
			while (i--) {
				var node = this[i];
				if (node.localName == localName && node.namespaceURI == namespaceURI) return node;
			}
			return null;
		}
	};
	/**
	* @see http://www.w3.org/TR/REC-DOM-Level-1/level-one-core.html#ID-102161490
	*/
	function DOMImplementation(features) {
		this._features = {};
		if (features) for (var feature in features) this._features = features[feature];
	}
	DOMImplementation.prototype = {
		hasFeature: function(feature, version) {
			var versions = this._features[feature.toLowerCase()];
			if (versions && (!version || version in versions)) return true;
			else return false;
		},
		createDocument: function(namespaceURI, qualifiedName, doctype) {
			var doc = new Document();
			doc.implementation = this;
			doc.childNodes = new NodeList();
			doc.doctype = doctype;
			if (doctype) doc.appendChild(doctype);
			if (qualifiedName) {
				var root = doc.createElementNS(namespaceURI, qualifiedName);
				doc.appendChild(root);
			}
			return doc;
		},
		createDocumentType: function(qualifiedName, publicId, systemId) {
			var node = new DocumentType();
			node.name = qualifiedName;
			node.nodeName = qualifiedName;
			node.publicId = publicId;
			node.systemId = systemId;
			return node;
		}
	};
	/**
	* @see http://www.w3.org/TR/2000/REC-DOM-Level-2-Core-20001113/core.html#ID-1950641247
	*/
	function Node() {}
	Node.prototype = {
		firstChild: null,
		lastChild: null,
		previousSibling: null,
		nextSibling: null,
		attributes: null,
		parentNode: null,
		childNodes: null,
		ownerDocument: null,
		nodeValue: null,
		namespaceURI: null,
		prefix: null,
		localName: null,
		insertBefore: function(newChild, refChild) {
			return _insertBefore(this, newChild, refChild);
		},
		replaceChild: function(newChild, oldChild) {
			this.insertBefore(newChild, oldChild);
			if (oldChild) this.removeChild(oldChild);
		},
		removeChild: function(oldChild) {
			return _removeChild(this, oldChild);
		},
		appendChild: function(newChild) {
			return this.insertBefore(newChild, null);
		},
		hasChildNodes: function() {
			return this.firstChild != null;
		},
		cloneNode: function(deep) {
			return cloneNode(this.ownerDocument || this, this, deep);
		},
		normalize: function() {
			var child = this.firstChild;
			while (child) {
				var next = child.nextSibling;
				if (next && next.nodeType == TEXT_NODE && child.nodeType == TEXT_NODE) {
					this.removeChild(next);
					child.appendData(next.data);
				} else {
					child.normalize();
					child = next;
				}
			}
		},
		isSupported: function(feature, version) {
			return this.ownerDocument.implementation.hasFeature(feature, version);
		},
		hasAttributes: function() {
			return this.attributes.length > 0;
		},
		lookupPrefix: function(namespaceURI) {
			var el = this;
			while (el) {
				var map = el._nsMap;
				if (map) {
					for (var n in map) if (map[n] == namespaceURI) return n;
				}
				el = el.nodeType == ATTRIBUTE_NODE ? el.ownerDocument : el.parentNode;
			}
			return null;
		},
		lookupNamespaceURI: function(prefix) {
			var el = this;
			while (el) {
				var map = el._nsMap;
				if (map) {
					if (prefix in map) return map[prefix];
				}
				el = el.nodeType == ATTRIBUTE_NODE ? el.ownerDocument : el.parentNode;
			}
			return null;
		},
		isDefaultNamespace: function(namespaceURI) {
			return this.lookupPrefix(namespaceURI) == null;
		}
	};
	function _xmlEncoder(c) {
		return c == "<" && "&lt;" || c == ">" && "&gt;" || c == "&" && "&amp;" || c == "\"" && "&quot;" || "&#" + c.charCodeAt() + ";";
	}
	copy(NodeType, Node);
	copy(NodeType, Node.prototype);
	/**
	* @param callback return true for continue,false for break
	* @return boolean true: break visit;
	*/
	function _visitNode(node, callback) {
		if (callback(node)) return true;
		if (node = node.firstChild) do
			if (_visitNode(node, callback)) return true;
		while (node = node.nextSibling);
	}
	function Document() {}
	function _onAddAttribute(doc, el, newAttr) {
		doc && doc._inc++;
		if (newAttr.namespaceURI == "http://www.w3.org/2000/xmlns/") el._nsMap[newAttr.prefix ? newAttr.localName : ""] = newAttr.value;
	}
	function _onRemoveAttribute(doc, el, newAttr, remove) {
		doc && doc._inc++;
		if (newAttr.namespaceURI == "http://www.w3.org/2000/xmlns/") delete el._nsMap[newAttr.prefix ? newAttr.localName : ""];
	}
	function _onUpdateChild(doc, el, newChild) {
		if (doc && doc._inc) {
			doc._inc++;
			var cs = el.childNodes;
			if (newChild) cs[cs.length++] = newChild;
			else {
				var child = el.firstChild;
				var i = 0;
				while (child) {
					cs[i++] = child;
					child = child.nextSibling;
				}
				cs.length = i;
			}
		}
	}
	/**
	* attributes;
	* children;
	* 
	* writeable properties:
	* nodeValue,Attr:value,CharacterData:data
	* prefix
	*/
	function _removeChild(parentNode, child) {
		var previous = child.previousSibling;
		var next = child.nextSibling;
		if (previous) previous.nextSibling = next;
		else parentNode.firstChild = next;
		if (next) next.previousSibling = previous;
		else parentNode.lastChild = previous;
		_onUpdateChild(parentNode.ownerDocument, parentNode);
		return child;
	}
	/**
	* preformance key(refChild == null)
	*/
	function _insertBefore(parentNode, newChild, nextChild) {
		var cp = newChild.parentNode;
		if (cp) cp.removeChild(newChild);
		if (newChild.nodeType === DOCUMENT_FRAGMENT_NODE) {
			var newFirst = newChild.firstChild;
			if (newFirst == null) return newChild;
			var newLast = newChild.lastChild;
		} else newFirst = newLast = newChild;
		var pre = nextChild ? nextChild.previousSibling : parentNode.lastChild;
		newFirst.previousSibling = pre;
		newLast.nextSibling = nextChild;
		if (pre) pre.nextSibling = newFirst;
		else parentNode.firstChild = newFirst;
		if (nextChild == null) parentNode.lastChild = newLast;
		else nextChild.previousSibling = newLast;
		do
			newFirst.parentNode = parentNode;
		while (newFirst !== newLast && (newFirst = newFirst.nextSibling));
		_onUpdateChild(parentNode.ownerDocument || parentNode, parentNode);
		if (newChild.nodeType == DOCUMENT_FRAGMENT_NODE) newChild.firstChild = newChild.lastChild = null;
		return newChild;
	}
	function _appendSingleChild(parentNode, newChild) {
		var cp = newChild.parentNode;
		if (cp) {
			var pre = parentNode.lastChild;
			cp.removeChild(newChild);
			var pre = parentNode.lastChild;
		}
		var pre = parentNode.lastChild;
		newChild.parentNode = parentNode;
		newChild.previousSibling = pre;
		newChild.nextSibling = null;
		if (pre) pre.nextSibling = newChild;
		else parentNode.firstChild = newChild;
		parentNode.lastChild = newChild;
		_onUpdateChild(parentNode.ownerDocument, parentNode, newChild);
		return newChild;
	}
	Document.prototype = {
		nodeName: "#document",
		nodeType: DOCUMENT_NODE,
		doctype: null,
		documentElement: null,
		_inc: 1,
		insertBefore: function(newChild, refChild) {
			if (newChild.nodeType == DOCUMENT_FRAGMENT_NODE) {
				var child = newChild.firstChild;
				while (child) {
					var next = child.nextSibling;
					this.insertBefore(child, refChild);
					child = next;
				}
				return newChild;
			}
			if (this.documentElement == null && newChild.nodeType == ELEMENT_NODE) this.documentElement = newChild;
			return _insertBefore(this, newChild, refChild), newChild.ownerDocument = this, newChild;
		},
		removeChild: function(oldChild) {
			if (this.documentElement == oldChild) this.documentElement = null;
			return _removeChild(this, oldChild);
		},
		importNode: function(importedNode, deep) {
			return importNode(this, importedNode, deep);
		},
		getElementById: function(id) {
			var rtv = null;
			_visitNode(this.documentElement, function(node) {
				if (node.nodeType == ELEMENT_NODE) {
					if (node.getAttribute("id") == id) {
						rtv = node;
						return true;
					}
				}
			});
			return rtv;
		},
		getElementsByClassName: function(className) {
			var pattern = new RegExp("(^|\\s)" + className + "(\\s|$)");
			return new LiveNodeList(this, function(base) {
				var ls = [];
				_visitNode(base.documentElement, function(node) {
					if (node !== base && node.nodeType == ELEMENT_NODE) {
						if (pattern.test(node.getAttribute("class"))) ls.push(node);
					}
				});
				return ls;
			});
		},
		createElement: function(tagName) {
			var node = new Element();
			node.ownerDocument = this;
			node.nodeName = tagName;
			node.tagName = tagName;
			node.childNodes = new NodeList();
			var attrs = node.attributes = new NamedNodeMap();
			attrs._ownerElement = node;
			return node;
		},
		createDocumentFragment: function() {
			var node = new DocumentFragment();
			node.ownerDocument = this;
			node.childNodes = new NodeList();
			return node;
		},
		createTextNode: function(data) {
			var node = new Text();
			node.ownerDocument = this;
			node.appendData(data);
			return node;
		},
		createComment: function(data) {
			var node = new Comment();
			node.ownerDocument = this;
			node.appendData(data);
			return node;
		},
		createCDATASection: function(data) {
			var node = new CDATASection();
			node.ownerDocument = this;
			node.appendData(data);
			return node;
		},
		createProcessingInstruction: function(target, data) {
			var node = new ProcessingInstruction();
			node.ownerDocument = this;
			node.tagName = node.target = target;
			node.nodeValue = node.data = data;
			return node;
		},
		createAttribute: function(name) {
			var node = new Attr();
			node.ownerDocument = this;
			node.name = name;
			node.nodeName = name;
			node.localName = name;
			node.specified = true;
			return node;
		},
		createEntityReference: function(name) {
			var node = new EntityReference();
			node.ownerDocument = this;
			node.nodeName = name;
			return node;
		},
		createElementNS: function(namespaceURI, qualifiedName) {
			var node = new Element();
			var pl = qualifiedName.split(":");
			var attrs = node.attributes = new NamedNodeMap();
			node.childNodes = new NodeList();
			node.ownerDocument = this;
			node.nodeName = qualifiedName;
			node.tagName = qualifiedName;
			node.namespaceURI = namespaceURI;
			if (pl.length == 2) {
				node.prefix = pl[0];
				node.localName = pl[1];
			} else node.localName = qualifiedName;
			attrs._ownerElement = node;
			return node;
		},
		createAttributeNS: function(namespaceURI, qualifiedName) {
			var node = new Attr();
			var pl = qualifiedName.split(":");
			node.ownerDocument = this;
			node.nodeName = qualifiedName;
			node.name = qualifiedName;
			node.namespaceURI = namespaceURI;
			node.specified = true;
			if (pl.length == 2) {
				node.prefix = pl[0];
				node.localName = pl[1];
			} else node.localName = qualifiedName;
			return node;
		}
	};
	_extends(Document, Node);
	function Element() {
		this._nsMap = {};
	}
	Element.prototype = {
		nodeType: ELEMENT_NODE,
		hasAttribute: function(name) {
			return this.getAttributeNode(name) != null;
		},
		getAttribute: function(name) {
			var attr = this.getAttributeNode(name);
			return attr && attr.value || "";
		},
		getAttributeNode: function(name) {
			return this.attributes.getNamedItem(name);
		},
		setAttribute: function(name, value) {
			var attr = this.ownerDocument.createAttribute(name);
			attr.value = attr.nodeValue = "" + value;
			this.setAttributeNode(attr);
		},
		removeAttribute: function(name) {
			var attr = this.getAttributeNode(name);
			attr && this.removeAttributeNode(attr);
		},
		appendChild: function(newChild) {
			if (newChild.nodeType === DOCUMENT_FRAGMENT_NODE) return this.insertBefore(newChild, null);
			else return _appendSingleChild(this, newChild);
		},
		setAttributeNode: function(newAttr) {
			return this.attributes.setNamedItem(newAttr);
		},
		setAttributeNodeNS: function(newAttr) {
			return this.attributes.setNamedItemNS(newAttr);
		},
		removeAttributeNode: function(oldAttr) {
			return this.attributes.removeNamedItem(oldAttr.nodeName);
		},
		removeAttributeNS: function(namespaceURI, localName) {
			var old = this.getAttributeNodeNS(namespaceURI, localName);
			old && this.removeAttributeNode(old);
		},
		hasAttributeNS: function(namespaceURI, localName) {
			return this.getAttributeNodeNS(namespaceURI, localName) != null;
		},
		getAttributeNS: function(namespaceURI, localName) {
			var attr = this.getAttributeNodeNS(namespaceURI, localName);
			return attr && attr.value || "";
		},
		setAttributeNS: function(namespaceURI, qualifiedName, value) {
			var attr = this.ownerDocument.createAttributeNS(namespaceURI, qualifiedName);
			attr.value = attr.nodeValue = "" + value;
			this.setAttributeNode(attr);
		},
		getAttributeNodeNS: function(namespaceURI, localName) {
			return this.attributes.getNamedItemNS(namespaceURI, localName);
		},
		getElementsByTagName: function(tagName) {
			return new LiveNodeList(this, function(base) {
				var ls = [];
				_visitNode(base, function(node) {
					if (node !== base && node.nodeType == ELEMENT_NODE && (tagName === "*" || node.tagName == tagName)) ls.push(node);
				});
				return ls;
			});
		},
		getElementsByTagNameNS: function(namespaceURI, localName) {
			return new LiveNodeList(this, function(base) {
				var ls = [];
				_visitNode(base, function(node) {
					if (node !== base && node.nodeType === ELEMENT_NODE && (namespaceURI === "*" || node.namespaceURI === namespaceURI) && (localName === "*" || node.localName == localName)) ls.push(node);
				});
				return ls;
			});
		}
	};
	Document.prototype.getElementsByTagName = Element.prototype.getElementsByTagName;
	Document.prototype.getElementsByTagNameNS = Element.prototype.getElementsByTagNameNS;
	_extends(Element, Node);
	function Attr() {}
	Attr.prototype.nodeType = ATTRIBUTE_NODE;
	_extends(Attr, Node);
	function CharacterData() {}
	CharacterData.prototype = {
		data: "",
		substringData: function(offset, count) {
			return this.data.substring(offset, offset + count);
		},
		appendData: function(text) {
			text = this.data + text;
			this.nodeValue = this.data = text;
			this.length = text.length;
		},
		insertData: function(offset, text) {
			this.replaceData(offset, 0, text);
		},
		appendChild: function(newChild) {
			throw new Error(ExceptionMessage[HIERARCHY_REQUEST_ERR]);
		},
		deleteData: function(offset, count) {
			this.replaceData(offset, count, "");
		},
		replaceData: function(offset, count, text) {
			var start = this.data.substring(0, offset);
			var end = this.data.substring(offset + count);
			text = start + text + end;
			this.nodeValue = this.data = text;
			this.length = text.length;
		}
	};
	_extends(CharacterData, Node);
	function Text() {}
	Text.prototype = {
		nodeName: "#text",
		nodeType: TEXT_NODE,
		splitText: function(offset) {
			var text = this.data;
			var newText = text.substring(offset);
			text = text.substring(0, offset);
			this.data = this.nodeValue = text;
			this.length = text.length;
			var newNode = this.ownerDocument.createTextNode(newText);
			if (this.parentNode) this.parentNode.insertBefore(newNode, this.nextSibling);
			return newNode;
		}
	};
	_extends(Text, CharacterData);
	function Comment() {}
	Comment.prototype = {
		nodeName: "#comment",
		nodeType: COMMENT_NODE
	};
	_extends(Comment, CharacterData);
	function CDATASection() {}
	CDATASection.prototype = {
		nodeName: "#cdata-section",
		nodeType: CDATA_SECTION_NODE
	};
	_extends(CDATASection, CharacterData);
	function DocumentType() {}
	DocumentType.prototype.nodeType = DOCUMENT_TYPE_NODE;
	_extends(DocumentType, Node);
	function Notation() {}
	Notation.prototype.nodeType = NOTATION_NODE;
	_extends(Notation, Node);
	function Entity() {}
	Entity.prototype.nodeType = ENTITY_NODE;
	_extends(Entity, Node);
	function EntityReference() {}
	EntityReference.prototype.nodeType = ENTITY_REFERENCE_NODE;
	_extends(EntityReference, Node);
	function DocumentFragment() {}
	DocumentFragment.prototype.nodeName = "#document-fragment";
	DocumentFragment.prototype.nodeType = DOCUMENT_FRAGMENT_NODE;
	_extends(DocumentFragment, Node);
	function ProcessingInstruction() {}
	ProcessingInstruction.prototype.nodeType = PROCESSING_INSTRUCTION_NODE;
	_extends(ProcessingInstruction, Node);
	function XMLSerializer() {}
	XMLSerializer.prototype.serializeToString = function(node, isHtml, nodeFilter) {
		return nodeSerializeToString.call(node, isHtml, nodeFilter);
	};
	Node.prototype.toString = nodeSerializeToString;
	function nodeSerializeToString(isHtml, nodeFilter) {
		var buf = [];
		var refNode = this.nodeType == 9 && this.documentElement || this;
		var prefix = refNode.prefix;
		var uri = refNode.namespaceURI;
		if (uri && prefix == null) {
			var prefix = refNode.lookupPrefix(uri);
			if (prefix == null) var visibleNamespaces = [{
				namespace: uri,
				prefix: null
			}];
		}
		serializeToString(this, buf, isHtml, nodeFilter, visibleNamespaces);
		return buf.join("");
	}
	function needNamespaceDefine(node, isHTML, visibleNamespaces) {
		var prefix = node.prefix || "";
		var uri = node.namespaceURI;
		if (!prefix && !uri) return false;
		if (prefix === "xml" && uri === "http://www.w3.org/XML/1998/namespace" || uri == "http://www.w3.org/2000/xmlns/") return false;
		var i = visibleNamespaces.length;
		while (i--) {
			var ns = visibleNamespaces[i];
			if (ns.prefix == prefix) return ns.namespace != uri;
		}
		return true;
	}
	function serializeToString(node, buf, isHTML, nodeFilter, visibleNamespaces) {
		if (nodeFilter) {
			node = nodeFilter(node);
			if (node) {
				if (typeof node == "string") {
					buf.push(node);
					return;
				}
			} else return;
		}
		switch (node.nodeType) {
			case ELEMENT_NODE:
				if (!visibleNamespaces) visibleNamespaces = [];
				visibleNamespaces.length;
				var attrs = node.attributes;
				var len = attrs.length;
				var child = node.firstChild;
				var nodeName = node.tagName;
				isHTML = htmlns === node.namespaceURI || isHTML;
				buf.push("<", nodeName);
				for (var i = 0; i < len; i++) {
					var attr = attrs.item(i);
					if (attr.prefix == "xmlns") visibleNamespaces.push({
						prefix: attr.localName,
						namespace: attr.value
					});
					else if (attr.nodeName == "xmlns") visibleNamespaces.push({
						prefix: "",
						namespace: attr.value
					});
				}
				for (var i = 0; i < len; i++) {
					var attr = attrs.item(i);
					if (needNamespaceDefine(attr, isHTML, visibleNamespaces)) {
						var prefix = attr.prefix || "";
						var uri = attr.namespaceURI;
						var ns = prefix ? " xmlns:" + prefix : " xmlns";
						buf.push(ns, "=\"", uri, "\"");
						visibleNamespaces.push({
							prefix,
							namespace: uri
						});
					}
					serializeToString(attr, buf, isHTML, nodeFilter, visibleNamespaces);
				}
				if (needNamespaceDefine(node, isHTML, visibleNamespaces)) {
					var prefix = node.prefix || "";
					var uri = node.namespaceURI;
					if (uri) {
						var ns = prefix ? " xmlns:" + prefix : " xmlns";
						buf.push(ns, "=\"", uri, "\"");
						visibleNamespaces.push({
							prefix,
							namespace: uri
						});
					}
				}
				if (child || isHTML && !/^(?:meta|link|img|br|hr|input)$/i.test(nodeName)) {
					buf.push(">");
					if (isHTML && /^script$/i.test(nodeName)) while (child) {
						if (child.data) buf.push(child.data);
						else serializeToString(child, buf, isHTML, nodeFilter, visibleNamespaces);
						child = child.nextSibling;
					}
					else while (child) {
						serializeToString(child, buf, isHTML, nodeFilter, visibleNamespaces);
						child = child.nextSibling;
					}
					buf.push("</", nodeName, ">");
				} else buf.push("/>");
				return;
			case DOCUMENT_NODE:
			case DOCUMENT_FRAGMENT_NODE:
				var child = node.firstChild;
				while (child) {
					serializeToString(child, buf, isHTML, nodeFilter, visibleNamespaces);
					child = child.nextSibling;
				}
				return;
			case ATTRIBUTE_NODE:
 /**
			* Well-formedness constraint: No < in Attribute Values
			* The replacement text of any entity referred to directly or indirectly in an attribute value must not contain a <.
			* @see https://www.w3.org/TR/xml/#CleanAttrVals
			* @see https://www.w3.org/TR/xml/#NT-AttValue
			*/
			return buf.push(" ", node.name, "=\"", node.value.replace(/[<&"]/g, _xmlEncoder), "\"");
			case TEXT_NODE:
 /**
			* The ampersand character (&) and the left angle bracket (<) must not appear in their literal form,
			* except when used as markup delimiters, or within a comment, a processing instruction, or a CDATA section.
			* If they are needed elsewhere, they must be escaped using either numeric character references or the strings
			* `&amp;` and `&lt;` respectively.
			* The right angle bracket (>) may be represented using the string " &gt; ", and must, for compatibility,
			* be escaped using either `&gt;` or a character reference when it appears in the string `]]>` in content,
			* when that string is not marking the end of a CDATA section.
			*
			* In the content of elements, character data is any string of characters
			* which does not contain the start-delimiter of any markup
			* and does not include the CDATA-section-close delimiter, `]]>`.
			*
			* @see https://www.w3.org/TR/xml/#NT-CharData
			*/
			return buf.push(node.data.replace(/[<&]/g, _xmlEncoder).replace(/]]>/g, "]]&gt;"));
			case CDATA_SECTION_NODE: return buf.push("<![CDATA[", node.data, "]]>");
			case COMMENT_NODE: return buf.push("<!--", node.data, "-->");
			case DOCUMENT_TYPE_NODE:
				var pubid = node.publicId;
				var sysid = node.systemId;
				buf.push("<!DOCTYPE ", node.name);
				if (pubid) {
					buf.push(" PUBLIC ", pubid);
					if (sysid && sysid != ".") buf.push(" ", sysid);
					buf.push(">");
				} else if (sysid && sysid != ".") buf.push(" SYSTEM ", sysid, ">");
				else {
					var sub = node.internalSubset;
					if (sub) buf.push(" [", sub, "]");
					buf.push(">");
				}
				return;
			case PROCESSING_INSTRUCTION_NODE: return buf.push("<?", node.target, " ", node.data, "?>");
			case ENTITY_REFERENCE_NODE: return buf.push("&", node.nodeName, ";");
			default: buf.push("??", node.nodeName);
		}
	}
	function importNode(doc, node, deep) {
		var node2;
		switch (node.nodeType) {
			case ELEMENT_NODE:
				node2 = node.cloneNode(false);
				node2.ownerDocument = doc;
			case DOCUMENT_FRAGMENT_NODE: break;
			case ATTRIBUTE_NODE:
				deep = true;
				break;
		}
		if (!node2) node2 = node.cloneNode(false);
		node2.ownerDocument = doc;
		node2.parentNode = null;
		if (deep) {
			var child = node.firstChild;
			while (child) {
				node2.appendChild(importNode(doc, child, deep));
				child = child.nextSibling;
			}
		}
		return node2;
	}
	function cloneNode(doc, node, deep) {
		var node2 = new node.constructor();
		for (var n in node) {
			var v = node[n];
			if (typeof v != "object") {
				if (v != node2[n]) node2[n] = v;
			}
		}
		if (node.childNodes) node2.childNodes = new NodeList();
		node2.ownerDocument = doc;
		switch (node2.nodeType) {
			case ELEMENT_NODE:
				var attrs = node.attributes;
				var attrs2 = node2.attributes = new NamedNodeMap();
				var len = attrs.length;
				attrs2._ownerElement = node2;
				for (var i = 0; i < len; i++) node2.setAttributeNode(cloneNode(doc, attrs.item(i), true));
				break;
			case ATTRIBUTE_NODE: deep = true;
		}
		if (deep) {
			var child = node.firstChild;
			while (child) {
				node2.appendChild(cloneNode(doc, child, deep));
				child = child.nextSibling;
			}
		}
		return node2;
	}
	function __set__(object, key, value) {
		object[key] = value;
	}
	try {
		if (Object.defineProperty) {
			Object.defineProperty(LiveNodeList.prototype, "length", { get: function() {
				_updateLiveList(this);
				return this.$$length;
			} });
			Object.defineProperty(Node.prototype, "textContent", {
				get: function() {
					return getTextContent(this);
				},
				set: function(data) {
					switch (this.nodeType) {
						case ELEMENT_NODE:
						case DOCUMENT_FRAGMENT_NODE:
							while (this.firstChild) this.removeChild(this.firstChild);
							if (data || String(data)) this.appendChild(this.ownerDocument.createTextNode(data));
							break;
						default:
							this.data = data;
							this.value = data;
							this.nodeValue = data;
					}
				}
			});
			function getTextContent(node) {
				switch (node.nodeType) {
					case ELEMENT_NODE:
					case DOCUMENT_FRAGMENT_NODE:
						var buf = [];
						node = node.firstChild;
						while (node) {
							if (node.nodeType !== 7 && node.nodeType !== 8) buf.push(getTextContent(node));
							node = node.nextSibling;
						}
						return buf.join("");
					default: return node.nodeValue;
				}
			}
			__set__ = function(object, key, value) {
				object["$$" + key] = value;
			};
		}
	} catch (e) {}
	exports.Node = Node;
	exports.DOMException = DOMException;
	exports.DOMImplementation = DOMImplementation;
	exports.XMLSerializer = XMLSerializer;
}));
//#endregion
//#region node_modules/xmldom/lib/dom-parser.js
var require_dom_parser = /* @__PURE__ */ __commonJSMin(((exports) => {
	function DOMParser(options) {
		this.options = options || { locator: {} };
	}
	DOMParser.prototype.parseFromString = function(source, mimeType) {
		var options = this.options;
		var sax = new XMLReader();
		var domBuilder = options.domBuilder || new DOMHandler();
		var errorHandler = options.errorHandler;
		var locator = options.locator;
		var defaultNSMap = options.xmlns || {};
		var isHTML = /\/x?html?$/.test(mimeType);
		var entityMap = isHTML ? htmlEntity.entityMap : {
			"lt": "<",
			"gt": ">",
			"amp": "&",
			"quot": "\"",
			"apos": "'"
		};
		if (locator) domBuilder.setDocumentLocator(locator);
		sax.errorHandler = buildErrorHandler(errorHandler, domBuilder, locator);
		sax.domBuilder = options.domBuilder || domBuilder;
		if (isHTML) defaultNSMap[""] = "http://www.w3.org/1999/xhtml";
		defaultNSMap.xml = defaultNSMap.xml || "http://www.w3.org/XML/1998/namespace";
		if (source && typeof source === "string") sax.parse(source, defaultNSMap, entityMap);
		else sax.errorHandler.error("invalid doc source");
		return domBuilder.doc;
	};
	function buildErrorHandler(errorImpl, domBuilder, locator) {
		if (!errorImpl) {
			if (domBuilder instanceof DOMHandler) return domBuilder;
			errorImpl = domBuilder;
		}
		var errorHandler = {};
		var isCallback = errorImpl instanceof Function;
		locator = locator || {};
		function build(key) {
			var fn = errorImpl[key];
			if (!fn && isCallback) fn = errorImpl.length == 2 ? function(msg) {
				errorImpl(key, msg);
			} : errorImpl;
			errorHandler[key] = fn && function(msg) {
				fn("[xmldom " + key + "]	" + msg + _locator(locator));
			} || function() {};
		}
		build("warning");
		build("error");
		build("fatalError");
		return errorHandler;
	}
	/**
	* +ContentHandler+ErrorHandler
	* +LexicalHandler+EntityResolver2
	* -DeclHandler-DTDHandler
	*
	* DefaultHandler:EntityResolver, DTDHandler, ContentHandler, ErrorHandler
	* DefaultHandler2:DefaultHandler,LexicalHandler, DeclHandler, EntityResolver2
	* @link http://www.saxproject.org/apidoc/org/xml/sax/helpers/DefaultHandler.html
	*/
	function DOMHandler() {
		this.cdata = false;
	}
	function position(locator, node) {
		node.lineNumber = locator.lineNumber;
		node.columnNumber = locator.columnNumber;
	}
	/**
	* @see org.xml.sax.ContentHandler#startDocument
	* @link http://www.saxproject.org/apidoc/org/xml/sax/ContentHandler.html
	*/
	DOMHandler.prototype = {
		startDocument: function() {
			this.doc = new DOMImplementation().createDocument(null, null, null);
			if (this.locator) this.doc.documentURI = this.locator.systemId;
		},
		startElement: function(namespaceURI, localName, qName, attrs) {
			var doc = this.doc;
			var el = doc.createElementNS(namespaceURI, qName || localName);
			var len = attrs.length;
			appendElement(this, el);
			this.currentElement = el;
			this.locator && position(this.locator, el);
			for (var i = 0; i < len; i++) {
				var namespaceURI = attrs.getURI(i);
				var value = attrs.getValue(i);
				var qName = attrs.getQName(i);
				var attr = doc.createAttributeNS(namespaceURI, qName);
				this.locator && position(attrs.getLocator(i), attr);
				attr.value = attr.nodeValue = value;
				el.setAttributeNode(attr);
			}
		},
		endElement: function(namespaceURI, localName, qName) {
			var current = this.currentElement;
			current.tagName;
			this.currentElement = current.parentNode;
		},
		startPrefixMapping: function(prefix, uri) {},
		endPrefixMapping: function(prefix) {},
		processingInstruction: function(target, data) {
			var ins = this.doc.createProcessingInstruction(target, data);
			this.locator && position(this.locator, ins);
			appendElement(this, ins);
		},
		ignorableWhitespace: function(ch, start, length) {},
		characters: function(chars, start, length) {
			chars = _toString.apply(this, arguments);
			if (chars) {
				if (this.cdata) var charNode = this.doc.createCDATASection(chars);
				else var charNode = this.doc.createTextNode(chars);
				if (this.currentElement) this.currentElement.appendChild(charNode);
				else if (/^\s*$/.test(chars)) this.doc.appendChild(charNode);
				this.locator && position(this.locator, charNode);
			}
		},
		skippedEntity: function(name) {},
		endDocument: function() {
			this.doc.normalize();
		},
		setDocumentLocator: function(locator) {
			if (this.locator = locator) locator.lineNumber = 0;
		},
		comment: function(chars, start, length) {
			chars = _toString.apply(this, arguments);
			var comm = this.doc.createComment(chars);
			this.locator && position(this.locator, comm);
			appendElement(this, comm);
		},
		startCDATA: function() {
			this.cdata = true;
		},
		endCDATA: function() {
			this.cdata = false;
		},
		startDTD: function(name, publicId, systemId) {
			var impl = this.doc.implementation;
			if (impl && impl.createDocumentType) {
				var dt = impl.createDocumentType(name, publicId, systemId);
				this.locator && position(this.locator, dt);
				appendElement(this, dt);
			}
		},
		/**
		* @see org.xml.sax.ErrorHandler
		* @link http://www.saxproject.org/apidoc/org/xml/sax/ErrorHandler.html
		*/
		warning: function(error) {
			console.warn("[xmldom warning]	" + error, _locator(this.locator));
		},
		error: function(error) {
			console.error("[xmldom error]	" + error, _locator(this.locator));
		},
		fatalError: function(error) {
			throw new ParseError(error, this.locator);
		}
	};
	function _locator(l) {
		if (l) return "\n@" + (l.systemId || "") + "#[line:" + l.lineNumber + ",col:" + l.columnNumber + "]";
	}
	function _toString(chars, start, length) {
		if (typeof chars == "string") return chars.substr(start, length);
		else {
			if (chars.length >= start + length || start) return new java.lang.String(chars, start, length) + "";
			return chars;
		}
	}
	"endDTD,startEntity,endEntity,attributeDecl,elementDecl,externalEntityDecl,internalEntityDecl,resolveEntity,getExternalSubset,notationDecl,unparsedEntityDecl".replace(/\w+/g, function(key) {
		DOMHandler.prototype[key] = function() {
			return null;
		};
	});
	function appendElement(hander, node) {
		if (!hander.currentElement) hander.doc.appendChild(node);
		else hander.currentElement.appendChild(node);
	}
	var htmlEntity = require_entities();
	var sax = require_sax();
	var XMLReader = sax.XMLReader;
	var ParseError = sax.ParseError;
	var DOMImplementation = exports.DOMImplementation = require_dom().DOMImplementation;
	exports.XMLSerializer = require_dom().XMLSerializer;
	exports.DOMParser = DOMParser;
	exports.__DOMHandler = DOMHandler;
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-to-qml.js
var require_ocad_to_qml = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { LineSymbolType, AreaSymbolType } = require_symbol_types();
	var { patternToSvg, createSvgNode } = require_ocad_to_svg();
	var uuidv4 = require_v4();
	var _global = getGlobal();
	var DOMImplementation = _global.DOMImplementation ? _global.DOMImplementation : new (require_dom_parser()).DOMImplementation();
	var XMLSerializer = _global.XMLSerializer ? _global.XMLSerializer : require_dom_parser().XMLSerializer;
	var defaultOptions = {
		generateSymbolElements: true,
		exportHidden: false
	};
	/**
	* @typedef {object} NodeDef
	//  * @property {string=} id
	//  * @property {string} type
	//  * @property {Record<string, string|number>=} attrs
	//  * @property {NodeDef[]=} children
	*/
	module.exports = function ocadToQml(ocadFile, options) {
		options = {
			...defaultOptions,
			...options
		};
		const usedSymbols = usedSymbolNumbers(ocadFile).map((symNum) => ocadFile.symbols.find((s) => symNum === s.symNum)).filter((s) => s);
		const root = {
			type: "qgis",
			attrs: {
				simplifyMaxScale: 1,
				minScale: 1e8,
				readOnly: 0,
				simplifyDrawingTol: 1,
				hasScaleBasedVisibilityFlag: 0,
				simplifyAlgorithm: 0,
				maxScale: 0,
				labelsEnabled: 0,
				styleCategories: "AllStyleCategories",
				simplifyDrawingHints: 1,
				simplifyLocal: 1
			},
			children: [{
				type: "renderer-v2",
				attrs: {
					forceraster: 0,
					symbollevels: 0,
					type: "RuleRenderer",
					enableorderby: 0
				},
				children: [{
					type: "rules",
					attrs: { key: `{${uuidv4()}}` },
					children: usedSymbols.map((sym, i) => ({
						type: "rule",
						attrs: {
							key: `{${uuidv4()}}`,
							symbol: i,
							label: `${Math.floor(sym.symNum / 1e3)}.${sym.symNum % 1e3} ${sym.description}`,
							filter: `sym=${sym.symNum}`
						}
					}))
				}, {
					type: "symbols",
					children: usedSymbols.map((sym, i) => ({
						...symbolToQml(ocadFile.getCrs().scale, ocadFile.colors, sym),
						type: "symbol",
						attrs: {
							name: i,
							clip_to_extent: 1,
							alpha: 1,
							type: sym.type === LineSymbolType ? "line" : sym.type === AreaSymbolType ? "fill" : "",
							force_rhr: 0
						}
					})).sort((a, b) => a.order - b.order)
				}]
			}]
		};
		return createXmlNode(DOMImplementation.createDocument(null, "xml", null), root);
	};
	/**
	*
	* @param {Document} document
	* @param {NodeDef} n
	* @returns {HTMLElement}
	*/
	var createXmlNode = (document, n) => {
		const node = document.createElement(n.type);
		if (n.id) node.id = n.id;
		if (n.attrs) for (const attrName in n.attrs) node.setAttribute(attrName, n.attrs[attrName].toString());
		if (n.children) for (const child of n.children) node.appendChild(createXmlNode(document, child));
		return node;
	};
	var usedSymbolNumbers = (ocadFile) => ocadFile.objects.reduce((a, f) => {
		const symbolNum = f.sym;
		if (!a.idSet.has(symbolNum)) {
			a.symbolNums.push(symbolNum);
			a.idSet.add(symbolNum);
		}
		return a;
	}, {
		symbolNums: [],
		idSet: /* @__PURE__ */ new Set()
	}).symbolNums;
	/**
	* Transform a symbol to QML
	* @param {number} scale
	* @param {*} colors
	* @param {import("./ocad-reader/symbol").BaseSymbolDef} sym
	* @returns {{children: NodeDef[]}}
	*/
	var symbolToQml = (scale, colors, sym) => {
		/** @type {NodeDef[]} */
		let children;
		switch (sym.type) {
			case LineSymbolType: {
				if (sym.type !== 2) throw new Error("Symbol mismatch: area object with non-area symbol");
				const lineColor = colors[sym.lineColor];
				if (lineColor) {
					const baseMainGap = sym.mainGap;
					const baseMainLength = sym.mainLength;
					children = [{
						type: "layer",
						attrs: {
							class: "SimpleLine",
							pass: 1e3 - lineColor.renderOrder,
							enabled: 1,
							locked: 0
						},
						children: [
							prop("line_color", Array.from(lineColor.rgbArray).concat([255]).join(",")),
							prop("line_style", "solid"),
							prop("line_width", toMapUnit(scale, sym.lineWidth)),
							prop("line_width_unit", "MapUnit"),
							prop("joinstyle", "bevel"),
							prop("capstyle", "flat")
						].concat(baseMainGap && baseMainLength ? [
							prop("customdash", [baseMainLength, baseMainGap].map((x) => toMapUnit(scale, x)).join(";")),
							prop("customdash_unit", "MapUnit"),
							prop("use_custom_dash", 1)
						] : [])
					}];
				}
				break;
			}
			case AreaSymbolType: {
				if (sym.type !== 3) throw new Error("Symbol mismatch: area object with non-area symbol");
				const fillColor = colors[sym.fillColor];
				const hasPatternFill = sym.hatchMode || sym.structMode;
				children = [];
				if (fillColor && (!hasPatternFill || sym.fillOn)) children.push({
					type: "layer",
					attrs: {
						class: "SimpleFill",
						pass: 1e3 - fillColor.renderOrder,
						enabled: 1,
						locked: 0
					},
					children: [
						prop("color", Array.from(fillColor.rgbArray).concat([255]).join(",")),
						prop("outline_style", "no"),
						prop("style", "solid")
					]
				});
				if (fillColor && hasPatternFill) {
					const patterns = patternToSvg(colors, sym);
					children = children.concat(patterns.map((p) => svgPatternToFill(scale, fillColor, p)));
				}
				break;
			}
		}
		return { children };
	};
	/**
	*
	* @param {number} scale
	* @param {import('./ocad-reader/ocad-file').Color} fillColor
	* @param {NodeDef} pattern
	* @returns {NodeDef}
	*/
	var svgPatternToFill = (scale, fillColor, pattern) => {
		const { width, height, patternTransform } = pattern.attrs;
		const angle = getPatternRotation(patternTransform);
		const svgDoc = DOMImplementation.createDocument("http://www.w3.org/2000/svg", "svg", null);
		svgDoc.firstChild.setAttribute("width", width);
		svgDoc.firstChild.setAttribute("height", height);
		pattern.children.forEach((c) => svgDoc.firstChild.appendChild(createSvgNode(svgDoc, c)));
		const serializedSvg = new XMLSerializer().serializeToString(svgDoc);
		const patternBase64 = "base64:" + Buffer.from(serializedSvg).toString("base64");
		return {
			type: "layer",
			attrs: {
				class: "SVGFill",
				pass: 1e3 - fillColor.renderOrder,
				enabled: 1,
				locked: 0
			},
			children: [
				prop("pattern_width_unit", "MapUnit"),
				prop("outline_style", "no"),
				prop("svgFile", patternBase64),
				prop("width", toMapUnit(scale, width)),
				prop("height", toMapUnit(scale, height)),
				prop("angle", angle),
				{
					type: "symbol",
					attrs: {
						clip_to_extent: 1,
						alpha: 1,
						type: "line",
						force_rhr: 0
					},
					children: [{
						type: "layer",
						attrs: {
							class: "SimpleLine",
							locked: 0,
							enabled: 1,
							pass: 0
						},
						children: [
							prop("line_color", "0,0,0,0"),
							prop("line_style", "solid"),
							prop("line_width", 0),
							prop("line_width_unit", "MapUnit"),
							prop("joinstyle", "bevel"),
							prop("capstyle", "flat")
						]
					}]
				}
			]
		};
	};
	var prop = (k, v) => ({
		type: "prop",
		attrs: {
			k,
			v
		}
	});
	var toMapUnit = (scale, x) => x / (100 * 1e3) * scale;
	function getGlobal() {
		if (typeof global !== "undefined") return global;
		else if (typeof window !== "undefined") return window;
		else return {};
	}
	function getPatternRotation(patternTransform) {
		if (patternTransform) {
			const rotationMatch = /rotate\((.*)\)/.exec(patternTransform);
			if (rotationMatch) return Number(rotationMatch[1]);
		}
		return 0;
	}
}));
//#endregion
//#region node_modules/ocad2geojson/src/ocad-to-mapbox-gl-style.js
var require_ocad_to_mapbox_gl_style = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var flatten = require_arr_flatten();
	var { PointSymbolType, LineSymbolType, AreaSymbolType, TextSymbolType, DblFillColorOn } = require_symbol_types();
	var { LineElementType, AreaElementType, CircleElementType, DotElementType } = require_symbol_element_types();
	var { HorizontalAlignCenter, HorizontalAlignRight, VerticalAlignMiddle } = require_text_symbol();
	module.exports = function ocadToMapboxGlStyle(ocadFile, options) {
		options = {
			scaleFactor: ocadFile.getCrs().scale / 15e3,
			...options
		};
		const usedSymbols = usedSymbolNumbers(ocadFile).map((symNum) => ocadFile.symbols.find((s) => symNum === s.symNum)).filter((s) => s);
		const metadata = (symbol) => {
			const metadata = Object.keys(symbol).filter((k) => symbol[k] !== Object(symbol[k])).reduce((a, k) => {
				a[k] = symbol[k];
				return a;
			}, {});
			return (layer) => ({
				...layer,
				metadata: {
					...metadata,
					sort: layer.metadata.sort
				}
			});
		};
		const symbolLayers = flatten(usedSymbols.map((symbol) => (symbolToMapboxLayer(symbol, ocadFile.colors, options) || []).map(metadata(symbol))));
		const elementLayers = flatten(usedSymbols.map((symbol) => (symbolElementsToMapboxLayer(symbol, ocadFile.colors, options) || []).map(metadata(symbol))));
		return symbolLayers.concat(elementLayers).sort((a, b) => b.metadata.sort - a.metadata.sort);
	};
	var usedSymbolNumbers = (ocadFile) => ocadFile.objects.reduce((a, f) => {
		const symbolNum = f.sym;
		if (!a.idSet.has(symbolNum)) {
			a.symbolNums.push(symbolNum);
			a.idSet.add(symbolNum);
		}
		return a;
	}, {
		symbolNums: [],
		idSet: /* @__PURE__ */ new Set()
	}).symbolNums;
	var symbolToMapboxLayer = (symbol, colors, options) => {
		const id = `symbol-${symbol.symNum}`;
		const filter = [
			"==",
			["get", "sym"],
			symbol.symNum
		];
		let layerFactory;
		switch (symbol.type) {
			case LineSymbolType:
				layerFactory = lineLayer;
				break;
			case AreaSymbolType:
				layerFactory = areaLayer;
				break;
			case TextSymbolType:
				layerFactory = textLayer;
				break;
		}
		return layerFactory && layerFactory(id, options.source, options.sourceLayer, options.scaleFactor, filter, symbol, colors);
	};
	var symbolElementsToMapboxLayer = (symbol, colors, options) => {
		var elements = [];
		switch (symbol.type) {
			case PointSymbolType:
				elements = symbol.elements.map((e) => [e, "element"]);
				break;
			case LineSymbolType:
				elements = symbol.primSymElements.map((e) => [e, "prim"]).concat(symbol.cornerSymElements.map((e) => [e, "corner"]));
				break;
		}
		return flatten(elements.map(([e, name], i) => createElementLayer(e, name, i, symbol, colors, options)).filter((l) => l));
	};
	var createElementLayer = (element, name, index, symbol, colors, options) => {
		const id = `symbol-${symbol.symNum}-${name}-${index}`;
		const filter = [
			"==",
			["get", "element"],
			`${symbol.symNum}-${name}-${index}`
		];
		switch (element.type) {
			case LineElementType: return lineLayer(id, options.source, options.sourceLayer, options.scaleFactor, filter, element, colors);
			case AreaElementType: return areaLayer(id, options.source, options.sourceLayer, options.scaleFactor, filter, element, colors);
			case CircleElementType:
			case DotElementType: return circleLayer(id, options.source, options.sourceLayer, options.scaleFactor, filter, element, colors);
		}
	};
	var lineLayer = (id, source, sourceLayer, scaleFactor, filter, lineDef, colors) => {
		const createLayer = (id, width, length, gap, color) => {
			if (width <= 0 || color >= colors.length) return;
			const baseWidth = width / 10 * scaleFactor;
			const baseMainLength = length / (10 * baseWidth);
			const baseMainGap = gap / (10 * baseWidth);
			const l = {
				id,
				source,
				"source-layer": sourceLayer,
				type: "line",
				filter,
				paint: {
					"line-color": colors[color].rgb,
					"line-width": expFunc(baseWidth)
				},
				metadata: { sort: colors[color].renderOrder }
			};
			if (baseMainLength && baseMainGap) l.paint["line-dasharray"] = [baseMainLength, baseMainGap];
			return l;
		};
		const isDoubleLine = lineDef.doubleLine && lineDef.doubleLine.dblMode;
		let layers;
		if (!isDoubleLine) layers = [createLayer(id, lineDef.lineWidth, lineDef.mainLength, lineDef.mainGap, lineDef.lineColor !== void 0 ? lineDef.lineColor : lineDef.color)];
		else {
			const dbl = lineDef.doubleLine;
			if (dbl.dblFlags & DblFillColorOn) layers = [dbl.dblLeftWidth > 0 && dbl.dblRightWidth > 0 && createLayer(id, dbl.dblLeftWidth * 1.5 + dbl.dblRightWidth * 1.5 + dbl.dblWidth * 2, dbl.dblLength, dbl.dblGap, dbl.dblLeftColor), createLayer(id + "_fill", dbl.dblWidth * 2, dbl.dblLength, dbl.dblGap, dbl.dblFillColor)];
			else layers = [-dbl.dblWidth - dbl.dblLeftWidth / 2, dbl.dblWidth + dbl.dblRightWidth / 2].map((offset, i) => {
				const l = createLayer(id + "_" + i, i === 0 ? dbl.dblLeftWidth : dbl.dblRightWidth, dbl.dblLength, dbl.dblGap, i === 0 ? dbl.dblLeftColor : dbl.dblRightColor);
				if (l) l.paint["line-offset"] = expFunc(offset / 10 * scaleFactor);
				return l;
			});
		}
		return layers.filter((l) => l);
	};
	var areaLayer = (id, source, sourceLayer, scaleFactor, filter, areaDef, colors) => {
		const fillColorIndex = areaDef.fillOn !== void 0 ? areaDef.fillOn ? areaDef.fillColor : areaDef.colors[0] : areaDef.color;
		return [{
			id,
			source,
			"source-layer": sourceLayer,
			type: "fill",
			filter,
			paint: {
				"fill-color": colors[fillColorIndex].rgb,
				"fill-opacity": areaDef.fillOn === void 0 || areaDef.fillOn ? 1 : areaDef.hatchLineWidth / areaDef.hatchDist || .5
			},
			metadata: { sort: colors[fillColorIndex].renderOrder }
		}];
	};
	var circleLayer = (id, source, sourceLayer, scaleFactor, filter, element, colors) => {
		const layer = {
			id,
			source,
			"source-layer": sourceLayer,
			type: "circle",
			filter,
			paint: {
				"circle-radius": expFunc((element.diameter / 2 / 10 || 1) * scaleFactor),
				"circle-pitch-alignment": "map"
			},
			metadata: { sort: colors[element.color].renderOrder }
		};
		const color = colors[element.color].rgb;
		if (element.type === CircleElementType) {
			const baseWidth = element.lineWidth / 10;
			layer.paint["circle-opacity"] = 0;
			layer.paint["circle-stroke-color"] = color;
			layer.paint["circle-stroke-width"] = expFunc(baseWidth);
		} else layer.paint["circle-color"] = color;
		return [layer];
	};
	var textLayer = (id, source, sourceLayer, scaleFactor, filter, element, colors) => {
		const horizontalAlign = element.getHorizontalAlignment();
		const verticalAlign = element.getVerticalAlignment();
		const justify = horizontalAlign === HorizontalAlignCenter ? "center" : horizontalAlign === HorizontalAlignRight ? "right" : "left";
		const anchor = verticalAlign === VerticalAlignMiddle ? "center" : "top";
		const weightModifier = element.weight > 400 ? " Bold" : "";
		return [{
			id,
			source,
			"source-layer": sourceLayer,
			type: "symbol",
			filter,
			layout: {
				"symbol-placement": "point",
				"text-font": [`Open Sans${`${weightModifier}${element.italic ? " Italic" : !weightModifier ? " Regular" : ""}`}`],
				"text-field": ["get", "text"],
				"text-size": expFunc(element.fontSize / 2.3 * scaleFactor),
				"text-allow-overlap": true,
				"text-ignore-placement": true,
				"text-max-width": Infinity,
				"text-justify": justify,
				"text-anchor": `${anchor}${justify !== "center" ? `-${justify}` : ""}`,
				"text-pitch-alignment": "map",
				"text-rotation-alignment": "map"
			},
			paint: { "text-color": colors[element.fontColor].rgb },
			metadata: { sort: colors[element.fontColor].renderOrder }
		}];
	};
	var expFunc = (base) => ({
		type: "exponential",
		base: 2,
		stops: [[0, base * Math.pow(2, -15)], [24, base * Math.pow(2, 9)]]
	});
}));
//#endregion
//#region node_modules/ocad2geojson/src/index.js
var require_src = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var readOcad = require_ocad_reader();
	var ocadToGeoJson = require_ocad_to_geojson();
	var { ocadToSvg } = require_ocad_to_svg();
	var ocadToQml = require_ocad_to_qml();
	module.exports = {
		readOcad,
		ocadToGeoJson,
		ocadToSvg,
		ocadToMapboxGlStyle: require_ocad_to_mapbox_gl_style(),
		ocadToQml
	};
}));
//#endregion
export default require_src();

//# sourceMappingURL=ocad2geojson.js.map